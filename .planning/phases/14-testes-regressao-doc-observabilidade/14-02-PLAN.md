---
phase: 14-testes-regressao-doc-observabilidade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - leadflow-backend/app/routers/admin.py
  - leadflow-backend/app/main.py
autonomous: true
requirements: [OBS-V2-G2]
must_haves:
  truths:
    - "`GET /admin/grupo-membership/health` retorna campo top-level `cache_hit_rate` (float arredondado ou null se sem lookups)"
    - "Endpoint retorna campo `total_rows_24h` (count das rows criadas nas ultimas 24h)"
    - "Campos existentes (total_rows, last_write_at, writes_by_source_24h, cache_stats, lookup_stats) seguem presentes — nada quebrado"
    - "cache_hit_rate nao levanta ZeroDivisionError em deploy fresco (guard div0 → null)"
    - "BUILD_VERSION em app/main.py = '2026-06-10-v2-2-completo' (deploy v2.2 completo rastreavel via /version)"
  artifacts:
    - path: "leadflow-backend/app/routers/admin.py"
      provides: "healthcheck estendido com cache_hit_rate + total_rows_24h + phase '14-observability'"
      contains: "cache_hit_rate"
    - path: "leadflow-backend/app/main.py"
      provides: "BUILD_VERSION bump"
      contains: "2026-06-10-v2-2-completo"
  key_links:
    - from: "admin.py grupo_membership_health()"
      to: "probe_cache.stats()"
      via: "hits/(hits+misses) com guard div0"
      pattern: "cache_hit_rate"
    - from: "/version endpoint"
      to: "BUILD_VERSION constant"
      via: "main.py response 'build' field"
      pattern: "2026-06-10-v2-2-completo"
---

<objective>
Fechar OBS-V2-G2 estendendo o healthcheck existente com os 2 campos faltantes
(`cache_hit_rate` + `total_rows_24h`) e bumpar BUILD_VERSION para tornar o deploy
v2.2 completo (Fases 12+13 shippadas sem bump) verificavel via /version.

Purpose: Operador detecta cache mascarando falha de persistencia
(cache_hit_rate alto + total_rows_24h=0 em horario ativo = alerta). O bump fecha
o gap de rastreabilidade: BUILD_VERSION ainda esta em
"2026-06-09-lead-in-group-consumer" (valor da Fase 11) apesar das Fases 12 e 13
em prod.

Output: admin.py healthcheck estendido (sem quebrar campos existentes) +
main.py BUILD_VERSION = "2026-06-10-v2-2-completo".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

# Research tem o gap analysis exato do endpoint (linhas 158-202) + snippets prontos
# de cache_hit_rate (480-491) e total_rows_24h (493-509) + Pitfall 4 (div0) e
# Pitfall 5 (BUILD_VERSION nao bumpado).
@.planning/phases/14-testes-regressao-doc-observabilidade/14-RESEARCH.md
@leadflow-backend/CLAUDE.md
</context>

<interfaces>
<!-- Estado ATUAL do source (lido nesta sessao). Usar direto, sem explorar. -->

app/routers/admin.py — endpoint `@router.get("/grupo-membership/health")` em
  `async def grupo_membership_health()` (~linha 1180). Hoje monta `out` com:
  total_rows, last_write_at, writes_by_source_24h, cache_stats (=probe_cache.stats()),
  lookup_stats.lock_dict_size, phase="11-consumer".
  Ja importa: get_supabase, probe_cache, datetime, timedelta.
  Ja faz query de cutoff_24h pra writes_by_source_24h (reusar o padrao gte criado_em).
  Caminho REAL do endpoint: GET /admin/grupo-membership/health (router prefix /admin).
  O criterio OBS-V2-G2 cita "/health/grupo-membership" — discrepancia conhecida;
  ESTENDER o endpoint existente, documentar a discrepancia em comentario (Pitfall 3).

probe_cache.stats() retorna dict com:
  {hits, misses, writes, invalidations, size, maxsize, ttl_seconds}.
  cache_hit_rate = hits/(hits+misses) — guard: None se (hits+misses)==0 (Pitfall 4).

app/main.py:248 — `BUILD_VERSION = "2026-06-09-lead-in-group-consumer"`.
  Consumido em main.py:283 no campo "build" do /version.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Estender healthcheck com cache_hit_rate + total_rows_24h (OBS-V2-G2)</name>
  <files>leadflow-backend/app/routers/admin.py</files>
  <read_first>
    - leadflow-backend/app/routers/admin.py (linhas 1180-1262 — corpo atual de grupo_membership_health(); preservar todos os campos e blocos try/except existentes)
    - .planning/phases/14-testes-regressao-doc-observabilidade/14-RESEARCH.md (linhas 158-202 gap analysis; 480-509 snippets prontos; Pitfall 3 path discrepancy; Pitfall 4 div0)
    - leadflow-backend/app/services/probe_cache.py (stats() return shape — chaves hits/misses)
  </read_first>
  <action>
    Editar `grupo_membership_health()` em admin.py, ADITIVO apenas (nao remover/
    renomear campos existentes — clientes de monitoramento dependem deles):

    1. Apos `out["cache_stats"] = probe_cache.stats()` (ou apos a montagem inicial
       de out), adicionar campo top-level `cache_hit_rate`:
         cs = out["cache_stats"]  # ja e probe_cache.stats()
         _total_lk = cs.get("hits", 0) + cs.get("misses", 0)
         out["cache_hit_rate"] = round(cs["hits"] / _total_lk, 3) if _total_lk > 0 else None
       Guard div0 OBRIGATORIO (Pitfall 4): emitir None quando sem lookups, NUNCA 0.0
       (0.0 enganaria — sugere "tudo miss" em vez de "sem dados").

    2. Adicionar bloco try/except (mesmo estilo dos existentes) que conta rows
       criadas nas ultimas 24h em `total_rows_24h`:
         try:
             cutoff_24h = (datetime.utcnow() - timedelta(hours=24)).isoformat()
             cnt_24h = (sb.table("grupo_membership").select("id", count="exact")
                        .gte("criado_em", cutoff_24h).limit(1).execute())
             out["total_rows_24h"] = getattr(cnt_24h, "count", None) or 0
         except Exception as e:
             out["total_rows_24h_erro"] = str(e)
       (datetime/timedelta ja importados na funcao; cutoff_24h ja existe no bloco
       writes_by_source_24h — pode reusar a variavel se estiver no escopo.)

    3. Trocar `"phase": "11-consumer"` por `"phase": "14-observability"`.

    4. Adicionar comentario curto documentando a discrepancia de path
       (criterio OBS-V2-G2 cita /health/grupo-membership; endpoint real e
       /admin/grupo-membership/health — estendido aqui, sem rota nova).

    NAO criar rota nova /health/grupo-membership. NAO mexer em google_calendar.py
    nem em nada fora desta funcao (CLAUDE.md: cirurgico).
  </action>
  <verify>
    <automated>cd leadflow-backend; python -c "import ast,sys; ast.parse(open('app/routers/admin.py',encoding='utf-8').read()); print('OK syntax')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "cache_hit_rate" leadflow-backend/app/routers/admin.py` retorna >=1
    - `grep -n "total_rows_24h" leadflow-backend/app/routers/admin.py` retorna >=1
    - `grep -n "14-observability" leadflow-backend/app/routers/admin.py` retorna 1
    - `grep -n "total_rows\b" leadflow-backend/app/routers/admin.py` confirma campo all-time ainda presente (nao removido)
    - python ast.parse sem erro de sintaxe
    - Guard div0 presente: `grep -n "_total_lk > 0\|(hits + misses)\|+ misses" leadflow-backend/app/routers/admin.py`
  </acceptance_criteria>
  <done>
    Endpoint retorna cache_hit_rate (float|null) + total_rows_24h; campos antigos
    intactos; phase atualizado; discrepancia de path documentada em comentario.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump BUILD_VERSION para v2.2 completo</name>
  <files>leadflow-backend/app/main.py</files>
  <read_first>
    - leadflow-backend/app/main.py (linha 248 BUILD_VERSION + linha 283 onde e exposto em /version)
    - .planning/phases/14-testes-regressao-doc-observabilidade/14-RESEARCH.md (Pitfall 5 — Fases 12/13 shippadas sem bump; valor atual ainda da Fase 11)
  </read_first>
  <action>
    Editar app/main.py linha 248:
      DE:   BUILD_VERSION = "2026-06-09-lead-in-group-consumer"
      PARA: BUILD_VERSION = "2026-06-10-v2-2-completo"
    String EXATA "2026-06-10-v2-2-completo". So esta linha — nada mais.
    Isto torna o deploy completo de v2.2 (Fases 10-14) verificavel via GET /version.
  </action>
  <verify>
    <automated>cd leadflow-backend; python -c "import ast; ast.parse(open('app/main.py',encoding='utf-8').read()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "2026-06-10-v2-2-completo" leadflow-backend/app/main.py` retorna exatamente 1 (linha 248)
    - `grep -c "2026-06-09-lead-in-group-consumer" leadflow-backend/app/main.py` retorna 0 (valor antigo removido)
    - main.py:283 segue expondo BUILD_VERSION no campo "build" (inalterado)
  </acceptance_criteria>
  <done>
    BUILD_VERSION = "2026-06-10-v2-2-completo"; /version refletira o deploy v2.2
    completo apos redeploy.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → /admin/grupo-membership/health | endpoint sem auth gate (heranca Fase 10/11, AR-11-01 aceito) — so expoe metricas agregadas de ops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Information Disclosure | healthcheck adiciona cache_hit_rate + total_rows_24h sem auth | accept | Apenas metricas agregadas de operacao (taxa de cache, contagem 24h); zero PII, zero conteudo de lead; herda risco aceito AR-11-01 (endpoint /admin/grupo-membership/health ja unauthenticated). Nenhum campo novo expoe telefone/empresa individual |
| T-14-04 | Denial of Service | total_rows_24h faz query count adicional por request | accept | count="exact" com limit(1) e barato (index em criado_em); endpoint de baixa frequencia (operador manual); query envolta em try/except — falha isolada nao derruba o resto do payload |
</threat_model>

<verification>
- python ast.parse OK em admin.py e main.py (sem syntax error)
- grep confirma cache_hit_rate + total_rows_24h + 14-observability em admin.py
- grep confirma BUILD_VERSION = 2026-06-10-v2-2-completo (valor antigo ausente)
- Campos legados do healthcheck preservados
</verification>

<success_criteria>
- OBS-V2-G2 fechado: cache_hit_rate (com guard div0) + total_rows_24h presentes
- Nada quebrado no payload existente do healthcheck
- BUILD_VERSION rastreavel pro deploy v2.2 completo
</success_criteria>

<output>
After completion, create `.planning/phases/14-testes-regressao-doc-observabilidade/14-02-SUMMARY.md`
</output>
