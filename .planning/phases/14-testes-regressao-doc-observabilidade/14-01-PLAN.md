---
phase: 14-testes-regressao-doc-observabilidade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - leadflow-backend/tests/test_v2_regression.py
autonomous: true
requirements: [TEST-V2-G1, TEST-V2-G2, TEST-V2-G3, TEST-V2-G4, TEST-V2-G5, OBS-V2-G1]
must_haves:
  truths:
    - "`pytest tests/test_v2_regression.py -q` reports 4 passed + 1 skipped, 0 failed"
    - "Caso Ana Carla (Path 3) prova source='membership' sem chamar probe"
    - "Caso Rosania (T+138s) prova TABLE_HIT no retry job sem chamar probe"
    - "Caso Fernanda (instance_key mismatch) prova source='orphan', probe nao chamado"
    - "Caso Valquiria (47h) prova job descartado por max_age, probe nao chamado"
    - "TEST-V2-G5 (553891500357) existe como stub pytest.mark.skip com reason 'blocked-pending-data'"
    - "Os 4 marker families [MEMB-WRITE]/[MEMB-LOOKUP]/[PROBE-RETRY]/GSC: estao verificados presentes no source"
  artifacts:
    - path: "leadflow-backend/tests/test_v2_regression.py"
      provides: "5 case-named regression tests (4 pass + 1 skip) + 1 log-marker source-audit test"
      contains: "test_v2_g1_ana_carla_path3_create_group"
  key_links:
    - from: "test_v2_regression.py"
      to: "app.services.grupo_membership.lead_in_group"
      via: "import + await with monkeypatched _query_membership_row"
      pattern: "from app.services.grupo_membership import lead_in_group"
    - from: "test_v2_regression.py"
      to: "app.services.evolution.verificar_lead_no_grupo"
      via: "monkeypatch.setattr probe spy (counts calls)"
      pattern: "monkeypatch.setattr.*app.services.evolution.verificar_lead_no_grupo"
---

<objective>
Bloquear regressao dos 5 casos motivadores v2.2 via suite pytest case-named, e
verificar (via teste de source-audit) que as 4 familias de log marker exigidas
por OBS-V2-G1 estao presentes no codigo de producao.

Purpose: Conclui o ciclo v2.2 — ninguem mexe na inversao webhook-first sem que
Ana Carla / Rosania / Fernanda / Valquiria sigam protegidos. As assertions sao
WRAPPERS finos sobre comportamento ja coberto pelos testes existentes
(test_lead_in_group_decision.py, test_probe_retry.py) — case-named para
rastreabilidade, nao re-implementacao de logica.

Output: `leadflow-backend/tests/test_v2_regression.py` com 6 testes (4 pass +
1 skip + 1 source-audit dos markers).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Research tem o skeleton COMPLETO de test_v2_regression.py (linhas 281-478) +
# o mapa de cobertura existente. USAR como base verbatim, ajustando assinaturas.
@.planning/phases/14-testes-regressao-doc-observabilidade/14-RESEARCH.md

# Codigo do backend e SUBMODULE. Paths reais comecam em leadflow-backend/.
# CLAUDE.md do backend: cirurgico, NAO mexer em google_calendar.py.
@leadflow-backend/CLAUDE.md
</context>

<interfaces>
<!-- Contratos que os testes consomem. NAO explorar o codebase — usar estes. -->

`lead_in_group()` (app/services/grupo_membership.py) retorna dict com chaves:
  {in_group: bool, source: str, last_event_at: ..., instance_key_match: bool}
  source values relevantes: 'membership' (row valida), 'orphan' (instance_key
  mismatch), 'probe' (foi pro Evolution), 'left_group', 'stale'.
  Decisao: consulta `_query_membership_row` PRIMEIRO; so chama probe se row
  ausente OU saiu_em != null. Probe target real = app.services.evolution.verificar_lead_no_grupo.

`_probe_retry_job(empresa_id, grupo_jid, telefone, lid, attempts_left,
  max_age_seconds, enqueued_at, evo_url, evo_key, evo_inst)` — job async.
  Guard: se `now - enqueued_at >= max_age_seconds` → descarta sem probe.
  Antes de probar: consulta `_query_membership_row` → se row presente, TABLE_HIT,
  encerra sem chamar Evolution.

Mocks (helpers de teste ja existentes — reusar padrao):
  - `app.services.grupo_membership._reset_probe_locks_for_tests()`
  - `app.services.probe_cache.clear_all()`
  - `app.services.grupo_membership._query_membership_row` (monkeypatch retorna row|None)
  - `app.services.grupo_membership.get_instance_key` (monkeypatch retorna instance str)

PITFALL CONFIRMADO (research Pitfall 2): patchear
  'app.services.evolution.verificar_lead_no_grupo' (import LOCAL no lead_in_group),
  NUNCA 'app.services.grupo_membership.verificar_lead_no_grupo'.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar test_v2_regression.py com os 5 testes case-named (TEST-V2-G1..G5)</name>
  <files>leadflow-backend/tests/test_v2_regression.py</files>
  <read_first>
    - .planning/phases/14-testes-regressao-doc-observabilidade/14-RESEARCH.md (linhas 281-478 = skeleton COMPLETO do arquivo; copiar verbatim ajustando assinaturas reais)
    - leadflow-backend/tests/test_probe_retry.py (padrao stub APScheduler/supabase em sys.modules + assinatura real de _probe_retry_job)
    - leadflow-backend/tests/test_lead_in_group_decision.py (autouse reset_state fixture + assinatura real de lead_in_group + alvo de patch do probe)
  </read_first>
  <behavior>
    - test_v2_g1_ana_carla_path3_create_group (async): _query_membership_row retorna row
      valida (entrou_em=now, saiu_em=None, instance_key bate) → lead_in_group retorna
      in_group=True, source='membership', probe NAO chamado (spy count==0).
    - test_v2_g2_rosania_table_hit_at_138s (async): enqueued_at = now-138s (< max_age 600),
      _query_membership_row retorna row → _probe_retry_job encerra TABLE_HIT, probe NAO chamado.
    - test_v2_g3_fernanda_orphan_instance_key (async): row com instance_key='bia-rejane',
      get_instance_key retorna 'rejane-leal-mentora' → in_group=False, source='orphan',
      instance_key_match=False, probe NAO chamado.
    - test_v2_g4_valquiria_max_age_discard: enqueued_at = now-47h, max_age=600 →
      _probe_retry_job descarta sem chamar probe (spy count==0).
    - test_v2_g5_lead_553891500357: @pytest.mark.skip(reason="blocked-pending-data: ...").
  </behavior>
  <action>
    Criar leadflow-backend/tests/test_v2_regression.py usando o skeleton do
    14-RESEARCH.md (linhas 281-478) como base. PASSOS:

    1. Copiar o cabecalho de stubs module-level (sys.modules.setdefault para
       "app.scheduler" e "app.db.client") — necessario porque schedule_probe_retry
       importa transitivamente. Padrao identico ao topo de test_probe_retry.py.
    2. Copiar a fixture autouse `reset_state` (probe_cache.clear_all() +
       gm._reset_probe_locks_for_tests()).
    3. Implementar os 5 testes conforme <behavior>. CADA teste tem docstring
       nomeando a pessoa real + data + cenario (rastreabilidade).
    4. VERIFICAR contra os arquivos lidos em <read_first> que:
       - a assinatura real de `lead_in_group(...)` bate (parametros/kwargs);
       - a assinatura real de `_probe_retry_job(...)` bate (incluindo enqueued_at);
       - o alvo de patch do probe e EXATAMENTE
         'app.services.evolution.verificar_lead_no_grupo' (Pitfall 2).
       Ajustar o skeleton se a assinatura real divergir — o skeleton e guia, o
       source e a verdade.
    5. TEST-V2-G5: usar exatamente
       @pytest.mark.skip(reason="blocked-pending-data: requires full webhook trace
       for lead 553891500357 (09/06 incident). Provide timestamps + webhook payloads
       + conversas log to unlock. See REQUIREMENTS.md TEST-V2-G5.")
       — NAO usar `assert True` (Pitfall 6: skip deve ser visivel em CI).
    6. Para test_v2_g4 preferir `@pytest.mark.asyncio` + `async def` em vez de
       `asyncio.get_event_loop().run_until_complete()` (Assumption A1 do research —
       event loop policy mais robusta); ajustar se o resto da suite usar outra config.

    NAO duplicar logica pesada (Pitfall 1): setup inline minimo (3-4 linhas de
    MagicMock por teste). Se o arquivo passar de ~150 linhas, esta duplicando.
  </action>
  <verify>
    <automated>cd leadflow-backend; python -m pytest tests/test_v2_regression.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd leadflow-backend; python -m pytest tests/test_v2_regression.py -q` → "4 passed, 1 skipped" (0 failed)
    - `grep -c "def test_v2_g" leadflow-backend/tests/test_v2_regression.py` → 5
    - `grep "blocked-pending-data" leadflow-backend/tests/test_v2_regression.py` encontra a reason no skip
    - `grep "app.services.evolution.verificar_lead_no_grupo" leadflow-backend/tests/test_v2_regression.py` (alvo de patch correto)
    - Nenhum `assert True` substituindo o skip de G5
  </acceptance_criteria>
  <done>
    test_v2_regression.py existe; 4 testes passam, G5 pulado com reason correta;
    probe spy confirma 0 chamadas nos 4 casos (table-first / orphan / max_age).
  </done>
</task>

<task type="auto">
  <name>Task 2: Teste de source-audit dos 4 log markers (OBS-V2-G1)</name>
  <files>leadflow-backend/tests/test_v2_regression.py</files>
  <read_first>
    - .planning/phases/14-testes-regressao-doc-observabilidade/14-RESEARCH.md (secao "Structured Logs: OBS-V2-G1 Audit", linhas 204-222 — confirma os 4 markers ja presentes + a resolucao de naming [GRUPO-STATE-CHANGE]→GSC:)
  </read_first>
  <action>
    Adicionar UM teste `test_v2_obs_g1_log_markers_present` (sync) ao final de
    test_v2_regression.py. OBS-V2-G1 ja esta IMPLEMENTADO (os 4 markers existem em
    prod); este teste e a GUARDA de regressao + documentacao executavel da
    resolucao de naming.

    Usar o padrao source-inspection do research (pathlib.Path read_text), NAO
    importar modulos pesados:

    1. Ler app/services/grupo_membership.py → assert presenca de "[MEMB-WRITE]",
       "[MEMB-LOOKUP]" e "[PROBE-RETRY]" (todos literais no source).
    2. Ler app/services/grupo_state.py → assert presenca do prefixo "GSC:"
       (satisfaz o criterio [GRUPO-STATE-CHANGE] — Phase 13 escolheu GSC: para
       evitar colisao com .like("GRUPO_STATE:%") em get_grupo_state).
    3. Docstring do teste documenta o mapeamento criterio→prefixo real
       ([GRUPO-STATE-CHANGE] = GSC: marker em conversas).

    Resolver paths via pathlib relativo ao __file__ do teste
    (parent.parent / "app" / "services" / ...), igual ao padrao PROBE-RETRY-02 de
    test_probe_retry.py.
  </action>
  <verify>
    <automated>cd leadflow-backend; python -m pytest tests/test_v2_regression.py::test_v2_obs_g1_log_markers_present -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd leadflow-backend; python -m pytest tests/test_v2_regression.py -q` → "5 passed, 1 skipped"
    - Os 4 markers conferidos: `grep -rn "\[MEMB-WRITE\]" leadflow-backend/app/services/grupo_membership.py` retorna >=1; idem [MEMB-LOOKUP], [PROBE-RETRY]; `grep -n "GSC:" leadflow-backend/app/services/grupo_state.py` retorna >=1
    - Docstring do teste cita o mapeamento [GRUPO-STATE-CHANGE]→GSC:
  </acceptance_criteria>
  <done>
    Teste source-audit verde; falha se algum dos 4 markers for removido do source
    em refactor futuro. OBS-V2-G1 travado contra regressao.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum novo) | Plan so adiciona arquivo de teste. Sem entrada de usuario, sem rede, sem auth nova. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | test_v2_regression.py (assertions falsas/placeholder) | mitigate | G5 usa pytest.mark.skip com reason explicita, nunca `assert True` (Pitfall 6); CI mostra skip visivelmente |
| T-14-02 | Information Disclosure | docstrings dos testes citam telefones reais (5514998151089 etc.) | accept | Telefones ja constam em STATE.md/REQUIREMENTS.md commitados; sao chave de rastreabilidade dos casos; repo privado |
</threat_model>

<verification>
- `cd leadflow-backend; python -m pytest tests/test_v2_regression.py -q` → 5 passed, 1 skipped, 0 failed
- Probe spy = 0 chamadas nos casos table-first/orphan/max_age (prova que o caminho webhook-first nao caiu pro Evolution)
- 4 marker families confirmadas no source
</verification>

<success_criteria>
- TEST-V2-G1..G4 passando (4 case-named tests verdes)
- TEST-V2-G5 stub presente e pulado com reason 'blocked-pending-data'
- OBS-V2-G1 travado por teste de source-audit
- Arquivo < ~180 linhas (sem duplicacao de logica)
</success_criteria>

<output>
After completion, create `.planning/phases/14-testes-regressao-doc-observabilidade/14-01-SUMMARY.md`
</output>
