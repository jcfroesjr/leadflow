---
phase: 14-testes-regressao-doc-observabilidade
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - leadflow-backend/docs/grupo_membership_v2.md
autonomous: true
requirements: [DOC-V2-G1, DOC-V2-G2]
must_haves:
  truths:
    - "Existe doc in-repo `leadflow-backend/docs/grupo_membership_v2.md` documentando a inversao webhook-first (tabela primaria + probe fallback)"
    - "Doc cobre: 3 paths de escrita, lead_in_group() decision tree, retry async max_age=600, LEAVE handler, FSM audit GSC:, healthcheck"
    - "Doc lista os 5 casos motivadores (Ana Carla/Rosania/Fernanda/Valquiria/553891500357) com o mecanismo que os protege"
    - "Doc inclui checklist de atualizacao das memorias do usuario (agente_referencia_compilada.md + sessao memory) — DOC-V2-G2 cross-ref"
    - "Suite completa passa: 113 testes (108 baseline + 4 novos + 1 skip) em <60s, 0 failures (gate de nao-regressao)"
  artifacts:
    - path: "leadflow-backend/docs/grupo_membership_v2.md"
      provides: "Doc de arquitetura v2.2 webhook-first consolidada + checklist de memorias"
      min_lines: 60
      contains: "lead_in_group"
  key_links:
    - from: "grupo_membership_v2.md"
      to: "5 casos motivadores"
      via: "tabela caso → mecanismo → teste de regressao"
      pattern: "Ana Carla|Rosania|Fernanda|Valquiria"
---

<objective>
Consolidar a documentacao da arquitetura webhook-first em um doc IN-REPO
verificavel e rodar o gate de nao-regressao (suite completa) contra a
arquitetura final v2.2.

Purpose: DOC-V2-G1/G2 pedem documentacao da inversao webhook-first. As memorias
de sessao do usuario vivem FORA do repo (~/.claude/.../memory/) e sao mantidas
pelo orquestrador; para deixar a entrega VERIFICAVEL, este plano produz um doc
in-repo (leadflow-backend/docs/grupo_membership_v2.md) que serve de fonte unica e
inclui um checklist apontando para os updates de memoria. O gate de suite
completa (113 testes) garante que os 13 testes v2.1 + toda a infra Fases 10-13
seguem verdes ao lado dos 5 novos.

Output: docs/grupo_membership_v2.md + confirmacao da suite completa verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Research tem o command exato da suite completa (linha 522) + baseline 108 +
# target 113, e a secao DOC artifacts (139-154) + Open Question 2 (append-only).
@.planning/phases/14-testes-regressao-doc-observabilidade/14-RESEARCH.md
@leadflow-backend/CLAUDE.md
</context>

<interfaces>
<!-- Comandos e numeros de baseline confirmados (research Validation Architecture). -->

Suite completa (research linha 522):
  cd leadflow-backend; python -m pytest tests/ -q \
    --ignore=tests/test_calendar_buffer.py \
    --ignore=tests/test_verificar_lead_no_grupo_phase3.py
  Baseline pre-Fase14: 108 passed. Os 2 ignores sao testes quebrados por ENV
  (tzdata / httpx-idna), nao por logica — ignore intencional e documentado.
  Target pos-Fase14: 113 passed (108 + 4 novos de 14-01) + 1 skipped (G5).

Mecanismos por caso (research Existing Coverage Map, para a tabela do doc):
  - Ana Carla → Path 3 createGroup direct write → source='membership'
  - Rosania  → retry async, TABLE_HIT em T+138s, max_age=600
  - Fernanda → instance_key mismatch → source='orphan' → cria novo grupo
  - Valquiria→ job descartado por max_age (47h late)
  - 553891... → blocked-pending-data (stub skip)

Memorias do usuario (FORA do repo — NAO escrever aqui, so referenciar no checklist):
  ~/.claude/projects/c--Projetos-Leadflow/memory/agente_referencia_compilada.md (DOC-V2-G2: append)
  ~/.claude/projects/c--Projetos-Leadflow/memory/sessao_2026-06-XX_grupo_membership_v2.md (DOC-V2-G1)
  Decisao (Open Question 2): APPEND nas memorias, nao reescrever.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar doc in-repo grupo_membership_v2.md (DOC-V2-G1/G2)</name>
  <files>leadflow-backend/docs/grupo_membership_v2.md</files>
  <read_first>
    - .planning/phases/14-testes-regressao-doc-observabilidade/14-RESEARCH.md (Summary + Existing Coverage Map + Healthcheck gap + Structured Logs audit — fonte do conteudo)
    - .planning/REQUIREMENTS.md (Cat 1-6 v2.2 — IDs e descricoes para mapear no doc)
    - .planning/STATE.md (Decisions Log v2.2 — rationale das decisoes arquiteturais)
  </read_first>
  <action>
    Criar leadflow-backend/docs/grupo_membership_v2.md (criar o diretorio docs/ —
    nao existe ainda). Documento de arquitetura consolidada v2.2, secoes:

    1. **Inversao webhook-first** — antes (probe Evolution primario, v2.1) vs depois
       (tabela `grupo_membership` primaria, probe = fallback). Por que mudou
       (4+ casos persistentes 08/06, Evolution propaga 60-180s).
    2. **3 paths de escrita** — webhook GROUP_PARTICIPANTS_UPDATE add (Path 1),
       MESSAGES_UPSERT @lid (Path 2), createGroup direct write (Path 3). Coluna
       instance_key + UPSERT idempotente por messageTimestamp.
    3. **lead_in_group() decision tree** — consulta tabela PRIMEIRO; probe so se row
       ausente OU saiu_em!=null; sources: membership/cache/probe/orphan/stale/left_group.
    4. **Retry async** — schedule_probe_retry 30s/2min/5min, max_age_seconds=600
       absoluto (cobre Rosania T+138s e descarta zumbi Valquiria 47h).
    5. **LEAVE handler + FSM audit** — webhook remove → saiu_em + LEAD_SAIU_GRUPO;
       FSM monotonico com caller obrigatorio; marker GSC:{ag}:{from}:{to}:{reason}:{caller}
       (prefixo GSC: evita colisao com .like("GRUPO_STATE:%")).
    6. **Observabilidade** — 4 log markers ([MEMB-WRITE]/[MEMB-LOOKUP]/[PROBE-RETRY]/GSC:)
       + healthcheck GET /admin/grupo-membership/health (cache_hit_rate + total_rows_24h);
       documentar a discrepancia de path (criterio cita /health/grupo-membership).
    7. **Tabela: 5 casos motivadores → mecanismo → teste de regressao** (usar o
       Existing Coverage Map do research; nomear test_v2_g1..g5).
    8. **Checklist de memorias do usuario (DOC-V2-G2)** — itens marcaveis apontando
       os updates que vivem fora do repo:
         [ ] agente_referencia_compilada.md: append secao tabela grupo_membership +
             lead_in_group() + retry async + FSM audit (APPEND, nao reescrever)
         [ ] nova memoria sessao_2026-06-XX_grupo_membership_v2.md documentando a inversao
       Nota explicita: estes arquivos ficam em ~/.claude/projects/.../memory/ e sao
       mantidos pelo usuario/orquestrador (fora deste repo).

    Tom: tecnico, conciso, "consultar ANTES de mexer". Min ~60 linhas. NAO duplicar
    o agente_referencia inteiro — este doc e o resumo arquitetural de v2.2.
  </action>
  <verify>
    <automated>test -f leadflow-backend/docs/grupo_membership_v2.md && grep -c "lead_in_group" leadflow-backend/docs/grupo_membership_v2.md</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo existe: `test -f leadflow-backend/docs/grupo_membership_v2.md`
    - `wc -l leadflow-backend/docs/grupo_membership_v2.md` >= 60
    - Cobre os 5 casos: `grep -E "Ana Carla|Rosania|Fernanda|Valquiria|553891500357" leadflow-backend/docs/grupo_membership_v2.md` retorna >=4 matches
    - `grep -c "lead_in_group" ...` >= 1; `grep "GSC:" ...` >= 1; `grep "max_age" ...` >= 1
    - Checklist de memorias presente: `grep -E "agente_referencia_compilada|sessao_2026-06" leadflow-backend/docs/grupo_membership_v2.md` >=1
  </acceptance_criteria>
  <done>
    Doc in-repo consolidado existe, cobre as 8 secoes, mapeia os 5 casos e inclui
    o checklist DOC-V2-G2 das memorias externas.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gate de nao-regressao — suite pytest completa verde</name>
  <files>leadflow-backend/docs/grupo_membership_v2.md</files>
  <read_first>
    - .planning/phases/14-testes-regressao-doc-observabilidade/14-RESEARCH.md (Validation Architecture, linhas 513-547 — command exato, baseline 108, target 113, os 2 ignores justificados)
  </read_first>
  <action>
    Rodar a suite completa como gate final do milestone v2.2 (depende de 14-01 ter
    criado test_v2_regression.py e 14-02 ter editado admin.py/main.py):

      cd leadflow-backend; python -m pytest tests/ -q \
        --ignore=tests/test_calendar_buffer.py \
        --ignore=tests/test_verificar_lead_no_grupo_phase3.py

    Esperado: 113 passed, 1 skipped, 0 failed, em <60s.
    (108 baseline + 4 novos de 14-01 + G5 skipped. Os 2 --ignore sao testes
    quebrados por ENV — tzdata e httpx/idna — nao por logica; ignore documentado
    no research.)

    Se houver QUALQUER failure: NAO marcar a fase completa. Investigar — provavel
    causa = assinatura divergente em test_v2_regression.py (ajustar 14-01) ou
    edicao em admin.py/main.py que quebrou import. Reportar o teste que falhou.

    Apos suite verde, anexar ao final de grupo_membership_v2.md uma secao curta
    "## Non-regression gate" registrando: data, command usado, contagem final
    (113 passed / 1 skipped) e os 2 ignores justificados.
  </action>
  <verify>
    <automated>cd leadflow-backend; python -m pytest tests/ -q --ignore=tests/test_calendar_buffer.py --ignore=tests/test_verificar_lead_no_grupo_phase3.py</automated>
  </verify>
  <acceptance_criteria>
    - Suite completa: 0 failed (output termina sem "failed"); skipped esperado = G5 (>=1 skipped)
    - Total passed >= 112 (baseline 108 + 4 novos; margem caso contagem varie levemente)
    - Tempo de execucao < 60s
    - Secao "## Non-regression gate" anexada ao doc com a contagem final
  </acceptance_criteria>
  <done>
    Suite completa verde contra arquitetura final v2.2; gate registrado no doc.
    Os 13 testes v2.1 + infra Fases 10-13 nao regrediram.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum novo) | Plan so cria um doc markdown e roda a suite de testes. Sem codigo de runtime, sem rede, sem auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Information Disclosure | doc in-repo cita telefones reais dos casos motivadores | accept | Mesmos numeros ja em STATE.md/REQUIREMENTS.md commitados; rastreabilidade dos casos; repo privado |
| T-14-06 | Tampering | gate de regressao mascarado por --ignore amplo demais | mitigate | Apenas 2 ignores especificos e nomeados (tzdata/httpx-idna), justificados no research como falhas de ENV nao-logica; nunca `--ignore` de diretorio inteiro nem `-k` filtrando casos v2.2 |
</threat_model>

<verification>
- docs/grupo_membership_v2.md existe, >=60 linhas, cobre 5 casos + checklist memorias
- Suite completa: 0 failed, G5 skipped, <60s
- Secao non-regression gate registrada no doc
</verification>

<success_criteria>
- DOC-V2-G1/G2 entregues via doc in-repo verificavel + checklist das memorias externas
- Gate de nao-regressao verde (113 passed + 1 skipped) contra arquitetura final v2.2
- Milestone v2.2 fechavel: 5 casos protegidos, infra Fases 10-13 nao regredida
</success_criteria>

<output>
After completion, create `.planning/phases/14-testes-regressao-doc-observabilidade/14-03-SUMMARY.md`
</output>
