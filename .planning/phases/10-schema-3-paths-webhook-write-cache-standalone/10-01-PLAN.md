---
phase: 10-schema-3-paths-webhook-write-cache-standalone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - leadflow-backend/requirements.txt
autonomous: true
requirements: [MEMB-02, MEMB-03, PROBE-CACHE-01]
must_haves:
  truths:
    - "cachetools>=5.5.0,<6.0.0 instala sem conflito no Python 3.13 do Easypanel"
    - "Path 2 handler MESSAGES_UPSERT @lid capture localizado em agente.py:3822-3867 (bloco [LID-CAPTURE-MSG])"
    - "Payload real de GROUP_PARTICIPANTS_UPDATE coletado de prod confirma shape de data.messageTimestamp (segundos epoch vs ms vs string ISO)"
  artifacts:
    - path: "leadflow-backend/requirements.txt"
      provides: "cachetools dependency declared"
      contains: "cachetools>=5.5.0,<6.0.0"
    - path: ".planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-01-WAVE0-NOTES.md"
      provides: "Findings da Wave 0 — payload shape + cachetools version + handler localization"
      min_lines: 30
  key_links:
    - from: "tests Plan 02/03/04/05"
      to: "requirements.txt"
      via: "pip install -r requirements.txt na execucao local"
      pattern: "cachetools"
    - from: "Plan 04 (Path 2 patch agente.py)"
      to: "Wave 0 notes localization"
      via: "linha exata 3822-3867 documentada"
      pattern: "LID-CAPTURE-MSG"
---

<objective>
Wave 0 (pré-deps): resolver ASSUMPTIONs do research antes de mexer em código de produção.

Purpose: A2 (`messageTimestamp` shape) e A4 (Path 2 localização) sao os 2 ASSUMED MEDIO do research/10-RESEARCH.md. Resolvê-los em 15min agora elimina retrabalho em Plans 03 e 04. Tambem confirma `cachetools` 5.5.0 disponível no PyPI (A1) antes de adicionar ao requirements.txt.

Output: requirements.txt com `cachetools>=5.5.0,<6.0.0` + notes file `10-01-WAVE0-NOTES.md` documentando: (a) versão exata cachetools instalada, (b) payload real GROUP_PARTICIPANTS_UPDATE com `messageTimestamp` extraído de log Easypanel, (c) confirmação que `[LID-CAPTURE-MSG]` em agente.py:3822-3867 é o callsite Path 2 alvo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-RESEARCH.md

<interfaces>
<!-- Wave 0 nao depende de interfaces de codigo. Depende de fatos do ambiente. -->
<!-- Cachetools API ja documentada em RESEARCH.md §Pattern 3. -->
<!-- Payload Evolution Baileys ja documentado em RESEARCH.md §Code Examples Path 1. -->
</interfaces>

reference_easypanel_logs (memoria): container `3e74f7da5a68` no Hostinger VPS. Comando: `docker logs 3e74f7da5a68 --since 24h | grep -i "GROUP_PARTICIPANTS_UPDATE\|participants.update" | head -50`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirmar cachetools 5.5.0 disponível no PyPI e adicionar a requirements.txt</name>
  <files>leadflow-backend/requirements.txt</files>
  <read_first>
    - leadflow-backend/requirements.txt (todas as 28 linhas — confirma alfabeticamente onde inserir)
    - .planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-RESEARCH.md (linhas 90-115 — Standard Stack + installation block)
  </read_first>
  <action>
    1. Verificar disponibilidade da versao no PyPI:
       ```bash
       pip index versions cachetools 2>&1 | head -5
       ```
       Se nenhuma versao 5.5.x aparecer, fallback aceitavel: `cachetools>=5.3.0,<6.0.0` (RESEARCH.md §Environment Availability autoriza explicitamente).
    2. Adicionar UMA linha em `leadflow-backend/requirements.txt` na posicao alfabetica correta — INSERIR ENTRE `apscheduler==3.10.4` (linha 25) e `pytz>=2024.1` (linha 26):
       ```
       cachetools>=5.5.0,<6.0.0
       ```
       (ou versao fallback se A1 falhou — documentar em WAVE0-NOTES qual escolha)
    3. NAO instalar localmente nesta task — deploy Easypanel faz install no rebuild. Esta task so adiciona a linha.
    4. Documentar em `.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-01-WAVE0-NOTES.md`:
       ```markdown
       ### A1: cachetools version
       - Comando: `pip index versions cachetools`
       - Output: <colar 5 primeiras linhas>
       - Versao escolhida: cachetools>=5.5.0,<6.0.0  (ou fallback se nao disponivel)
       - Decisao: <explicar se foi a primeira escolha ou fallback>
       ```
  </action>
  <verify>
    <automated>grep -n "^cachetools" leadflow-backend/requirements.txt | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^cachetools" leadflow-backend/requirements.txt` retorna `1`
    - Linha esta EXATAMENTE entre `apscheduler==3.10.4` e `pytz>=2024.1` (verificar com `grep -n "apscheduler\|cachetools\|pytz" leadflow-backend/requirements.txt` — deve aparecer em sequencia 25/26/27 ou similar consecutiva)
    - WAVE0-NOTES.md tem secao `### A1: cachetools version` com output de `pip index versions cachetools`
    - Versao especificada bate com pattern regex `^cachetools>=(5\.[3-9]|5\.[1-9][0-9])\.[0-9]+,<6\.0\.0$`
  </acceptance_criteria>
  <done>Linha cachetools adicionada em requirements.txt na posicao alfabetica correta; WAVE0-NOTES documenta versao escolhida + saida de pip index versions.</done>
</task>

<task type="auto">
  <name>Task 2: Localizar Path 2 (MESSAGES_UPSERT @lid capture) em agente.py e confirmar shape</name>
  <files>.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-01-WAVE0-NOTES.md</files>
  <read_first>
    - leadflow-backend/app/routers/agente.py (offset=3800, limit=120 — bloco [LID-CAPTURE-MSG] confirmado em 3822-3867 pelo planner)
    - .planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-RESEARCH.md (linha 671-693 — Path 2 code example pattern)
  </read_first>
  <action>
    1. Confirmar callsite Path 2 via grep (NAO mexer no codigo):
       ```bash
       grep -n "LID-CAPTURE-MSG\|_salvar_lead_lid\|MESSAGES_UPSERT" leadflow-backend/app/routers/agente.py
       ```
    2. Ler explicitamente as linhas 3820-3870 e extrair os nomes EXATOS das variaveis no scope:
       - `_emp_g_id` (empresa_id local)
       - `_remoto_raw` (grupo_jid local)
       - `_participant` (vem de `key.get("participant", "")`)
       - `_tel_lead` (telefone do unico lead aguardando)
       - `payload` (variavel global do handler)
       - `key` (objeto data.key do payload)
    3. Documentar em `.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-01-WAVE0-NOTES.md` (apendar):
       ```markdown
       ### A4: Path 2 callsite — CONFIRMADO

       Localizacao: `leadflow-backend/app/routers/agente.py:3822-3867`

       Bloco: `[LID-CAPTURE-MSG]` dentro do try/except em `if _emp_g_id:` (linha 3821).

       Variaveis no scope (verificadas via leitura direta linhas 3820-3870):
       - `_emp_g_id` — empresa_id resolvida via lookup por instance
       - `_remoto_raw` — grupo_jid (formato `120363xxx@g.us`)
       - `_participant` — vem de `(key or {}).get("participant", "")` — pode ser `@lid` ou telefone limpo
       - `_tel_lead` — telefone do lead aguardando (set apos heuristica unique-aguardando)
       - `_tel_match` — telefone matched por sufixo (path telefone limpo)
       - `payload` — payload completo do webhook (escopo do handler)
       - `key` — `(data or {}).get("key", {})`

       Plan 04 patch (Task 2 — Path 2):
       - Inserir UPSERT em grupo_membership APOS `_salvar_lead_lid(...)` na linha 3846 (path @lid unique-aguardando)
       - Inserir UPSERT TAMBEM no path telefone limpo apos linha 3865 (path `_tel_match` matched por sufixo)
       - instance_key = `payload.get("instance", "")`
       - lid = `_participant.strip()` (path @lid) ou `None` (path telefone limpo)
       - telefone = `_tel_lead` (path @lid) ou `_tel_match` (path telefone limpo)
       - entrou_em = derivar de `(data or {}).get("messageTimestamp")` com fallback `datetime.utcnow()`
       - last_event_id = `(key or {}).get("id", "")` ou `(data or {}).get("key", {}).get("id", "")`
       ```
    4. Resolver A2 (`messageTimestamp` shape): consultar logs Easypanel ou arquivar plano de fallback se logs indisponiveis:
       ```bash
       # SE acesso SSH disponivel:
       # docker logs 3e74f7da5a68 --since 24h | grep -i "WEBHOOK-GRUPO\|participants.update" | head -20
       ```
       Documentar em WAVE0-NOTES:
       ```markdown
       ### A2: messageTimestamp shape
       - Tentativa: <colar comando ou dizer "logs indisponiveis no momento">
       - Shape observado: <epoch segundos | epoch ms | ISO string | ausente>
       - Decisao: usar parser defensivo (try epoch int -> try ISO -> fallback datetime.utcnow())
       ```
       Se logs indisponiveis, documentar fallback: parser cobre os 3 shapes (epoch int, epoch ms se >10^12, ISO string). Plan 03 Task 1 implementa parser.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-01-WAVE0-NOTES.md && grep -q "### A4: Path 2 callsite" .planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-01-WAVE0-NOTES.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-01-WAVE0-NOTES.md` existe (test -f retorna 0)
    - WAVE0-NOTES.md contem secao `### A4: Path 2 callsite` (grep -q retorna 0)
    - Secao A4 documenta: localizacao com linha (agente.py:3822-3867), lista de 7 variaveis no scope, plano de patch (4 fields minimos: instance_key/lid/telefone/entrou_em)
    - WAVE0-NOTES.md contem secao `### A2: messageTimestamp shape` com decisao do parser
    - `grep -c "LID-CAPTURE-MSG" leadflow-backend/app/routers/agente.py` retorna >= 1 (sanity check de que callsite preexistente continua existindo — separado do deliverable principal)
  </acceptance_criteria>
  <done>WAVE0-NOTES documenta path 2 callsite EXATO (linha + variaveis no scope) e parser strategy para messageTimestamp; Plan 03/04 podem citar diretamente sem re-investigar.</done>
</task>

</tasks>

<verification>
- Wave 0 entregavel mínimo: arquivo `10-01-WAVE0-NOTES.md` existe com 3 secoes (A1, A2, A4) preenchidas
- `cachetools` aparece uma vez em requirements.txt entre apscheduler e pytz
- Nenhum codigo de producao foi tocado (so requirements.txt + 1 .md de notes)
</verification>

<success_criteria>
- requirements.txt tem nova linha cachetools — `git diff leadflow-backend/requirements.txt` mostra 1 linha added
- WAVE0-NOTES.md tem 3 secoes documentadas (A1/A2/A4)
- Wave 1 (Plan 02) pode começar sem clarificar nada com humano
</success_criteria>

<output>
After completion, create `.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-01-SUMMARY.md`
</output>
</content>
</invoke>