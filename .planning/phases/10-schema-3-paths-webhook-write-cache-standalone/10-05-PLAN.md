---
phase: 10-schema-3-paths-webhook-write-cache-standalone
plan: 05
type: execute
wave: 4
depends_on: [10-02, 10-03, 10-04]
files_modified:
  - leadflow-backend/tests/test_grupo_membership_upsert.py
  - leadflow-backend/tests/test_probe_cache.py
  - leadflow-backend/app/routers/admin.py
  - leadflow-backend/app/main.py
autonomous: false
requirements: [MEMB-01, MEMB-02, MEMB-03, MEMB-04, MEMB-06, PROBE-CACHE-01]
must_haves:
  truths:
    - "Suite pytest passa green: idempotencia UPSERT (insert + update + noop), write-through invalidation, input invalido, parser timestamp 4 shapes, multi-tenant cache isolation, TTL/clear_for_group, set(None) rejected, lid lowercase normalization"
    - "Endpoint skinny `GET /admin/grupo-membership/health` retorna JSON com {total_rows, last_write_at, cache_stats, paths_24h} — operador detecta cache mascarando falha de persistencia DURANTE Fases 11/12 development"
    - "BUILD_VERSION em app/main.py atualizado pra `2026-06-09-grupo-membership-v2-fundacao` (rastreabilidade do redeploy Easypanel)"
    - "Smoke manual no Supabase Studio: 1 INSERT via service_role + 1 INSERT via anon key — primeiro succeeds, segundo retorna 0 rows ou erro RLS (verifica policy ativa em prod)"
    - "Backend `pytest tests/` passa green com 4 testes existentes v2.1 + 2 novos testes Fase 10"
  artifacts:
    - path: "leadflow-backend/tests/test_grupo_membership_upsert.py"
      provides: "6 tests para helper upsert_grupo_membership + parse_evolution_timestamp"
      min_lines: 120
      contains: "def test_upsert_basico_insert\\|def test_upsert_segundo_call_eh_update\\|def test_invalida_cache_apos_upsert\\|def test_telefone_e_lid_nulos_retorna_invalid\\|def test_upsert_out_of_order_noop\\|def test_parse_evolution_timestamp_4_shapes"
    - path: "leadflow-backend/tests/test_probe_cache.py"
      provides: "6 tests para TTLCache singleton (set/get/invalidate/clear_for_group/multi-tenant/None-rejection/lid-lowercase)"
      min_lines: 100
      contains: "def test_set_get_basic\\|def test_invalidate_removes_and_counts\\|def test_clear_for_group\\|def test_multi_tenant_isolation\\|def test_set_none_silently_rejected\\|def test_lid_lowercase_normalization"
    - path: "leadflow-backend/app/routers/admin.py"
      provides: "Endpoint skinny GET /admin/grupo-membership/health"
      contains: "/grupo-membership/health"
    - path: "leadflow-backend/app/main.py"
      provides: "BUILD_VERSION bumped pra rastreabilidade do redeploy"
      contains: "2026-06-09-grupo-membership-v2-fundacao"
  key_links:
    - from: "leadflow-backend/tests/test_grupo_membership_upsert.py"
      to: "leadflow-backend/app/services/grupo_membership.py"
      via: "import upsert_grupo_membership + parse_evolution_timestamp"
      pattern: "from app.services.grupo_membership import"
    - from: "leadflow-backend/tests/test_probe_cache.py"
      to: "leadflow-backend/app/services/probe_cache.py"
      via: "import probe_cache (todos os 6 publicos: get/set/invalidate/clear_for_group/stats/clear_all)"
      pattern: "from app.services import probe_cache"
    - from: "leadflow-backend/app/routers/admin.py:/grupo-membership/health"
      to: "leadflow-backend/app/services/probe_cache.py:stats()"
      via: "chama probe_cache.stats() pra cache_stats no response"
      pattern: "probe_cache.stats()"
---

<objective>
Wave 4 (verification + observability skinny + ship): cobre testes unitarios pra helper + cache, endpoint skinny pra detectar cache mascarando falha de persistencia, bump de BUILD_VERSION pra rastrear deploy, smoke manual final no Supabase Studio.

Purpose: Sem testes, qualquer regressao em Plans 03/04 fica invisivel ate prod. Sem endpoint healthcheck skinny, operador nao tem visibilidade durante Fases 11/12 (proximos 2-3 deploys). BUILD_VERSION bumped permite confirmar redeploy Easypanel real (feedback_deploy_easypanel_rebuild reminded).

Output: 2 novos arquivos test em `tests/`, novo endpoint em `admin.py`, BUILD_VERSION bumped em `main.py`, smoke manual final no Supabase + push pra GitHub + bump submodule no parent repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-RESEARCH.md
@.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-VALIDATION.md
@.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-02-SUMMARY.md
@.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-03-SUMMARY.md
@.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-04-SUMMARY.md

<interfaces>
<!-- Helpers do Plan 02 (recapitulado): -->
From `app/services/grupo_membership.py`:
```python
def upsert_grupo_membership(sb, *, empresa_id, grupo_jid, telefone, lid, instance_key,
                             entrou_em, fonte, last_event_id=None) -> dict:
    # Returns {'ok': bool, 'id': int|None, 'action': 'insert'|'update'|'noop'|'invalid'|'rpc_empty'|'exception',
    #          'erro': str}

def parse_evolution_timestamp(raw) -> datetime: ...
def get_instance_key(sb, empresa_id: str) -> str: ...
```

From `app/services/probe_cache.py`:
```python
def get(empresa_id, grupo_jid, telefone=None, lid=None) -> Optional[bool]: ...
def set(empresa_id, grupo_jid, telefone, lid, value: bool) -> None: ...    # rejeita silenciosamente value=None
def invalidate(*, empresa_id, grupo_jid, telefone=None, lid=None) -> int: ...
def clear_for_group(empresa_id, grupo_jid) -> int: ...
def stats() -> dict: ...   # {hits, misses, writes, invalidations, size, maxsize, ttl_seconds}
def clear_all() -> None: ...   # fixture-only
```

<!-- admin.py existing router (Plan 02 mapeou linha 18, 271, 286, 737, 784, 997, 1039): -->
From `app/routers/admin.py`:
- Linha 18: `router = APIRouter(prefix="/admin", tags=["admin"])`
- Padrao endpoints: `@router.get("/...")` ou `@router.post("/...")`
- Linha 737: `/grupo/probe` (existente)
- Linha 784: `/grupo/status` (existente)
- Linha 997: `/grupo/forcar-revalidacao` (existente)
- Adicionar novo endpoint em LINHA APROPRIADA (apos /grupo/status na linha ~785; ou no final do arquivo — preferir final pra evitar conflito de patch)

<!-- main.py BUILD_VERSION (linha 239 atual): -->
Linha 239 em app/main.py:
```python
BUILD_VERSION = "2026-06-08-aquec-recovery-janela-6h"
```
Esta linha sera substituida verbatim por:
```python
BUILD_VERSION = "2026-06-09-grupo-membership-v2-fundacao"
```
</interfaces>

reference_easypanel_logs (memoria): bumpar BUILD_VERSION em main.py permite verificar via `/version` endpoint que redeploy Easypanel pegou commit novo (nao so restartou container — feedback_deploy_easypanel_rebuild reminded).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar tests/test_grupo_membership_upsert.py (6 testes — idempotencia + out-of-order + write-through + invalid + parser timestamp)</name>
  <files>leadflow-backend/tests/test_grupo_membership_upsert.py</files>
  <behavior>
    - Test 1 (insert basico): mock supabase RPC retorna `was_insert=True, was_update=False, id=42` → helper retorna `action='insert', id=42, ok=True`
    - Test 2 (update segunda chamada): mesma chave, RPC retorna `was_insert=False, was_update=True` → helper retorna `action='update'`
    - Test 3 (write-through invalidation): cache previamente populado (set) → apos upsert, get retorna None (invalidado)
    - Test 4 (input invalido — telefone e lid ambos nulos): helper retorna `action='invalid', ok=False`, RPC nunca foi chamada
    - Test 5 (out-of-order/noop): RPC retorna `was_insert=False, was_update=False` (guard `p_entrou_em < atualizado_em` rejeitou) → helper retorna `action='noop'`
    - Test 6 (parse_evolution_timestamp 4 shapes): epoch int (segundos), epoch int (ms >10^12), ISO string, None → todos retornam datetime
  </behavior>
  <read_first>
    - leadflow-backend/app/services/grupo_membership.py (signature exata do upsert_grupo_membership + parse_evolution_timestamp)
    - leadflow-backend/app/services/probe_cache.py (signature exata pra teste de write-through)
    - leadflow-backend/tests/test_qualificacao_lock_phase7.py (template pytest+mock estabelecido na v2.1 — copiar style)
    - leadflow-backend/tests/test_verificar_lead_no_grupo_phase3.py (mais um template — confirma pattern AsyncMock e pytest fixtures)
  </read_first>
  <action>
    Criar `leadflow-backend/tests/test_grupo_membership_upsert.py` com este conteudo (6 testes):

    ```python
    """
    Tests para app/services/grupo_membership.py (Fase 10).

    Cobertura:
    - test_upsert_basico_insert         — RPC retorna was_insert=True
    - test_upsert_segundo_call_eh_update — RPC retorna was_update=True
    - test_invalida_cache_apos_upsert    — write-through cache invalidation
    - test_telefone_e_lid_nulos_retorna_invalid — guard de input invalido
    - test_upsert_out_of_order_noop     — RPC retorna was_insert=False, was_update=False
    - test_parse_evolution_timestamp_4_shapes — epoch s, epoch ms, ISO, None
    """
    import pytest
    from datetime import datetime, timedelta
    from unittest.mock import MagicMock


    @pytest.fixture
    def fresh_cache():
        """Limpa cache global entre testes (Anti-pattern 2 prevention)."""
        from app.services import probe_cache
        probe_cache.clear_all()
        yield probe_cache
        probe_cache.clear_all()


    @pytest.fixture
    def mock_sb_rpc():
        """Mock supabase client retornando RPC success (was_insert=True por default)."""
        sb = MagicMock()
        sb.rpc.return_value.execute.return_value.data = [
            {'id': 42, 'was_insert': True, 'was_update': False}
        ]
        return sb


    def test_upsert_basico_insert(mock_sb_rpc, fresh_cache):
        from app.services.grupo_membership import upsert_grupo_membership
        result = upsert_grupo_membership(
            mock_sb_rpc,
            empresa_id='emp-uuid-1234',
            grupo_jid='120363@g.us',
            telefone='5511999999999', lid=None,
            instance_key='rejane-leal-mentora',
            entrou_em=datetime.utcnow(),
            fonte='webhook_add',
            last_event_id='msg_id_123',
        )
        assert result['ok'] is True
        assert result['action'] == 'insert'
        assert result['id'] == 42
        # Verifica RPC chamada com argumentos corretos
        mock_sb_rpc.rpc.assert_called_once()
        args = mock_sb_rpc.rpc.call_args
        assert args[0][0] == 'upsert_grupo_membership'
        assert args[0][1]['p_telefone'] == '5511999999999'
        assert args[0][1]['p_fonte'] == 'webhook_add'
        assert args[0][1]['p_last_event_id'] == 'msg_id_123'


    def test_upsert_segundo_call_eh_update(mock_sb_rpc, fresh_cache):
        """Segunda chamada com mesmo (empresa,grupo,telefone) retorna was_update=True
        pela RPC. Helper deve retornar action='update'."""
        from app.services.grupo_membership import upsert_grupo_membership
        # 1a chamada: insert
        mock_sb_rpc.rpc.return_value.execute.return_value.data = [
            {'id': 42, 'was_insert': True, 'was_update': False}
        ]
        upsert_grupo_membership(mock_sb_rpc, empresa_id='e', grupo_jid='g',
                                 telefone='t', lid=None, instance_key='i',
                                 entrou_em=datetime.utcnow(), fonte='webhook_add')
        # 2a chamada: update (RPC retorna was_update=True)
        mock_sb_rpc.rpc.return_value.execute.return_value.data = [
            {'id': 42, 'was_insert': False, 'was_update': True}
        ]
        result = upsert_grupo_membership(mock_sb_rpc, empresa_id='e', grupo_jid='g',
                                          telefone='t', lid=None, instance_key='i',
                                          entrou_em=datetime.utcnow() + timedelta(seconds=10),
                                          fonte='webhook_add')
        assert result['action'] == 'update'
        assert result['id'] == 42  # mesmo id


    def test_invalida_cache_apos_upsert(mock_sb_rpc, fresh_cache):
        """write-through: cache invalidado apos UPSERT bem-sucedido."""
        from app.services.grupo_membership import upsert_grupo_membership
        from app.services import probe_cache
        # Popula cache com valor stale
        probe_cache.set('emp-uuid', '120363@g.us', '5511999999999', None, True)
        assert probe_cache.get('emp-uuid', '120363@g.us', '5511999999999', None) is True

        upsert_grupo_membership(mock_sb_rpc, empresa_id='emp-uuid',
                                 grupo_jid='120363@g.us', telefone='5511999999999',
                                 lid=None, instance_key='i',
                                 entrou_em=datetime.utcnow(), fonte='webhook_add')

        # Cache foi invalidado
        assert probe_cache.get('emp-uuid', '120363@g.us', '5511999999999', None) is None


    def test_telefone_e_lid_nulos_retorna_invalid(mock_sb_rpc, fresh_cache):
        from app.services.grupo_membership import upsert_grupo_membership
        result = upsert_grupo_membership(mock_sb_rpc, empresa_id='e', grupo_jid='g',
                                          telefone=None, lid=None, instance_key='i',
                                          entrou_em=datetime.utcnow(), fonte='webhook_add')
        assert result['ok'] is False
        assert result['action'] == 'invalid'
        # RPC nao deve ter sido chamada
        mock_sb_rpc.rpc.assert_not_called()


    def test_upsert_out_of_order_noop(mock_sb_rpc, fresh_cache):
        """RPC retorna was_insert=False, was_update=False quando guard rejeita
        timestamp antigo (p_entrou_em < atualizado_em). Helper retorna action='noop'."""
        from app.services.grupo_membership import upsert_grupo_membership
        mock_sb_rpc.rpc.return_value.execute.return_value.data = [
            {'id': 42, 'was_insert': False, 'was_update': False}
        ]
        result = upsert_grupo_membership(
            mock_sb_rpc,
            empresa_id='e', grupo_jid='g', telefone='t', lid=None,
            instance_key='i', entrou_em=datetime.utcnow() - timedelta(hours=2),
            fonte='webhook_add',
        )
        assert result['ok'] is True
        assert result['action'] == 'noop'
        assert result['id'] == 42


    def test_parse_evolution_timestamp_4_shapes():
        """Parser defensivo cobre 4 shapes observaveis em payloads Evolution."""
        from app.services.grupo_membership import parse_evolution_timestamp
        # 1. Epoch segundos (Baileys padrao)
        ts_s = parse_evolution_timestamp(1717948800)
        assert isinstance(ts_s, datetime)
        assert ts_s.year == 2024

        # 2. Epoch ms (raro — >10^12)
        ts_ms = parse_evolution_timestamp(1717948800000)
        assert isinstance(ts_ms, datetime)
        assert ts_ms.year == 2024
        # Mesma data quando convertido corretamente
        assert abs((ts_ms - ts_s).total_seconds()) < 1

        # 3. ISO 8601 string com Z
        ts_iso = parse_evolution_timestamp('2024-06-09T14:00:00Z')
        assert isinstance(ts_iso, datetime)
        assert ts_iso.year == 2024

        # 4. None → fallback datetime.utcnow()
        ts_none = parse_evolution_timestamp(None)
        assert isinstance(ts_none, datetime)
        # Aproximadamente "agora"
        assert abs((datetime.utcnow() - ts_none).total_seconds()) < 5

        # 5. Garbage string → fallback nao quebra
        ts_bad = parse_evolution_timestamp('not-a-timestamp')
        assert isinstance(ts_bad, datetime)
    ```
  </action>
  <verify>
    <automated>cd leadflow-backend && python -m pytest tests/test_grupo_membership_upsert.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `leadflow-backend/tests/test_grupo_membership_upsert.py` existe com >= 120 linhas
    - `python -m pytest tests/test_grupo_membership_upsert.py -v` retorna `6 passed` ou equivalente
    - Grep `def test_` retorna EXATAMENTE 6 matches (6 testes)
    - Grep `@pytest.fixture` retorna 2 matches (fresh_cache + mock_sb_rpc)
    - Cada um dos 6 testes referenciados aparece como `PASSED` no output do pytest
    - Nenhum import error: `python -c "from tests.test_grupo_membership_upsert import test_upsert_basico_insert; print('OK')"` (rodado a partir de leadflow-backend) retorna `OK`
  </acceptance_criteria>
  <done>6 testes pytest cobrindo idempotencia (insert+update+noop), write-through invalidation, input invalido e parser timestamp — todos green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar tests/test_probe_cache.py (6 testes — set/get/invalidate/clear_for_group/multi-tenant/None-rejection/lid-lowercase)</name>
  <files>leadflow-backend/tests/test_probe_cache.py</files>
  <behavior>
    - Test 1 (set/get basico): set retorna valor; get retorna mesmo valor; metricas hits/misses incrementam corretamente
    - Test 2 (invalidate removes): set valor; invalidate retorna 1; get retorna None; metric invalidations += 1
    - Test 3 (clear_for_group): set 3 entries pro mesmo grupo; clear_for_group retorna 3; get pra cada entry retorna None
    - Test 4 (multi-tenant isolation): empresa A e empresa B com mesma (grupo, telefone) → valores isolados, nao se afetam
    - Test 5 (set None silently rejected): set value=None NAO armazena; get retorna None (MISS); metric writes nao incrementa
    - Test 6 (lid lowercase normalization): set com '@LID-ABC' permite get com '@lid-abc' (chave normalizada lowercase em _make_key)
  </behavior>
  <read_first>
    - leadflow-backend/app/services/probe_cache.py (signature exata dos 6 publicos)
    - leadflow-backend/tests/test_verificar_lead_no_grupo_phase3.py (pattern pytest sync — probe_cache nao usa async)
  </read_first>
  <action>
    Criar `leadflow-backend/tests/test_probe_cache.py`:

    ```python
    """
    Tests para app/services/probe_cache.py (Fase 10).

    Cobertura:
    - test_set_get_basic                  — set armazena, get retorna valor
    - test_invalidate_removes_and_counts  — invalidate retorna 1 + metric
    - test_clear_for_group                — bulk invalidate por (empresa, grupo)
    - test_multi_tenant_isolation         — empresa A e empresa B isoladas
    - test_set_none_silently_rejected     — value=None nao cacheia (evita ambiguidade hit/miss)
    - test_lid_lowercase_normalization    — '@LID' e '@lid' viram a mesma chave (case-insensitive)
    """
    import pytest


    @pytest.fixture(autouse=True)
    def fresh_cache():
        """Auto-aplica clear_all entre todos os testes deste arquivo."""
        from app.services import probe_cache
        probe_cache.clear_all()
        yield probe_cache
        probe_cache.clear_all()


    def test_set_get_basic(fresh_cache):
        from app.services import probe_cache
        # MISS antes de set
        assert probe_cache.get('emp-A', 'grupo-1', '5511111', None) is None
        s0 = probe_cache.stats()
        assert s0['size'] == 0
        assert s0['hits'] == 0
        assert s0['misses'] == 1

        # SET + HIT
        probe_cache.set('emp-A', 'grupo-1', '5511111', None, True)
        assert probe_cache.get('emp-A', 'grupo-1', '5511111', None) is True
        s1 = probe_cache.stats()
        assert s1['size'] == 1
        assert s1['writes'] == 1
        assert s1['hits'] == 1


    def test_invalidate_removes_and_counts(fresh_cache):
        from app.services import probe_cache
        probe_cache.set('emp-A', 'grupo-1', '5511111', None, True)
        # Invalidate existente: retorna 1
        n = probe_cache.invalidate(empresa_id='emp-A', grupo_jid='grupo-1',
                                    telefone='5511111', lid=None)
        assert n == 1
        # Cache vazio agora
        assert probe_cache.get('emp-A', 'grupo-1', '5511111', None) is None
        # Invalidate inexistente: retorna 0
        n2 = probe_cache.invalidate(empresa_id='emp-A', grupo_jid='grupo-1',
                                     telefone='5511111', lid=None)
        assert n2 == 0
        # Metric: 1 invalidation contabilizada
        s = probe_cache.stats()
        assert s['invalidations'] == 1


    def test_clear_for_group(fresh_cache):
        from app.services import probe_cache
        # 3 leads no mesmo grupo
        probe_cache.set('emp-A', 'grupo-1', '5511111', None, True)
        probe_cache.set('emp-A', 'grupo-1', '5522222', None, True)
        probe_cache.set('emp-A', 'grupo-1', '5533333', None, False)
        # 1 lead em outro grupo (nao deve ser afetado)
        probe_cache.set('emp-A', 'grupo-2', '5544444', None, True)

        removed = probe_cache.clear_for_group('emp-A', 'grupo-1')
        assert removed == 3

        # Entries do grupo-1 sumiram
        assert probe_cache.get('emp-A', 'grupo-1', '5511111', None) is None
        assert probe_cache.get('emp-A', 'grupo-1', '5522222', None) is None
        assert probe_cache.get('emp-A', 'grupo-1', '5533333', None) is None
        # Entry do grupo-2 permanece
        assert probe_cache.get('emp-A', 'grupo-2', '5544444', None) is True


    def test_multi_tenant_isolation(fresh_cache):
        from app.services import probe_cache
        # Mesma (grupo, telefone) em empresas diferentes — devem ser isolados
        probe_cache.set('emp-A', 'grupo-shared', '5511111', None, True)
        probe_cache.set('emp-B', 'grupo-shared', '5511111', None, False)

        assert probe_cache.get('emp-A', 'grupo-shared', '5511111', None) is True
        assert probe_cache.get('emp-B', 'grupo-shared', '5511111', None) is False

        # clear_for_group da empresa A NAO afeta empresa B
        probe_cache.clear_for_group('emp-A', 'grupo-shared')
        assert probe_cache.get('emp-A', 'grupo-shared', '5511111', None) is None
        assert probe_cache.get('emp-B', 'grupo-shared', '5511111', None) is False


    def test_set_none_silently_rejected(fresh_cache):
        """value=None NAO cacheia (evita ambiguidade hit/miss)."""
        from app.services import probe_cache
        probe_cache.set('emp-A', 'grupo-1', '5511111', None, None)
        # Get retorna None (MISS, nao hit-com-None)
        assert probe_cache.get('emp-A', 'grupo-1', '5511111', None) is None
        s = probe_cache.stats()
        # Nenhum write contabilizado
        assert s['writes'] == 0
        assert s['size'] == 0


    def test_lid_lowercase_normalization(fresh_cache):
        """LID '@LID-123' e '@lid-123' viram a mesma chave (case-insensitive)."""
        from app.services import probe_cache
        probe_cache.set('emp-A', 'grupo-1', None, '@LID-ABC', True)
        # GET com case diferente retorna o mesmo valor
        assert probe_cache.get('emp-A', 'grupo-1', None, '@lid-abc') is True
        assert probe_cache.get('emp-A', 'grupo-1', None, '@Lid-Abc') is True
    ```
  </action>
  <verify>
    <automated>cd leadflow-backend && python -m pytest tests/test_probe_cache.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File `leadflow-backend/tests/test_probe_cache.py` existe com >= 100 linhas
    - `python -m pytest tests/test_probe_cache.py -v` retorna `6 passed`
    - Grep `def test_` retorna EXATAMENTE 6 matches
    - Fixture `fresh_cache` com `autouse=True` (limpa cache entre todos os testes)
    - Multi-tenant isolation EXPLICITAMENTE testado (empresa A vs empresa B com mesma chave)
    - lid lowercase normalization EXPLICITAMENTE testado (test_lid_lowercase_normalization)
  </acceptance_criteria>
  <done>6 testes pytest cobrindo TTLCache singleton end-to-end com fixture autouse pra isolamento — todos green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Adicionar endpoint skinny GET /admin/grupo-membership/health em admin.py</name>
  <files>leadflow-backend/app/routers/admin.py</files>
  <read_first>
    - leadflow-backend/app/routers/admin.py (linhas 1-30 + linhas 780-830 — confirmar APIRouter prefix + estilo dos endpoints existentes)
    - leadflow-backend/app/services/probe_cache.py (signature de stats())
  </read_first>
  <action>
    Adicionar NOVO endpoint no FINAL de `leadflow-backend/app/routers/admin.py` (apos o ultimo endpoint existente — provavelmente `/grupo/revalidar-bulk` linha 1039).

    Anexar este bloco no fim do arquivo (manter estilo dos endpoints existentes — sem auth gates extra; admin.py ja tem prefix `/admin`):

    ```python


    # ──────────────────────────────────────────────────────────────────────────
    # v2.2 Fase 10 — Healthcheck skinny pra grupo_membership table
    # ──────────────────────────────────────────────────────────────────────────
    # Endpoint preview pra detectar cache mascarando falha de persistencia
    # DURANTE Fases 11/12 development (decisao Plan 05 — OBS-V2-G2 full em Fase 14).
    #
    # Operador chama: curl https://leadflow-backend.bqvcbz.easypanel.host/admin/grupo-membership/health
    # Alerta operacional: 0 inserts via fonte=webhook_add por >1h em horario ativo.
    @router.get("/grupo-membership/health")
    async def grupo_membership_health():
        """Healthcheck skinny: confirma persistencia + cache live (Fase 10).

        Returns:
            {
                "total_rows": int,
                "last_write_at": str | None,  # ISO timestamp do row mais recente
                "writes_by_source_24h": {"webhook_add": int, "messages_upsert": int, "create_group": int, ...},
                "cache_stats": {"size": int, "maxsize": int, "ttl_seconds": int, "hits": int, "misses": int, "writes": int, "invalidations": int}
            }

        Sem auth gate em Fase 10 (mesma pratica dos outros endpoints /admin/grupo/*).
        Fase 14 expande pra full healthcheck com alerting hooks.
        """
        from app.services import probe_cache
        from datetime import datetime, timedelta

        sb = get_supabase()
        out: dict = {
            "total_rows": 0,
            "last_write_at": None,
            "writes_by_source_24h": {},
            "cache_stats": probe_cache.stats(),
            "phase": "10-fundacao",
        }

        try:
            # Count total (skinny — sem GROUP BY pra preservar custo)
            cnt = sb.table("grupo_membership").select("id", count="exact").limit(1).execute()
            out["total_rows"] = getattr(cnt, "count", None) or 0
        except Exception as e:
            out["total_rows_erro"] = str(e)

        try:
            # Last write timestamp
            last = sb.table("grupo_membership").select("atualizado_em") \
                .order("atualizado_em", desc=True).limit(1).execute()
            if last.data:
                out["last_write_at"] = last.data[0].get("atualizado_em")
        except Exception as e:
            out["last_write_erro"] = str(e)

        try:
            # Writes por fonte ultimas 24h
            cutoff_24h = (datetime.utcnow() - timedelta(hours=24)).isoformat()
            rows = sb.table("grupo_membership").select("fonte") \
                .gte("criado_em", cutoff_24h).execute()
            counts: dict = {}
            for r in (rows.data or []):
                f = r.get("fonte") or "unknown"
                counts[f] = counts.get(f, 0) + 1
            out["writes_by_source_24h"] = counts
        except Exception as e:
            out["writes_24h_erro"] = str(e)

        return out
    ```

    **NAO MEXER em:**
    - Endpoints existentes `/grupo/probe`, `/grupo/status`, `/grupo/forcar-revalidacao`, `/grupo/revalidar-bulk`
    - APIRouter prefix line 18
  </action>
  <verify>
    <automated>cd leadflow-backend && python -c "from app.routers.admin import router; routes = [r.path for r in router.routes if hasattr(r, 'path')]; assert '/admin/grupo-membership/health' in routes, f'Expected route not found. Got: {routes}'; print('ENDPOINT_REGISTERED')" 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - Grep `/grupo-membership/health` em admin.py retorna 1 match (path)
    - Grep `def grupo_membership_health` retorna 1 match
    - Grep `probe_cache.stats()` em admin.py retorna 1 match
    - Smoke import: registra route `/admin/grupo-membership/health` (verify command)
    - Endpoints existentes ainda presentes: `grep -c "/grupo/probe\|/grupo/status\|/grupo/forcar-revalidacao"` retorna 3
  </acceptance_criteria>
  <done>Endpoint /admin/grupo-membership/health responde JSON com 4 chaves (total_rows, last_write_at, writes_by_source_24h, cache_stats); Fase 14 expandira.</done>
</task>

<task type="auto">
  <name>Task 4: Bump BUILD_VERSION em app/main.py:239 para rastreabilidade do redeploy</name>
  <files>leadflow-backend/app/main.py</files>
  <read_first>
    - leadflow-backend/app/main.py (offset=230, limit=20 — confirmar linha 239 com BUILD_VERSION atual)
  </read_first>
  <action>
    Substituir a linha 239 verbatim de:
    ```python
    BUILD_VERSION = "2026-06-08-aquec-recovery-janela-6h"
    ```
    para:
    ```python
    BUILD_VERSION = "2026-06-09-grupo-membership-v2-fundacao"
    ```

    Use o Edit tool com:
    - `old_string`: `BUILD_VERSION = "2026-06-08-aquec-recovery-janela-6h"`
    - `new_string`: `BUILD_VERSION = "2026-06-09-grupo-membership-v2-fundacao"`

    Este match e UNICO no arquivo (BUILD_VERSION definido uma vez), entao o Edit tool nao requer replace_all.

    Se por algum motivo a string antiga nao for encontrada (ex: outro commit ja bumpou entre planejamento e execucao):
    1. Rodar `grep -n "BUILD_VERSION" leadflow-backend/app/main.py` para localizar a linha atual
    2. Substituir o valor existente por `"2026-06-09-grupo-membership-v2-fundacao"` mantendo o nome da constante
    3. Documentar no SUMMARY que a string antiga era diferente do esperado

    Se BUILD_VERSION nao existir de jeito nenhum no arquivo (improvavel — confirmado em linha 239 atual):
    1. Adicionar no topo do arquivo (apos imports, antes de qualquer codigo):
       ```python
       BUILD_VERSION = "2026-06-09-grupo-membership-v2-fundacao"
       ```
    2. Adicionar endpoint `/version` se nao existir:
       ```python
       @app.get("/version")
       async def version():
           return {"version": BUILD_VERSION, "built_at": "2026-06-09"}
       ```

    NAO mexer em mais nada do main.py (recoveries startup, routers, etc).
  </action>
  <verify>
    <automated>grep -c "2026-06-09-grupo-membership-v2-fundacao" leadflow-backend/app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - Grep `2026-06-09-grupo-membership-v2-fundacao` em main.py retorna 1 match
    - Grep `BUILD_VERSION =` em main.py retorna 1 match (uma definicao apenas)
    - Grep `2026-06-08-aquec-recovery-janela-6h` em main.py retorna 0 matches (string antiga removida)
    - Modulo importa sem erro: `python -c "from app.main import app; print('OK')"`
  </acceptance_criteria>
  <done>BUILD_VERSION bumped na linha 239; permite confirmar via `/version` endpoint apos redeploy Easypanel que commit novo entrou (feedback_easypanel_rebuild_real preserved).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Smoke manual final — Supabase RLS verification + deploy Easypanel + observar /version + /admin/grupo-membership/health</name>
  <what-built>
    Plans 01-04 entregaram:
    - Migration 004 aplicada (tabela grupo_membership + RPC + RLS + indexes)
    - Helper centralizado upsert_grupo_membership + cache probe_cache
    - 3 paths de escrita aditivos (webhook ADD, MESSAGES_UPSERT @lid, createGroup direct)

    Esta task (Plan 05) entregou:
    - 12 testes pytest green (6 grupo_membership + 6 probe_cache)
    - Endpoint /admin/grupo-membership/health
    - BUILD_VERSION bumped

    Antes de fechar a fase, validar manualmente que tudo entrou em prod e RLS esta efetiva.
  </what-built>
  <how-to-verify>
    **A. Pre-deploy (local):**
    1. `cd leadflow-backend && python -m pytest tests/ -v 2>&1 | tail -30`
       - Esperado: 12 testes Fase 10 + testes v2.1 (test_qualificacao_lock_phase7 + test_verificar_lead_no_grupo_phase3 + test_agente_fixes + test_calendar_buffer) todos PASSED. Total >= 16 passed.

    **B. Commit + push backend:**
    2. `cd leadflow-backend && rtk git add app/db/migrations/004_grupo_membership.sql app/services/grupo_membership.py app/services/probe_cache.py app/routers/grupo_webhook.py app/routers/agente.py app/routers/leads.py app/routers/admin.py app/main.py requirements.txt tests/test_grupo_membership_upsert.py tests/test_probe_cache.py`
    3. `rtk git commit -m "feat(grupo): v2.2 Fase 10 - schema grupo_membership + 3 paths webhook write + cache standalone"`
    4. `rtk git push origin main` (ou branch conforme convencao)

    **C. Bump submodule no parent repo:**
    5. `cd c:/Projetos/Leadflow && rtk git add leadflow-backend`
    6. `rtk git commit -m "chore: bump backend pra v2.2 Fase 10 grupo_membership fundacao"`
    7. `rtk git push origin feat/pipeline-visual-refresh` (ou main)

    **D. Aguardar redeploy Easypanel (~3-5 min)**
    8. `curl -s https://leadflow-backend.bqvcbz.easypanel.host/version | jq`
       - Esperado: `{"version": "2026-06-09-grupo-membership-v2-fundacao", "built_at": "2026-06-09"}`
       - SE version ainda for antiga apos 10min: investigar pipeline GitHub→Easypanel (feedback_easypanel_rebuild_real — NAO pedir pra user clicar Rebuild)

    **E. Healthcheck pos-deploy:**
    9. `curl -s https://leadflow-backend.bqvcbz.easypanel.host/admin/grupo-membership/health | jq`
       - Esperado: JSON com 4 chaves (total_rows>=0, last_write_at, writes_by_source_24h, cache_stats com size>=0)
       - SE 500 erro: olhar logs Easypanel via SSH: `docker logs 3e74f7da5a68 --since 5m | grep -i "grupo-membership\|MEMB-WRITE"`

    **F. Smoke RLS manual (Supabase Studio):**
    10. SQL Editor → New query:
        ```sql
        -- Smoke 1: INSERT como service_role (Studio default) deve funcionar
        SELECT * FROM public.upsert_grupo_membership(
          (SELECT id FROM empresas LIMIT 1)::uuid,
          '120363TESTE-PLAN05@g.us',
          '5511999999999', NULL, 'test-instance',
          NOW(), 'admin_force', 'plan05-smoke'
        );
        -- Esperado: 1 row, was_insert=true

        -- Smoke 2: confirmar RLS bloqueia anon
        SET ROLE anon;
        SELECT * FROM public.grupo_membership WHERE grupo_jid = '120363TESTE-PLAN05@g.us';
        -- Esperado: 0 rows (RLS bloqueou — anon nao tem policy)
        RESET ROLE;

        -- Cleanup
        DELETE FROM public.grupo_membership WHERE grupo_jid = '120363TESTE-PLAN05@g.us';
        ```

    **G. Observabilidade Easypanel:**
    11. SSH → `docker logs 3e74f7da5a68 --since 10m | grep -iE "MEMB-WRITE|grupo-membership" | head -20`
        - Aguardar 5-10min apos deploy pra alguma atividade real disparar Paths 1/2/3
        - Esperado eventualmente: linhas `[MEMB-WRITE] OK ... fonte=webhook_add action=insert` quando proximo lead entrar em grupo

    **H. Confirmar todos 11 passos OK antes de digitar "approved"**

    Se A1-A11 todos OK: digitar "approved" + descrever brevemente metric observada (ex: "version OK, healthcheck retornou total_rows=0, RLS bloqueia anon confirmed, aguardando trafego real").
  </how-to-verify>
  <resume-signal>Digite "approved" se A1-A11 todos OK; ou descreva qual passo falhou (com saida do comando) para diagnose.</resume-signal>
</task>

</tasks>

<verification>
- 12 testes pytest novos green (6 + 6); 4+ testes v2.1 ainda passing (zero regressao)
- Endpoint `/admin/grupo-membership/health` responde em prod
- `/version` retorna nova BUILD_VERSION
- RLS verificada manualmente no Supabase Studio (anon bloqueada)
- 24h pos-deploy: logs `[MEMB-WRITE]` confirmando 3 fontes (webhook_add, messages_upsert, create_group)
</verification>

<success_criteria>
- Suite pytest `pytest tests/ -v` retorna >= 16 passed (4 v2.1 + 12 novos)
- Endpoint `/admin/grupo-membership/health` retorna JSON valido
- Endpoint `/version` confirma deploy do commit Fase 10
- Submodule bumped no parent repo + pushed
- Plan 11+ (`lead_in_group()` consumer) tem fundacao completa pra começar
- Roadmap Fase 10 marcado como complete em ROADMAP.md
</success_criteria>

<output>
After completion:
1. Create `.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-05-SUMMARY.md`
2. Create `.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/PHASE-SUMMARY.md` (consolida 5 plans)
3. Update `.planning/STATE.md` — Phase 10 complete, next: Phase 11
4. Update `.planning/ROADMAP.md` — Phase 10 box checked
</output>
</content>
</invoke>