---
phase: 10-schema-3-paths-webhook-write-cache-standalone
plan: 04
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - leadflow-backend/app/routers/agente.py
  - leadflow-backend/app/routers/leads.py
autonomous: true
requirements: [MEMB-03, MEMB-04]
must_haves:
  truths:
    - "Path 2 (MESSAGES_UPSERT @lid capture, agente.py:3822-3867) escreve em grupo_membership quando msg de grupo + EXATAMENTE 1 lead aguardando + @lid não-mapped (heuristica unique-aguardando v2.1) — antes ou junto com `_salvar_lead_lid`"
    - "Path 2 cobre TAMBEM path telefone limpo (linhas 3851-3865) quando participant nao e @lid mas matches lead aguardando por sufixo telefone"
    - "Path 3 (createGroup direct, leads.py:263-271) escreve em grupo_membership DENTRO de `if _lead_confirmado_no_grupo:`, JUNTO com (NAO substitui) marker `GRUPO_LEAD_ENTROU_CRIACAO` — caso Ana Carla resolvido na fundacao da fase"
    - "Marker `GRUPO_LEAD_ENTROU_CRIACAO` (commit 96b72cc PROBE-BYPASS-01) PERMANECE sendo escrito em paralelo — bypass <5min em ativar_fallback_se_necessario ainda depende dele (NAO REGREDIR)"
    - "Path 3 usa `evo_inst` (linha 33 leads.py — instancia que CRIOU o grupo) como instance_key, NAO get_instance_key()"
    - "Path 2 usa `payload.get('instance', '')` como instance_key (instancia que ENVIOU o webhook)"
    - "Import `upsert_grupo_membership, parse_evolution_timestamp` movido para o topo de agente.py (mesmo pattern de Plan 03 grupo_webhook.py) — evita re-import dentro de try blocks"
  artifacts:
    - path: "leadflow-backend/app/routers/agente.py"
      provides: "Path 2 — MESSAGES_UPSERT @lid capture escreve em grupo_membership (linha ~3846 e ~3865)"
      contains: "fonte='messages_upsert'"
    - path: "leadflow-backend/app/routers/leads.py"
      provides: "Path 3 — createGroup direct escreve em grupo_membership (linha ~270)"
      contains: "fonte='create_group'"
  key_links:
    - from: "leadflow-backend/app/routers/agente.py (top-level imports)"
      to: "leadflow-backend/app/services/grupo_membership.py"
      via: "import upsert_grupo_membership + parse_evolution_timestamp uma vez no topo do arquivo"
      pattern: "from app.services.grupo_membership import upsert_grupo_membership, parse_evolution_timestamp"
    - from: "leadflow-backend/app/routers/leads.py:270"
      to: "leadflow-backend/app/services/grupo_membership.py"
      via: "import upsert_grupo_membership dentro do try block apos promote_admin parsing"
      pattern: "upsert_grupo_membership.*create_group"
---

<objective>
Paths 2 e 3 dos 3 paths de escrita: defesa em profundidade quando webhook GROUP_PARTICIPANTS_UPDATE nao dispara ou demora.

Purpose:
- **Path 2 (MESSAGES_UPSERT @lid capture)**: quando WhatsApp esconde participants em `@lid` E webhook GROUP_PARTICIPANTS_UPDATE nao chegou ainda, a primeira mensagem do lead no grupo carrega `key.participant` — heuristica v2.1 unique-aguardando ja correlaciona. Plan 04 adiciona UPSERT no MESMO ponto.
- **Path 3 (createGroup direct)**: caso Ana Carla 08/06 — POST /group/create no Evolution adiciona participant na hora, mas webhook ADD pode nunca disparar (Evolution comportamento documentado). Sem Path 3, Fase 11 `lead_in_group()` consulta tabela vazia e cai pro probe (que tambem mente em T+60s).

Output: 2 patches aditivos em `agente.py` (Path 2 — 2 locais, um para @lid e um para telefone limpo, com import no topo do arquivo) + 1 patch aditivo em `leads.py` (Path 3 — dentro de `if _lead_confirmado_no_grupo:`). Markers v2.1 (`LEAD_LID:`, `GRUPO_LEAD_ENTROU_CRIACAO:`) preservados em paralelo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-RESEARCH.md
@.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-01-WAVE0-NOTES.md
@.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-02-SUMMARY.md

<interfaces>
<!-- Helpers do Plan 02 ja disponiveis: -->
From `app/services/grupo_membership.py`:
```python
def upsert_grupo_membership(sb, *, empresa_id, grupo_jid, telefone, lid, instance_key,
                             entrou_em, fonte, last_event_id=None) -> dict: ...
def get_instance_key(sb, empresa_id) -> str: ...                  # so usado se evo_inst nao disponivel no scope
def parse_evolution_timestamp(raw) -> datetime: ...
```

<!-- Path 2 callsite EXATO (confirmado em WAVE0-NOTES.md A4): -->
From `app/routers/agente.py:3822-3867` — bloco [LID-CAPTURE-MSG]:
- Linha 3821: `if _emp_g_id:` (empresa_id resolvida)
- Linha 3822-3825: comentario "FASE 4 v2.1: Captura @lid via MESSAGES_UPSERT..."
- Linha 3826: `try:`
- Linha 3827: `_participant = (key or {}).get("participant", "") or ""`
- Linha 3829-3830: imports `_salvar_lead_lid` + `processar_entrada_lead_no_grupo`
- Linha 3831: `if "@lid" in _participant.lower():` (path @lid)
- Linha 3834-3843: heuristica unique-aguardando (busca telefones aguardando)
- Linha 3844: `if len(_tels_aguard) == 1:`
- Linha 3845: `_tel_lead = _tels_aguard[0]`
- Linha 3846: `_salvar_lead_lid(_sb_g, _emp_g_id, _tel_lead, _remoto_raw, _participant.strip())` ← UPSERT aqui DEPOIS
- Linha 3847: `print(f"[LID-CAPTURE-MSG] {_tel_lead} via @lid em {_remoto_raw[:24]}... — promovendo FSM")`
- Linha 3848: `await processar_entrada_lead_no_grupo(_emp_g_id, _tel_lead, _remoto_raw)`
- Linha 3851: `else:` (path telefone limpo)
- Linha 3852-3855: parse `_phone_clean`, `_phone_norm`
- Linha 3856-3861: busca aguardando por sufixo telefone (`like("telefone", f"%{_phone_norm[-9:]}")`)
- Linha 3862: `if _aguard_q.data:`
- Linha 3863: `_tel_match = _aguard_q.data[0].get("telefone")`
- Linha 3864: `print(f"[LID-CAPTURE-MSG] {_tel_match} via telefone em {_remoto_raw[:24]}... — promovendo FSM")`
- Linha 3865: `await processar_entrada_lead_no_grupo(_emp_g_id, _tel_match, _remoto_raw)` ← UPSERT aqui DEPOIS
- Linha 3866-3867: `except Exception as _e_lid_msg: print(...)`

Variaveis no scope:
- `_sb_g` — supabase client
- `_emp_g_id` — empresa_id
- `_remoto_raw` — grupo_jid (formato 120363xxx@g.us)
- `_participant` — string com @lid OU telefone limpo
- `_tel_lead` (path @lid) ou `_tel_match` (path telefone)
- `payload`, `key`, `data` — variaveis do handler agente.py externo

<!-- Path 3 callsite EXATO (RESEARCH.md L505-538): -->
From `app/routers/leads.py:245-271`:
- Linha 245: `if _lead_confirmado_no_grupo:` (set por parser de promote_admin acima)
- Linha 246-247: imports `set_grupo_state, STATE_ATIVO`
- Linha 250-254: busca `_ag_pa = sb.table("agendamentos").select("id")...` 
- Linha 255-257: `if _ag_pa.data: set_grupo_state(sb, empresa_id, telefone, _ag_pa.data[0]["id"], STATE_ATIVO); print(...)`
- Linha 258-262: comentario "Marker recente (caso Ana Carla 01/06)..."
- Linha 263-271: `try: sb.table("conversas").insert({...GRUPO_LEAD_ENTROU_CRIACAO...}).execute(); except: pass`
- Linha 272: `except Exception as _e_parse_pa:`

Variaveis no scope:
- `sb` — supabase client (parametro da funcao)
- `empresa_id` — parametro
- `grupo_jid` — set em linha 187 (apos criar_grupo)
- `telefone` — parametro
- `evo_inst` — linha 33: `emp.get("evolution_instancia") or config_apis.get("evolution_instancia", "")` — instance_key correto pra Path 3
- `_lead_confirmado_no_grupo` — bool

<!-- NAO REGREDIR (PROBE-BYPASS-01 commit 96b72cc): -->
<!-- Marker `GRUPO_LEAD_ENTROU_CRIACAO:{grupo_jid}` insert linhas 263-271 — DEVE PERMANECER -->
<!-- ativar_fallback_se_necessario consulta esse marker pra bypass probe <5min -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Path 2 — adicionar import top-level + UPSERT em agente.py:3822-3867 (bloco LID-CAPTURE-MSG)</name>
  <files>leadflow-backend/app/routers/agente.py</files>
  <read_first>
    - leadflow-backend/app/routers/agente.py (offset=1, limit=50 — confirmar bloco de imports no topo)
    - leadflow-backend/app/routers/agente.py (offset=3800, limit=80 — bloco LID-CAPTURE-MSG completo)
    - .planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-01-WAVE0-NOTES.md (secao A4 — variaveis no scope confirmadas)
    - .planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-RESEARCH.md (linhas 666-693 — Path 2 pattern)
    - leadflow-backend/app/routers/grupo_webhook.py (referencia do pattern de import top-level adotado por Plan 03)
  </read_first>
  <action>
    Modificar `leadflow-backend/app/routers/agente.py` em 3 pontos: (a) import no topo do arquivo, (b) UPSERT path @lid, (c) UPSERT path telefone limpo.

    **Insercao 0 (NOVA — mover import pro topo do arquivo):**

    Localizar bloco de imports no topo de agente.py (primeiras ~50 linhas). Encontrar uma boa posicao APOS o ultimo `from app.services...` import. Use o Edit tool com old_string sendo a linha do ultimo `from app.services.*` import existente, e new_string sendo essa MESMA linha seguida da nova importacao:

    ```python
    # v2.2 MEMB-03: Path 2 write em grupo_membership (Fase 10) — import top-level
    # (evita re-import dentro de try blocks; mesmo pattern adotado por grupo_webhook.py em Plan 03)
    from app.services.grupo_membership import upsert_grupo_membership, parse_evolution_timestamp
    ```

    Se houver `from app.services.qualificacao_lock import ...` no topo do arquivo, inserir DEPOIS desse import. Caso contrario, inserir APOS o ultimo import `from app.services.*` que existir.

    **Insercao 1 — Path @lid (apos linha 3846 `_salvar_lead_lid(...)`):**

    Encontrar a linha EXATA:
    ```python
                                _salvar_lead_lid(_sb_g, _emp_g_id, _tel_lead, _remoto_raw, _participant.strip())
    ```
    DEPOIS dela (antes do print em 3847), INSERIR (mesma indentacao). NAO re-importar — usar funcao ja importada no topo:
    ```python
                                # v2.2 MEMB-03 Path 2: UPSERT em grupo_membership (paralelo ao marker LEAD_LID)
                                try:
                                    _entrou_ts_p2 = parse_evolution_timestamp((data or {}).get("messageTimestamp"))
                                    _evt_id_p2 = (key or {}).get("id") or ""
                                    _inst_p2 = (payload.get("instance") or "").strip()
                                    upsert_grupo_membership(
                                        _sb_g,
                                        empresa_id=_emp_g_id,
                                        grupo_jid=_remoto_raw,
                                        telefone=_tel_lead,
                                        lid=_participant.strip(),
                                        instance_key=_inst_p2,
                                        entrou_em=_entrou_ts_p2,
                                        fonte='messages_upsert',
                                        last_event_id=_evt_id_p2 or None,
                                    )
                                except Exception as _e_memb_p2a:
                                    print(f"[MEMB-WRITE-PATH2-LID] exc inesperada: {_e_memb_p2a}")
    ```

    **Insercao 2 — Path telefone limpo (apos linha 3865 `await processar_entrada_lead_no_grupo(_emp_g_id, _tel_match, _remoto_raw)`):**

    Encontrar a linha EXATA:
    ```python
                                    await processar_entrada_lead_no_grupo(_emp_g_id, _tel_match, _remoto_raw)
    ```
    DEPOIS dela (antes do `except Exception as _e_lid_msg:` da linha 3866), INSERIR (mesma indentacao). Novamente, NAO re-importar:
    ```python
                                    # v2.2 MEMB-03 Path 2: UPSERT em grupo_membership (path telefone limpo)
                                    try:
                                        _entrou_ts_p2b = parse_evolution_timestamp((data or {}).get("messageTimestamp"))
                                        _evt_id_p2b = (key or {}).get("id") or ""
                                        _inst_p2b = (payload.get("instance") or "").strip()
                                        upsert_grupo_membership(
                                            _sb_g,
                                            empresa_id=_emp_g_id,
                                            grupo_jid=_remoto_raw,
                                            telefone=_tel_match,
                                            lid=None,
                                            instance_key=_inst_p2b,
                                            entrou_em=_entrou_ts_p2b,
                                            fonte='messages_upsert',
                                            last_event_id=_evt_id_p2b or None,
                                        )
                                    except Exception as _e_memb_p2b:
                                        print(f"[MEMB-WRITE-PATH2-TEL] exc inesperada: {_e_memb_p2b}")
    ```

    **NAO MEXER em:**
    - Linhas 3822-3825 (comentario FASE 4 v2.1) — preservar narrativa historica
    - Linha 3846 chamada de `_salvar_lead_lid` — marker LEAD_LID continua sendo escrito
    - Linhas 3844, 3849-3850 (heuristica unique-aguardando) — backwards compat
    - Linha 3848 e 3865 `processar_entrada_lead_no_grupo` — handler v2.1 nao alterado
    - Qualquer codigo fora do range 3822-3867 (exceto o import no topo)
  </action>
  <verify>
    <automated>grep -c "MEMB-03 Path 2\|fonte='messages_upsert'\|MEMB-WRITE-PATH2-LID\|MEMB-WRITE-PATH2-TEL\|^from app.services.grupo_membership import upsert_grupo_membership, parse_evolution_timestamp" leadflow-backend/app/routers/agente.py</automated>
  </verify>
  <acceptance_criteria>
    - Grep `^from app.services.grupo_membership import upsert_grupo_membership, parse_evolution_timestamp` em agente.py retorna 1 match no TOPO do arquivo (verificar via `grep -n` que o numero da linha eh < 100, confirmando import top-level e nao dentro de funcao)
    - Grep `MEMB-03 Path 2` retorna EXATAMENTE 2 matches (path @lid + path telefone)
    - Grep `fonte='messages_upsert'` retorna EXATAMENTE 2 matches
    - Grep `MEMB-WRITE-PATH2-LID` retorna 1 match (logger do exc handler do path @lid)
    - Grep `MEMB-WRITE-PATH2-TEL` retorna 1 match (logger do exc handler do path telefone)
    - Grep `from app.services.grupo_membership import` em agente.py retorna EXATAMENTE 1 match (so o do topo — nao re-importado dentro de blocos try)
    - Grep `_salvar_lead_lid` AINDA aparece em agente.py (no minimo no import + chamada — `grep -c "_salvar_lead_lid" agente.py` >= 2)
    - Grep `LID-CAPTURE-MSG` ainda aparece (heuristica v2.1 preservada — >= 3 matches)
    - Grep `processar_entrada_lead_no_grupo` ainda aparece (handler v2.1 — >= 2 matches no bloco)
    - Smoke import: `cd leadflow-backend && python -c "from app.routers.agente import router; print('OK')" 2>&1 | tail -1` retorna `OK`
    - `git diff --stat leadflow-backend/app/routers/agente.py` mostra apenas linhas adicionadas (zero removidas) — Path 2 e puramente aditivo
  </acceptance_criteria>
  <done>Path 2 escreve em grupo_membership em ambos os paths (@lid e telefone limpo) DEPOIS de `_salvar_lead_lid` ou `processar_entrada_lead_no_grupo`; import movido pro topo do arquivo (mesmo pattern de grupo_webhook.py em Plan 03); markers LEAD_LID e handler v2.1 preservados.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Path 3 — UPSERT direto em leads.py:263-271 (DENTRO de `if _lead_confirmado_no_grupo:`)</name>
  <files>leadflow-backend/app/routers/leads.py</files>
  <read_first>
    - leadflow-backend/app/routers/leads.py (offset=240, limit=40 — bloco `if _lead_confirmado_no_grupo:` completo)
    - .planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-RESEARCH.md (linhas 698-741 — Path 3 code example)
  </read_first>
  <action>
    Modificar `leadflow-backend/app/routers/leads.py` adicionando UPSERT Path 3 DENTRO do bloco `if _lead_confirmado_no_grupo:` (linha 245), ANTES do insert do marker `GRUPO_LEAD_ENTROU_CRIACAO` (linha 263).

    NOTA: Em leads.py o import permanece dentro do try block por convencao local (callsite isolado em endpoint POST que pode nao ser exercitado em todo handler); Plan 03 grupo_webhook.py e Plan 04 agente.py movem pro topo porque ambos os patches sao no caminho quente do webhook. Esta decisao foi intencional na revisao.

    Local exato — encontrar essa sequencia atual:
    ```python
                        if _lead_confirmado_no_grupo:
                            from app.services.grupo_state import (
                                set_grupo_state, STATE_ATIVO,
                            )
                            # Busca ag atual pra setar state
                            _ag_pa = sb.table("agendamentos").select("id") \
                                .eq("empresa_id", empresa_id) \
                                .eq("telefone", telefone) \
                                .neq("status", "cancelado") \
                                .order("data", desc=False).limit(1).execute()
                            if _ag_pa.data:
                                set_grupo_state(sb, empresa_id, telefone, _ag_pa.data[0]["id"], STATE_ATIVO)
                                print(f"[GRUPO] FSM=ATIVO setado via promote_admin (lead {_tel_digits} confirmado em updateParticipants)")
                            # Marker recente (caso Ana Carla 01/06): aquec roda T+60s mas
    ```

    INSERIR ANTES do comentario "Marker recente (caso Ana Carla 01/06):" (mesma indentacao do `if _ag_pa.data:`):
    ```python
                            # v2.2 MEMB-04 Path 3: persiste membership direto (caso Ana Carla)
                            # Webhook GROUP_PARTICIPANTS_UPDATE nao dispara pra participants do
                            # POST /group/create. Sem este UPSERT, Fase 11 `lead_in_group()` consulta
                            # tabela vazia e cai pro probe (que tambem mente em T+60s).
                            # IMPORTANT: instance_key = `evo_inst` (linha 33 — instancia que CRIOU
                            # o grupo), NAO lookup atual da empresa (Pitfall 3).
                            try:
                                from app.services.grupo_membership import upsert_grupo_membership
                                upsert_grupo_membership(
                                    sb,
                                    empresa_id=empresa_id,
                                    grupo_jid=grupo_jid,
                                    telefone=telefone,
                                    lid=None,
                                    instance_key=evo_inst,
                                    entrou_em=datetime.utcnow(),
                                    fonte='create_group',
                                    last_event_id=None,
                                )
                            except Exception as _e_memb_p3:
                                print(f"[MEMB-WRITE-PATH3] exc inesperada: {_e_memb_p3}")
                            # ── fim MEMB-04 Path 3 ──
    ```

    **NAO MEXER em:**
    - Linhas 263-271 (insert do marker `GRUPO_LEAD_ENTROU_CRIACAO`) — PROBE-BYPASS-01 (commit 96b72cc) ainda confia nele em ativar_fallback_se_necessario; remover apenas em Fase 11+
    - Linhas 246-257 (set_grupo_state STATE_ATIVO) — Bug Vanusa 13/05 fix preservado
    - Linha 33 (`evo_inst = emp.get("evolution_instancia") or config_apis.get("evolution_instancia", "")`) — source de instance_key correto
    - Bloco GRUPO-REUSO-01 linhas 51-69 (commit 7e366bd — valida acesso ao grupo antes de reusar) — NAO REGREDIR
  </action>
  <verify>
    <automated>grep -c "MEMB-04 Path 3\|fonte='create_group'\|MEMB-WRITE-PATH3" leadflow-backend/app/routers/leads.py</automated>
  </verify>
  <acceptance_criteria>
    - Grep `MEMB-04 Path 3` retorna 1 match
    - Grep `fonte='create_group'` retorna 1 match em leads.py
    - Grep `MEMB-WRITE-PATH3` retorna 1 match (logger exc handler)
    - Grep `instance_key=evo_inst` retorna 1 match (Pitfall 3 — instancia criadora, NAO lookup)
    - Grep `GRUPO_LEAD_ENTROU_CRIACAO` AINDA retorna 1 match (marker PROBE-BYPASS-01 preservado)
    - Grep `set_grupo_state.*STATE_ATIVO` AINDA retorna >= 2 matches (Bug Vanusa 13/05 preservado)
    - Grep `findGroupInfos` AINDA retorna >= 1 match (GRUPO-REUSO-01 commit 7e366bd preservado em linhas 60-66)
    - Path 3 esta DENTRO do `if _lead_confirmado_no_grupo:` (verificar via grep de contexto: `grep -B 2 "MEMB-04 Path 3" leads.py | head -5` mostra contexto que confirma)
    - Smoke import: `cd leadflow-backend && python -c "from app.routers.leads import router; print('OK')" 2>&1 | tail -1` retorna `OK`
    - `git diff --stat leadflow-backend/app/routers/leads.py` mostra apenas linhas adicionadas (zero removidas)
  </acceptance_criteria>
  <done>Path 3 escreve em grupo_membership ANTES do marker GRUPO_LEAD_ENTROU_CRIACAO (que permanece); usa evo_inst como instance_key; smoke import OK; bloco GRUPO-REUSO-01 + Bug Vanusa preservados.</done>
</task>

<task type="auto">
  <name>Task 3: Validar ortogonalidade Paths 2+3 — nenhum dos 6 fixes 08/06 + Phases v2.1 foi regredido</name>
  <files>leadflow-backend/app/routers/agente.py, leadflow-backend/app/routers/leads.py</files>
  <read_first>
    - leadflow-backend/app/routers/agente.py (offset=3820, limit=60 — confirmar patch aplicado)
    - leadflow-backend/app/routers/leads.py (offset=240, limit=50 — confirmar patch aplicado)
  </read_first>
  <action>
    Esta task NAO altera codigo — valida via grep que nenhum dos fixes preservaveis foi removido.

    **Checks em agente.py:**
    ```bash
    # FASE 4 v2.1 captura @lid preservada
    grep -c "FASE 4 v2.1: Captura @lid" leadflow-backend/app/routers/agente.py
    # Esperado: 1

    # _salvar_lead_lid import + call preservados
    grep -c "_salvar_lead_lid" leadflow-backend/app/routers/agente.py
    # Esperado: >= 2

    # Heuristica unique-aguardando preservada
    grep -c "len(_tels_aguard) == 1" leadflow-backend/app/routers/agente.py
    # Esperado: 1

    # Marker GRUPO_AGUARDANDO_ENTRADA lookup preservado
    grep -c "GRUPO_AGUARDANDO_ENTRADA:{_remoto_raw}" leadflow-backend/app/routers/agente.py
    # Esperado: >= 2

    # processar_entrada_lead_no_grupo (handler v2.1) preservado
    grep -c "processar_entrada_lead_no_grupo" leadflow-backend/app/routers/agente.py
    # Esperado: >= 3 (import + 2 chamadas)
    ```

    **Checks em leads.py:**
    ```bash
    # GRUPO-REUSO-01 (commit 7e366bd) — valida acesso ao grupo antes de reusar
    grep -c "FIX 08/06 (caso Fernanda" leadflow-backend/app/routers/leads.py
    # Esperado: 1
    grep -c "findGroupInfos" leadflow-backend/app/routers/leads.py
    # Esperado: >= 1

    # FIX 18/05 (caso Luciane) — revalidacao no reuso preservada
    grep -c "FIX 18/05 (caso Luciane" leadflow-backend/app/routers/leads.py
    # Esperado: 1

    # FIX 21/05 v3 — limpa markers stale
    grep -c "FIX 21/05 v3" leadflow-backend/app/routers/leads.py
    # Esperado: 1

    # PROBE-BYPASS-01 marker (commit 96b72cc)
    grep -c "GRUPO_LEAD_ENTROU_CRIACAO" leadflow-backend/app/routers/leads.py
    # Esperado: 1

    # Bug Vanusa 13/05 (parse promote_admin)
    grep -c "Bug Vanusa/lead 5511981280591" leadflow-backend/app/routers/leads.py
    # Esperado: 1

    # FIX 16/05 fallback @lid (parser todos status 200)
    grep -c "FIX 16/05: Evolution moderno retorna jid=@lid" leadflow-backend/app/routers/leads.py
    # Esperado: 1

    # Convite nativo nominal (FIX 19/05 sessao_2026-05-19_fsm_bypass_e_convite_nominal)
    grep -c "nome_responsavel" leadflow-backend/app/routers/leads.py
    # Esperado: >= 1
    ```

    **Sintaxe Python — modulos importam sem erro:**
    ```bash
    cd leadflow-backend && python -c "
    from app.routers.agente import router as router_agente
    from app.routers.leads import router as router_leads
    from app.services.grupo_membership import upsert_grupo_membership
    print('ALL_IMPORTS_OK')
    " 2>&1 | tail -1
    ```

    Documentar resultado de todos os 12 greps + import smoke test no SUMMARY. Se QUALQUER um falhar, REVERTER patches e re-investigar.
  </action>
  <verify>
    <automated>cd leadflow-backend && python -c "from app.routers.agente import router as ra; from app.routers.leads import router as rl; from app.services.grupo_membership import upsert_grupo_membership; print('ALL_IMPORTS_OK')" 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - Todos os 12 greps acima retornam o valor esperado
    - Smoke imports retornam `ALL_IMPORTS_OK`
    - `git diff --stat leadflow-backend/app/routers/agente.py leadflow-backend/app/routers/leads.py` mostra apenas linhas adicionadas (zero removidas) — Paths 2+3 sao puramente aditivos
    - SUMMARY documenta cada grep com resultado (OK ou FAIL)
  </acceptance_criteria>
  <done>Ortogonalidade confirmada: 6 fixes 08/06 + Phases v2.1 (4-7) + Bug Vanusa/Caca/Luciane preservados; smoke imports OK; diff aditivo apenas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evolution MESSAGES_UPSERT → agente.py | Payload nao-confiavel (Pitfall 14 OUT-OF-SCOPE Fase 10) |
| Path 3 callsite → createGroup response confiabilidade | Mitigada via `_lead_confirmado_no_grupo=True` guard (Bug Vanusa fix — parser updateParticipants.status=200) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Tampering | Path 2 — atacante manda MESSAGES_UPSERT falso pra correlacionar @lid errado | accept | Heuristica unique-aguardando (`len(_tels_aguard) == 1`) ja filtra: se 2+ leads aguardando, ambiguidade ignorada. Atacante precisaria conhecer grupo_jid + ter exatamente 1 lead aguardando — low-info-leak. |
| T-10-11 | Tampering | Path 3 — promote_admin parser falsifica `_lead_confirmado_no_grupo=True` | mitigate | Path 3 so dispara DEPOIS de `_lead_confirmado_no_grupo=True` (set pelo parser updateParticipants.status=200 — Bug Vanusa fix); falsificar exigiria comprometer Evolution API. Fora do threat model Fase 10. |
| T-10-12 | Information Disclosure | Cache cross-tenant via Path 2/3 escrita | mitigate | Helper Plan 02 ja garante (`empresa_id` no key — T-10-03); Paths 2/3 passam empresa_id local correto (`_emp_g_id` e `empresa_id` respectivamente). |
| T-10-13 | Tampering | Out-of-order: Path 1 webhook ADD chega DEPOIS de Path 3 createGroup com `entrou_em` mais antigo | mitigate | RPC guard `p_entrou_em >= atualizado_em` — Path 1 webhook com timestamp antigo vira no-op (`action=noop`); Path 3 (createGroup) vence porque entrou primeiro. |
</threat_model>

<verification>
- `git diff --stat` em ambos os arquivos mostra apenas linhas adicionadas
- 12 greps de regressao todos OK
- Smoke imports OK
- Logs Easypanel pos-deploy: aparecer `[MEMB-WRITE] OK ... fonte=messages_upsert` (quando msg de grupo @lid chega) e `[MEMB-WRITE] OK ... fonte=create_group` (quando lead novo cria grupo)
</verification>

<success_criteria>
- 2 patches em agente.py (path @lid + path telefone) inseridos APOS handlers v2.1 existentes (com import unico no topo do arquivo)
- 1 patch em leads.py inserido DENTRO de `if _lead_confirmado_no_grupo:`, ANTES do marker GRUPO_LEAD_ENTROU_CRIACAO
- Markers v2.1 (LEAD_LID, GRUPO_LEAD_ENTROU_CRIACAO) continuam sendo escritos em paralelo
- Plan 05 (testes) pode validar `fonte` correto em cada path
- Pos-Fase 10 deploy: caso Ana Carla NAO repete (Path 3 escreve membership independente de webhook)
</success_criteria>

<output>
After completion, create `.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-04-SUMMARY.md`
</output>
</content>
</invoke>