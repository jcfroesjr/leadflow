---
phase: 10-schema-3-paths-webhook-write-cache-standalone
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - leadflow-backend/app/routers/grupo_webhook.py
autonomous: true
requirements: [MEMB-02]
must_haves:
  truths:
    - "Webhook GROUP_PARTICIPANTS_UPDATE action=add escreve em grupo_membership para cada telefone e cada @lid em participants — antes dos handlers existentes (marker check + processar_entrada_lead_no_grupo)"
    - "instance_key gravado e EXATAMENTE `payload.get('instance')` (instancia que ORIGINOU o evento, NAO lookup atual da empresa) — Pitfall 3 evitado"
    - "entrou_em usa parse_evolution_timestamp(data.messageTimestamp) com fallback datetime.utcnow() (cobre epoch s/ms/ISO/missing)"
    - "last_event_id gravado = `data.key.id` ou `data.id` (idempotencia adicional caso webhook re-entregue)"
    - "Caso webhook re-delivery (Pitfall 9 out-of-order): RPC ja garante via guard `p_entrou_em >= atualizado_em` — UPSERT antigo vira no-op com log `action=noop`"
    - "Handlers v2.1 existentes (linhas 161-225 — marker GRUPO_AGUARDANDO_ENTRADA + heuristica @lid + processar_entrada_lead_no_grupo) NAO sao alterados — UPSERT roda ANTES, processamento continua igual (backwards compat)"
  artifacts:
    - path: "leadflow-backend/app/routers/grupo_webhook.py"
      provides: "Path 1 — webhook ADD escreve em grupo_membership"
      contains: "upsert_grupo_membership"
  key_links:
    - from: "leadflow-backend/app/routers/grupo_webhook.py"
      to: "leadflow-backend/app/services/grupo_membership.py"
      via: "import upsert_grupo_membership + parse_evolution_timestamp"
      pattern: "from app.services.grupo_membership import"
    - from: "Path 1 UPSERT calls"
      to: "fonte='webhook_add' (SQL CHECK constraint)"
      via: "kwarg fonte='webhook_add' em todas as chamadas"
      pattern: "fonte='webhook_add'"
---

<objective>
Path 1 dos 3 paths de escrita: webhook GROUP_PARTICIPANTS_UPDATE quando `action=add` faz UPSERT em `grupo_membership` para cada participant (telefone e/ou @lid) ANTES dos handlers existentes.

Purpose: Webhook e a fonte natural #1 — quando WhatsApp confirma "lead entrou", esse evento e o sinal mais confiavel. Mas v2.1 nao persiste em tabela materializada, so dispara handlers ad-hoc. Path 1 fixa isso aditivamente: handlers existentes continuam rodando (backwards compat), UPSERT roda ANTES pra garantir que Fase 11 `lead_in_group()` encontre dados ao consultar.

Output: `grupo_webhook.py:_processar_grupo_payload` ganha bloco de UPSERT entre linha 130 (apos collect participants) e linha 161 (loop atual de markers). Tudo idempotente, com try/except interno do helper para nunca quebrar o handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-RESEARCH.md
@.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-01-WAVE0-NOTES.md
@.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-02-SUMMARY.md

<interfaces>
<!-- Helpers do Plan 02 ja disponiveis: -->

From `app/services/grupo_membership.py` (Plan 02 Task 4):
```python
def upsert_grupo_membership(
    sb, *,
    empresa_id: str,
    grupo_jid: str,
    telefone: Optional[str],
    lid: Optional[str],
    instance_key: str,
    entrou_em: datetime,
    fonte: Literal['webhook_add', 'messages_upsert', 'create_group', 'admin_force', 'probe_backfill'],
    last_event_id: Optional[str] = None,
) -> dict: ...   # {'ok', 'id', 'action', 'erro'} — NUNCA propaga exception

def parse_evolution_timestamp(raw) -> datetime: ...   # int/float/str/None → datetime UTC-naive
```

From `app/routers/grupo_webhook.py` (CODIGO ATUAL — linhas 81-226):
- `_processar_grupo_payload(payload: dict)` recebe payload completo
- Linha 99: `data = payload.get("data") or {}`
- Linha 100: `action = (data.get("action") or "").lower()`
- Linha 104: `grupo_jid = data.get("id") or ""`
- Linhas 111-128: parse `participants_raw` em `telefones_entraram: list[str]` e `lids_entraram: list[str]`
- Linha 130: `if not telefones_entraram and not lids_entraram: return {ok, ignorado, motivo}`
- Linha 133: `instancia = payload.get("instance") or ""`
- Linha 142: `sb = get_supabase()`
- Linhas 144-149: lookup `empresa_id` por instance (case-insensitive + fallback config_apis)
- Linha 156: `print(f"[WEBHOOK-GRUPO] instancia={instancia} -> empresa_id={empresa_id[:8]}")`
- Linha 161+: loop atual `for tel in telefones_entraram: ...` (NAO ALTERAR)

<!-- NAO REGREDIR: -->
<!-- - Linha 101-102 retorna cedo pra action != 'add' — MANTER nesta fase (LEAVE/REMOVE e Fase 13) -->
<!-- - Heuristica @lid unique-aguardando linhas 192-222 — MANTER (continua escrevendo LEAD_LID marker em paralelo) -->
<!-- - processar_entrada_lead_no_grupo linha 180 — MANTER (sistema legacy continua funcional) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar UPSERT Path 1 em grupo_webhook.py:_processar_grupo_payload (apos lookup empresa_id, antes do loop atual)</name>
  <files>leadflow-backend/app/routers/grupo_webhook.py</files>
  <read_first>
    - leadflow-backend/app/routers/grupo_webhook.py (linhas 1-227 — handler completo)
    - .planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-RESEARCH.md (linhas 604-661 — Path 1 code example exato)
    - leadflow-backend/app/services/grupo_membership.py (helper Plan 02 — confirmar signature exata)
  </read_first>
  <action>
    Modificar `leadflow-backend/app/routers/grupo_webhook.py`:

    **Insercao 1 — adicionar imports no topo do arquivo, junto com os existentes (linha 14):**

    Procurar a linha que importa datetime:
    ```python
    from datetime import datetime
    ```
    DEPOIS dela, adicionar (na linha 15 — antes de `from fastapi`):
    ```python
    # v2.2 MEMB-02: Path 1 write em grupo_membership (Fase 10)
    from app.services.grupo_membership import upsert_grupo_membership, parse_evolution_timestamp
    ```

    **Insercao 2 — patch dentro de `_processar_grupo_payload`:**

    LOCAL EXATO: APOS a linha 156 (`print(f"[WEBHOOK-GRUPO] instancia={instancia} -> empresa_id={empresa_id[:8]}")`) e ANTES da linha 157 (linha em branco) seguida do comentario linha 158 (`# Pra cada telefone que entrou...`).

    INSERIR este bloco (estilo identico ao codigo circundante — sem black/autoformatter):

    ```python
        # ── v2.2 MEMB-02: persiste membership ANTES dos handlers atuais (Path 1) ──
        # Idempotencia via partial unique (empresa_id, grupo_jid, COALESCE(telefone, lid))
        # WHERE saiu_em IS NULL. RPC SQL trata race + out-of-order via guard
        # `p_entrou_em >= atualizado_em`. Helper NUNCA propaga exception — falha
        # silenciosa logada como [MEMB-WRITE] FAIL/EXC, handler continua.
        #
        # IMPORTANT — Pitfall 3: instance_key = `payload.instance` (instancia que
        # ORIGINOU o evento), NAO lookup atual da empresa. Caso Fernanda (grupo
        # orfao de instancia antiga) so e detectavel em Fase 11 quando lead_in_group
        # compara este valor com empresas.evolution_instancia atual.
        _evento_ts = parse_evolution_timestamp(
            data.get("messageTimestamp") or payload.get("date_time")
        )
        _last_event_id = (
            (data.get("key") or {}).get("id")
            or data.get("id")
            or payload.get("id")
            or ""
        ) or None
        _instance_key_evento = (payload.get("instance") or "").strip()

        for _tel_memb in telefones_entraram:
            upsert_grupo_membership(
                sb,
                empresa_id=empresa_id,
                grupo_jid=grupo_jid,
                telefone=_tel_memb,
                lid=None,
                instance_key=_instance_key_evento,
                entrou_em=_evento_ts,
                fonte='webhook_add',
                last_event_id=_last_event_id,
            )
        for _lid_memb in lids_entraram:
            upsert_grupo_membership(
                sb,
                empresa_id=empresa_id,
                grupo_jid=grupo_jid,
                telefone=None,
                lid=_lid_memb,
                instance_key=_instance_key_evento,
                entrou_em=_evento_ts,
                fonte='webhook_add',
                last_event_id=_last_event_id,
            )
        # ── fim MEMB-02 Path 1 ──

    ```

    **NAO MEXER em:**
    - Linhas 99-102 (early return action != "add") — LEAVE/REMOVE e Fase 13
    - Linhas 161-225 (loop existente + heuristica @lid + processar_entrada_lead_no_grupo) — backwards compat absoluto
    - Funcao `_salvar_lead_lid` linhas 33-50 — marker LEAD_LID continua sendo escrito em paralelo
    - Funcao `_normalizar_jid_telefone` linhas 26-30
    - Rota linha 53-78
  </action>
  <verify>
    <automated>grep -c "MEMB-02: persiste membership\|fonte='webhook_add'\|upsert_grupo_membership\|parse_evolution_timestamp" leadflow-backend/app/routers/grupo_webhook.py</automated>
  </verify>
  <acceptance_criteria>
    - Grep `from app.services.grupo_membership import upsert_grupo_membership, parse_evolution_timestamp` retorna 1 match em grupo_webhook.py
    - Grep `MEMB-02: persiste membership` retorna 1 match
    - Grep `fonte='webhook_add'` retorna EXATAMENTE 2 matches (1 pro loop telefones, 1 pro loop @lids)
    - Grep `_instance_key_evento = \(payload.get\("instance"\)` retorna 1 match (Pitfall 3 — instance origem do evento)
    - Grep `_evento_ts = parse_evolution_timestamp` retorna 1 match
    - Grep `_last_event_id` retorna >= 4 matches (declaracao + 2 usos nos UPSERTs)
    - Grep `processar_entrada_lead_no_grupo` AINDA retorna 1 match (handler v2.1 nao removido)
    - Grep `_salvar_lead_lid` AINDA retorna >= 2 matches (heuristica @lid v2.1 nao removida)
    - Grep `if action != "add":` AINDA retorna 1 match (LEAVE handler e Fase 13 — nao alterado)
    - `wc -l leadflow-backend/app/routers/grupo_webhook.py` retorna mais que 227 (linhas adicionadas, nenhuma removida)
    - Python import sanity: `cd leadflow-backend && python -c "from app.routers.grupo_webhook import _processar_grupo_payload; print('OK')" 2>&1 | tail -1` retorna `OK`
  </acceptance_criteria>
  <done>grupo_webhook.py modificado aditivamente; loop existente (linhas 161-225) preservado; UPSERT roda ANTES com instance_key correto + parse_evolution_timestamp defensivo + last_event_id; smoke import sem erro.</done>
</task>

<task type="auto">
  <name>Task 2: Validar ortogonalidade — Path 1 não regride handlers v2.1 nem fixes 08/06</name>
  <files>leadflow-backend/app/routers/grupo_webhook.py</files>
  <read_first>
    - leadflow-backend/app/routers/grupo_webhook.py (depois do patch — confirmar estrutura)
  </read_first>
  <action>
    Esta task NAO altera codigo — valida via grep que nenhum dos 6 fixes 08/06 / 7 fases v2.1 foi regredido em grupo_webhook.py.

    Rodar esta sequencia de checks (todos devem PASSAR):

    ```bash
    # 1. Handler v2.1 ainda existe (processar_entrada_lead_no_grupo)
    grep -c "from app.services.grupo_fallback import processar_entrada_lead_no_grupo" leadflow-backend/app/routers/grupo_webhook.py
    # Esperado: 1

    # 2. Heuristica @lid unique-aguardando v2.1 nao foi removida
    grep -c "telefones_aguardando" leadflow-backend/app/routers/grupo_webhook.py
    # Esperado: >= 2 (declaracao + comparacao len == 1)

    # 3. _salvar_lead_lid (marker LEAD_LID) ainda existe e ainda e chamado
    grep -c "_salvar_lead_lid" leadflow-backend/app/routers/grupo_webhook.py
    # Esperado: >= 2 (def + chamada)

    # 4. Marker GRUPO_AGUARDANDO_ENTRADA lookup nao foi alterado (fase 1 18/05)
    grep -c "GRUPO_AGUARDANDO_ENTRADA:{grupo_jid}" leadflow-backend/app/routers/grupo_webhook.py
    # Esperado: >= 2 (path principal + path @lid)

    # 5. Marker LEGADO GRUPO_FALLBACK_ATIVADO ainda checado (compat)
    grep -c "GRUPO_FALLBACK_ATIVADO:{grupo_jid}" leadflow-backend/app/routers/grupo_webhook.py
    # Esperado: 1

    # 6. Case-insensitive empresa lookup (FIX 21/05) preservado
    grep -c "_i.lower() == instancia.lower()" leadflow-backend/app/routers/grupo_webhook.py
    # Esperado: 1

    # 7. Rota inalterada
    grep -c '@router.post("/agente/evolution/webhook/grupo")' leadflow-backend/app/routers/grupo_webhook.py
    # Esperado: 1
    ```

    Documentar resultados (todos OK) em commit message do patch. Se QUALQUER falhar, REVERTER patch e re-investigar.

    Adicionalmente, smoke test do parse_evolution_timestamp com payload realista:
    ```bash
    cd leadflow-backend && python -c "
    from app.services.grupo_membership import parse_evolution_timestamp
    from datetime import datetime
    # Epoch segundos (Baileys padrao)
    assert parse_evolution_timestamp(1717948800).year == 2024
    # Epoch ms (raro)
    assert parse_evolution_timestamp(1717948800000).year == 2024
    # ISO string
    assert parse_evolution_timestamp('2024-06-09T14:00:00Z').year == 2024
    # Missing
    assert isinstance(parse_evolution_timestamp(None), datetime)
    # Invalid
    assert isinstance(parse_evolution_timestamp('garbage'), datetime)
    print('PARSE_TS OK')
    "
    ```
  </action>
  <verify>
    <automated>cd leadflow-backend && python -c "from app.routers.grupo_webhook import _processar_grupo_payload, _salvar_lead_lid, _normalizar_jid_telefone; from app.services.grupo_membership import upsert_grupo_membership, parse_evolution_timestamp; print('ALL_IMPORTS_OK')" 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - Todos os 7 greps acima retornam o valor esperado (documentar resultado no SUMMARY)
    - Smoke test parse_evolution_timestamp imprime `PARSE_TS OK`
    - Smoke import `from app.routers.grupo_webhook import _processar_grupo_payload` sem ImportError
    - `git diff --stat leadflow-backend/app/routers/grupo_webhook.py` mostra apenas linhas ADICIONADAS (zero removidas) — Path 1 e puramente aditivo
  </acceptance_criteria>
  <done>Validacao ortogonal completa: handlers v2.1 + fixes 08/06 + heuristicas @lid preservados; smoke imports OK; diff apenas aditivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evolution → grupo_webhook | Payload nao-confiavel (Pitfall 14 — signature auth OUT-OF-SCOPE Fase 10, flagged para fase seguinte) |
| Path 1 UPSERT → Postgres | Mitigada pelo helper (Plan 02 threat model T-10-01..05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | Webhook re-delivery (mesmo evento 2x) duplica row | mitigate | RPC SQL UPSERT idempotente por (empresa_id, grupo_jid, COALESCE(tel,lid)); 2a chamada retorna `was_update=True` (action='update' no log). `last_event_id` permite deteccao adicional via auditoria. |
| T-10-08 | Tampering | Out-of-order: REMOVE chega antes do ADD com timestamp mais antigo | mitigate | RPC guard `p_entrou_em >= atualizado_em` — UPSERT antigo vira no-op (`action='noop'`). Path 1 nao trata REMOVE (Fase 13). |
| T-10-09 | Spoofing | POST falso com `payload.instance` de outra empresa | accept | Sem signature validation Fase 10 (Pitfall 14 OUT-OF-SCOPE). Mitigacao parcial: atacante precisa conhecer (a) instance_name valido, (b) grupo_jid valido, (c) telefone de lead existente. Lookup `empresas.evolution_instancia == payload.instance` ja filtra: instance fake retorna `empresa_id=''` e handler aborta na linha 154. |
</threat_model>

<verification>
- `git diff leadflow-backend/app/routers/grupo_webhook.py` mostra apenas adicoes (zero linhas removidas)
- 7 greps de regressao todos OK (Task 2)
- Suite pytest tests/ ainda passing (modulo nao tem teste novo nesta plan — Plan 05 adiciona)
- Smoke imports OK
- Logs Easypanel pos-deploy: aparecer `[MEMB-WRITE] OK ... fonte=webhook_add action=insert` quando proximo webhook GROUP_PARTICIPANTS_UPDATE chegar
</verification>

<success_criteria>
- `wc -l leadflow-backend/app/routers/grupo_webhook.py` antes vs depois: aumento de ~40-50 linhas, nenhuma removida
- Suite v2.1 (tests/test_qualificacao_lock_phase7.py + test_verificar_lead_no_grupo_phase3.py + test_agente_fixes.py + test_calendar_buffer.py) continua passing
- Modulo importa sem erro
- Plans 04/05 podem assumir Path 1 funcionando
</success_criteria>

<output>
After completion, create `.planning/phases/10-schema-3-paths-webhook-write-cache-standalone/10-03-SUMMARY.md`
</output>
</content>
</invoke>