---
phase: 13-leave-handler-fsm-audit-monotonico
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - leadflow-backend/app/routers/admin.py
  - leadflow-backend/app/routers/leads.py
  - leadflow-backend/app/services/grupo_fallback.py
autonomous: true
requirements: [FSM-AUDIT-03]
must_haves:
  truths:
    - "All 9 set_grupo_state/transition_grupo_state callsites pass reason= and caller= explicitly"
    - "No callsite passes caller='unknown' or omits caller"
    - "The admin promover_ativo callsite (C2) is the only one that passes force=True"
    - "All modified source files ast-parse cleanly after migration"
  artifacts:
    - path: "leadflow-backend/app/services/grupo_fallback.py"
      provides: "4 migrated callsites (C5, C6, C7/T1, C8/T2) with reason+caller"
      contains: "caller="
    - path: "leadflow-backend/app/routers/admin.py"
      provides: "C2 promover_ativo migrated with caller=admin_promover_ativo"
      contains: "admin_promover_ativo"
    - path: "leadflow-backend/app/routers/leads.py"
      provides: "C3 + C4 migrated"
      contains: "caller="
  key_links:
    - from: "9 callsites"
      to: "set_grupo_state / transition_grupo_state"
      via: "explicit reason= + caller= kwargs"
      pattern: "caller="
---

<objective>
Migrate every caller of the FSM API to the new mandatory `reason`/`caller` signature introduced in Plan 01. After Plan 01, the existing callsites pass too few args and would raise TypeError at runtime — this plan fixes all of them with descriptive, traceable `caller` values so the GSC audit log is complete and FSM-AUDIT-03's CI grep check (authored in Plan 04) passes.

Purpose: An audit trail is only as good as its coverage. Every state change must name who caused it. This plan ensures no `set_grupo_state` call anywhere in the codebase is anonymous.

Output: migrated callsites across `admin.py`, `leads.py`, `grupo_fallback.py`.

NOTE: `grupo_state.py` itself (the C1 internal `transition_grupo_state -> set_grupo_state` delegation) was already handled in Plan 01 — this plan touches only the EXTERNAL callers. The C9 webhook_remove callsite is created in Plan 02. Both are excluded here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-leave-handler-fsm-audit-monotonico/13-RESEARCH.md
@leadflow-backend/CLAUDE.md
@.planning/phases/13-leave-handler-fsm-audit-monotonico/13-01-PLAN.md

<interfaces>
<!-- New signatures from Plan 01 (Wave 1, complete). Migrate every call to match. -->
New signatures:
- set_grupo_state(sb, empresa_id, telefone, agendamento_id, state, *, reason, caller, force=False) -> bool
- transition_grupo_state(sb, empresa_id, telefone, agendamento_id, from_states, to_state, *, reason, caller, force=False) -> bool

EXACT callsite map (verified via grep 2026-06-09 — file:line + exact caller value to add):

- C2  admin.py:917           set_grupo_state(..., STATE_ATIVO)         reason="admin_promover_ativo"          caller="admin_promover_ativo"      force=True
- C3  leads.py:137           set_grupo_state(..., STATE_ATIVO)         reason="reuso_grupo_lead_confirmado"   caller="leads_reuso_grupo"
- C4  leads.py:256           set_grupo_state(..., STATE_ATIVO)         reason="promote_admin"                 caller="leads_promote_admin"
- C5  grupo_fallback.py:278  set_grupo_state(..., STATE_ATIVO)         reason="lead_ja_no_grupo_retroativo"   caller="fallback_retroativo_ativo"
- C6  grupo_fallback.py:415  set_grupo_state(..., STATE_AGUARDANDO)    reason="grupo_criado"                  caller="fallback_criar_grupo"
- C7  grupo_fallback.py:890  transition_grupo_state(... FALLBACK_1_1)  reason="timeout_30min"                 caller="fallback_timeout_30min"
- C8  grupo_fallback.py:994  transition_grupo_state(... ATIVO)         reason="lead_entrou"                   caller="fallback_lead_entrou"

C2 force=True rationale: operator visually confirmed the lead is in the group — the one sanctioned override path. All other callsites use force=False (default).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate admin.py (C2) + leads.py (C3, C4)</name>
  <files>leadflow-backend/app/routers/admin.py, leadflow-backend/app/routers/leads.py</files>
  <read_first>
    - leadflow-backend/app/routers/admin.py (lines 914-923 — promover_ativo block)
    - leadflow-backend/app/routers/leads.py (lines 130-140 and 250-260 — the two STATE_ATIVO sets)
  </read_first>
  <action>
    C2 (admin.py:917): change to
    set_grupo_state(sb, empresa_id, telefone, agendamento_id, STATE_ATIVO, reason="admin_promover_ativo", caller="admin_promover_ativo", force=True)
    C3 (leads.py:137): add reason="reuso_grupo_lead_confirmado", caller="leads_reuso_grupo" to the existing call.
    C4 (leads.py:256): add reason="promote_admin", caller="leads_promote_admin" to the existing call.
    Preserve the exact existing positional args and surrounding control flow — only add the kwargs.
  </action>
  <verify>
    <automated>cd leadflow-backend; python -c "import ast; ast.parse(open('app/routers/admin.py').read()); ast.parse(open('app/routers/leads.py').read()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep finds caller="admin_promover_ativo" in app/routers/admin.py
    - grep finds force=True in app/routers/admin.py (the promover_ativo line)
    - grep finds caller="leads_reuso_grupo" in app/routers/leads.py
    - grep finds caller="leads_promote_admin" in app/routers/leads.py
    - both files ast.parse clean (the python -c above prints OK)
  </acceptance_criteria>
  <done>C2 (with force=True), C3, C4 pass reason+caller; admin.py and leads.py parse clean.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate grupo_fallback.py (C5, C6, C7, C8)</name>
  <files>leadflow-backend/app/services/grupo_fallback.py</files>
  <read_first>
    - leadflow-backend/app/services/grupo_fallback.py (lines 270-285 C5; lines 408-420 C6; lines 883-900 C7; lines 985-1000 C8)
  </read_first>
  <action>
    C5 (grupo_fallback.py:278): add reason="lead_ja_no_grupo_retroativo", caller="fallback_retroativo_ativo". (Call has a get_grupo_state() is None guard so the monotonic guard never blocks it — safe.)
    C6 (grupo_fallback.py:415): add reason="grupo_criado", caller="fallback_criar_grupo".
    C7 (grupo_fallback.py:890, transition_grupo_state): add reason="timeout_30min", caller="fallback_timeout_30min" as kwargs after to_state=STATE_FALLBACK_1_1.
    C8 (grupo_fallback.py:994, transition_grupo_state): add reason="lead_entrou", caller="fallback_lead_entrou" after to_state=STATE_ATIVO.
    Preserve all existing args + control flow; only add kwargs.
  </action>
  <verify>
    <automated>cd leadflow-backend; python -c "import ast; ast.parse(open('app/services/grupo_fallback.py').read()); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "caller=" in app/services/grupo_fallback.py returns >= 4
    - grep finds caller="fallback_timeout_30min", caller="fallback_lead_entrou", caller="fallback_criar_grupo", caller="fallback_retroativo_ativo"
    - file ast.parse clean
  </acceptance_criteria>
  <done>All 4 grupo_fallback.py callsites (2 set_grupo_state + 2 transition_grupo_state) pass reason+caller.</done>
</task>

<task type="auto">
  <name>Task 3: Whole-repo no-anonymous-caller sweep</name>
  <files>leadflow-backend/scripts/check_fsm_callers.py</files>
  <read_first>
    - 13-RESEARCH.md "FSM caller migration" (lines 161-187) — confirm no callsite missed
  </read_first>
  <action>
    Run a repo-wide grep to confirm every set_grupo_state( / transition_grupo_state( call (excluding the def lines in grupo_state.py and the C9 webhook_remove line from Plan 02) includes caller=. If any callsite is found without caller= (a hidden one not in the map), migrate it with a descriptive caller derived from its context and document it in the SUMMARY. Write the AST sweep as a standalone script file tests/_fsm_sweep_check.py (so the one-liner quoting is robust on PowerShell) OR fold it into Plan 04's tests/test_fsm_caller_ci.py — coordinate: this task only needs to PROVE no anonymous callsite remains right now.
    Implementation: create a tiny script leadflow-backend/scripts/check_fsm_callers.py that ast-walks app/**/*.py, finds Call nodes whose func name is set_grupo_state or transition_grupo_state, and asserts 'caller' is among the call keywords (skipping FunctionDef). Exit non-zero with the offending file:line list if any are missing.
  </action>
  <verify>
    <automated>cd leadflow-backend; python scripts/check_fsm_callers.py</automated>
  </verify>
  <acceptance_criteria>
    - scripts/check_fsm_callers.py exists and exits 0 (no anonymous callsite)
    - The script's output lists 0 violations
    - grep across leadflow-backend/app shows NO occurrence of caller="unknown"
  </acceptance_criteria>
  <done>No set_grupo_state/transition_grupo_state callsite in app/ omits caller= or uses "unknown"; sweep script proves it and is reusable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller literals -> GSC audit | each migrated callsite supplies a fixed code literal as caller |
| force=True authorization | only the admin promover_ativo path may bypass the monotonic guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Elevation of Privilege | force=True in a non-admin callsite | mitigate | This plan adds force=True to exactly ONE callsite (C2 admin.py promover_ativo, behind _check_auth). Plan 04's CI test asserts force=True appears only in admin.py. All other 6 callsites use the default force=False. |
| T-13-09 | Repudiation | anonymous FSM mutation | mitigate | Task 3 sweep + Plan 04 CI test guarantee every callsite names its caller; no "unknown" sentinel permitted. |
</threat_model>

<verification>
- All 3 modified files ast.parse clean
- scripts/check_fsm_callers.py exits 0
- grep confirms force=True exists only in admin.py (cross-check in Plan 04 CI test)
</verification>

<success_criteria>
- FSM-AUDIT-03: every set_grupo_state/transition_grupo_state callsite passes reason+caller; none anonymous; CI-verifiable
</success_criteria>

<output>
After completion, create `.planning/phases/13-leave-handler-fsm-audit-monotonico/13-03-SUMMARY.md`
</output>
