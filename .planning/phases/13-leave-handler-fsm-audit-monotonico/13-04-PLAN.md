---
phase: 13-leave-handler-fsm-audit-monotonico
plan: 04
type: tdd
wave: 3
depends_on: ["13-01", "13-02", "13-03"]
files_modified:
  - leadflow-backend/tests/test_fsm_monotonic.py
  - leadflow-backend/tests/test_fsm_caller_ci.py
  - leadflow-backend/tests/test_leave_handler.py
  - leadflow-backend/tests/test_lead_in_group_decision.py
  - leadflow-backend/app/routers/admin.py
autonomous: true
requirements: [FSM-AUDIT-01, FSM-AUDIT-02, FSM-AUDIT-03, LEAVE-01, LEAVE-02, LEAVE-03]
must_haves:
  truths:
    - "test_fsm_monotonic.py proves ATIVO->AGUARDANDO blocked without force, allowed with force, and that every transition writes a GSC marker"
    - "test_fsm_caller_ci.py proves set_grupo_state has no default caller AND no app/ callsite omits caller"
    - "test_leave_handler.py proves REMOVE marks saiu_em idempotently + LEAD_SAIU_GRUPO marker + FSM LEFT_GROUP + D-1 DM redirect"
    - "test_lead_in_group_decision.py covers the source='left_group' case"
    - "/admin/grupo/status?timeline=true returns GSC: audit markers grouped by agendamento"
    - "Full pytest suite passes in under 60s"
  artifacts:
    - path: "leadflow-backend/tests/test_fsm_monotonic.py"
      provides: "Monotonic FSM + audit marker tests"
      contains: "force"
    - path: "leadflow-backend/tests/test_fsm_caller_ci.py"
      provides: "AST-based CI grep test for mandatory caller"
      contains: "ast"
    - path: "leadflow-backend/tests/test_leave_handler.py"
      provides: "LEAVE-01/02/03 coverage"
      contains: "saiu_em"
    - path: "leadflow-backend/app/routers/admin.py"
      provides: "?timeline=true returning GSC: timeline"
      contains: "GSC:"
  key_links:
    - from: "/admin/grupo/status"
      to: "conversas GSC: markers"
      via: "timeline query param"
      pattern: "GSC:"
---

<objective>
Lock the phase behind a test suite and surface the audit trail. Author the three new test files referenced by Plans 01-03 (`test_fsm_monotonic.py`, `test_fsm_caller_ci.py`, `test_leave_handler.py`), extend `test_lead_in_group_decision.py` with the left_group case, and add the `?timeline=true` query param to the existing `/admin/grupo/status` endpoint so the dashboard can render an auditable timeline.

Purpose: FSM-AUDIT-03 IS a test (the CI grep check). LEAVE-01/02/03 and FSM-AUDIT-01/02 need regression coverage so a future change can't silently break departure handling or the monotonic guard. The timeline closes SC5 (dashboard exposes the audit log).

Output: 4 test files + the timeline query param on `/admin/grupo/status`.

LOCKED DECISIONS honored:
- D-2: timeline filters by `GSC:%` prefix (not GRUPO_STATE_CHANGE).
- D-3: NO new endpoint — `?timeline=true` augments the existing `/admin/grupo/status`; the existing frontend consumes it; minimal JSON display only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-leave-handler-fsm-audit-monotonico/13-RESEARCH.md
@leadflow-backend/CLAUDE.md
@.planning/phases/13-leave-handler-fsm-audit-monotonico/13-01-PLAN.md
@.planning/phases/13-leave-handler-fsm-audit-monotonico/13-02-PLAN.md

<interfaces>
<!-- Test harness pattern + final contracts to assert against. -->
conftest.py (tests/conftest.py): injects an app.services.evolution STUB into sys.modules so async tests can run without httpx/idna. verificar_lead_no_grupo defaults to {in_group: False}; override via monkeypatch on the stub. REUSE this — do not re-stub.

Existing test patterns to mirror:
- tests/test_lead_in_group_decision.py — mocks the sb client + _query_membership_row, asserts lead_in_group verdicts
- tests/test_grupo_fallback_migration.py — ast-based source inspection (precedent for the caller CI test)
- tests/test_probe_retry.py — APScheduler/job tests

Final contracts (post Plans 01-03):
- grupo_state.set_grupo_state(sb, empresa_id, telefone, agendamento_id, state, *, reason, caller, force=False)
- grupo_state.transition_grupo_state(..., from_states, to_state, *, reason, caller, force=False)
- grupo_state.STATE_LEFT_GROUP, ALLOWED_TRANSITIONS, _is_transition_allowed
- GSC marker conteudo: "GSC:{ag_id}:{from}:{to}:{reason}:{caller}" and BLOCKED variant "GSC:{ag_id}:{from}:{to}:BLOCKED:{reason}:{caller}"
- grupo_membership.lead_in_group returns source='left_group' when row.saiu_em is not None
- grupo_membership._mark_membership_left(sb, *, empresa_id, grupo_jid, telefone, lid, saiu_em) -> int
- grupo_webhook._processar_remove(payload, data), _trigger_fsm_left_group(sb, empresa_id, telefone, grupo_jid)

admin.py /admin/grupo/status (lines 784-874): currently reads GRUPO_STATE: markers, dedupes by (telefone, ag_id), returns items. Add `timeline: bool = Query(False)` param.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: test_fsm_monotonic.py + test_fsm_caller_ci.py (FSM-AUDIT-01/02/03)</name>
  <files>leadflow-backend/tests/test_fsm_monotonic.py, leadflow-backend/tests/test_fsm_caller_ci.py</files>
  <read_first>
    - leadflow-backend/tests/conftest.py
    - leadflow-backend/tests/test_grupo_fallback_migration.py (ast precedent)
    - leadflow-backend/app/services/grupo_state.py (final, post Plan 01)
    - 13-RESEARCH.md "Validation Architecture" (lines 457-538) — exact test specs incl. the ast snippet
  </read_first>
  <behavior>
    test_fsm_monotonic.py (mock sb that records conversas.insert calls):
    - set_grupo_state None->ATIVO with reason/caller returns True and writes GSC:{ag}:None:ATIVO:... + legacy GRUPO_STATE: marker
    - set_grupo_state ATIVO->AGUARDANDO (current=ATIVO) without force returns False, writes GSC ...:BLOCKED:..., does NOT write legacy GRUPO_STATE: marker
    - set_grupo_state ATIVO->AGUARDANDO with force=True returns True
    - set_grupo_state LEFT_GROUP->ATIVO without force returns False (terminal)
    - any->LEFT_GROUP without force returns True
    - transition_grupo_state passes reason+caller through to set_grupo_state (assert via recorded GSC marker containing the caller)
    - calling set_grupo_state without caller= raises TypeError
    test_fsm_caller_ci.py (FSM-AUDIT-03): the two functions from research lines 504-528 (test_set_grupo_state_has_no_default_caller via ast kwonly+kw_defaults; test_no_callsite_omits_caller scanning app/**/*.py) PLUS test_force_true_only_in_admin: assert that across app/, the only file containing a set_grupo_state/transition call with force=True is app/routers/admin.py.
  </behavior>
  <action>
    Create tests/test_fsm_monotonic.py. Use a FakeSB recorder: a class whose .table("conversas").insert(d).execute() appends d to a list, and .table("conversas").select(...).order(...).limit(...).execute() returns a configurable get_grupo_state result (so you can simulate current=ATIVO etc). Simpler: monkeypatch grupo_state.get_grupo_state to return the desired current state, and pass a FakeSB whose insert records conteudo. Assert on recorded conteudo strings.
    Create tests/test_fsm_caller_ci.py using ast per research lines 501-528. Resolve paths relative to the test file (pathlib.Path(__file__).parent.parent / "app") so it runs from any cwd. For test_force_true_only_in_admin, ast-walk app/**/*.py for Call nodes to set_grupo_state/transition_grupo_state with a force keyword whose value is Constant True, and assert every such node's file is app/routers/admin.py.
  </action>
  <verify>
    <automated>cd leadflow-backend; pytest tests/test_fsm_monotonic.py tests/test_fsm_caller_ci.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_fsm_monotonic.py and tests/test_fsm_caller_ci.py exist
    - pytest on both files exits 0
    - test_fsm_caller_ci.py contains an ast.parse on grupo_state.py and a walk over app/**/*.py
    - test_force_true_only_in_admin is present and passes (force=True only in admin.py)
  </acceptance_criteria>
  <done>Monotonic guard, audit markers, mandatory caller, and force-only-in-admin all covered and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_leave_handler.py + left_group case in test_lead_in_group_decision.py (LEAVE-01/02/03)</name>
  <files>leadflow-backend/tests/test_leave_handler.py, leadflow-backend/tests/test_lead_in_group_decision.py</files>
  <read_first>
    - leadflow-backend/tests/test_lead_in_group_decision.py (existing decision-tree tests to extend)
    - leadflow-backend/app/routers/grupo_webhook.py (final, post Plan 02 — _processar_remove)
    - leadflow-backend/app/services/grupo_membership.py (final — _mark_membership_left, lead_in_group left_group branch)
    - 13-RESEARCH.md "Validation Architecture" test map (lines 468-484) + test details (lines 486-533)
  </read_first>
  <behavior>
    test_leave_handler.py:
    - _processar_grupo_payload with action="remove" + 1 telefone → _mark_membership_left called once (mock returns 1 row) → LEAD_SAIU_GRUPO:{grupo_jid} inserted → set_grupo_state called with STATE_LEFT_GROUP, caller="webhook_remove"
    - idempotency: second REMOVE (mock _mark_membership_left returns 0) does not raise; LEAD_SAIU_GRUPO insert collision (unique) swallowed gracefully
    - _mark_membership_left builds an UPDATE with .is_('saiu_em','null') guard (assert via fake query recorder) — replay-safe
    - LEAVE-02 D-1: with a lead_in_group result source='left_group', the D-1 destino logic picks DM (telefone), not grupo_jid. (Test the decision predicate, mocking lead_in_group to return source='left_group'.)
    test_lead_in_group_decision.py (extend):
    - _query_membership_row returns row with saiu_em="2026-06-09T23:00:00" → lead_in_group returns {in_group: False, source: 'left_group'} and verificar_lead_no_grupo (stub) is NOT called (assert via a call counter on the stub)
  </behavior>
  <action>
    Create tests/test_leave_handler.py mirroring the async-test setup of test_lead_in_group_decision.py (uses the conftest evolution stub; monkeypatch grupo_membership._mark_membership_left, grupo_state.set_grupo_state, and the FakeSB recorder for conversas inserts). For the empresa-by-instancia lookup in the webhook, mock sb.table("empresas").select(...).execute() to return a row matching the payload instance.
    Extend tests/test_lead_in_group_decision.py with the left_group test: monkeypatch grupo_membership._query_membership_row to return a dict with saiu_em set, install a call-counter wrapper on the evolution stub's verificar_lead_no_grupo, call lead_in_group, assert source=='left_group' and counter==0.
  </action>
  <verify>
    <automated>cd leadflow-backend; pytest tests/test_leave_handler.py tests/test_lead_in_group_decision.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_leave_handler.py exists and pytest exits 0
    - test_lead_in_group_decision.py contains a left_group assertion (grep "left_group")
    - the left_group test asserts probe NOT called (call counter == 0)
    - idempotency test for REMOVE present (second call no-ops)
  </acceptance_criteria>
  <done>LEAVE-01/02/03 covered: REMOVE marks saiu_em idempotently, drops the marker, transitions FSM to LEFT_GROUP, and lead_in_group/D-1 short-circuit to DM without probing.</done>
</task>

<task type="auto">
  <name>Task 3: ?timeline=true on /admin/grupo/status + full suite gate</name>
  <files>leadflow-backend/app/routers/admin.py</files>
  <read_first>
    - leadflow-backend/app/routers/admin.py (lines 784-874 — admin_grupo_status)
    - 13-RESEARCH.md "/admin/grupos dashboard — audit timeline" (lines 431-453)
  </read_first>
  <action>
    Add `timeline: bool = Query(False, description="incluir state_timeline GSC: por agendamento")` to admin_grupo_status. After building `items` (before the return), if timeline is True: query conversas for `.like("conteudo", "GSC:%")` scoped to empresa_id + role=sistema, ordered by criado_em ASC, limit 5000. Parse each conteudo `GSC:{ag_id}:{from}:{to}:{reason}:{caller}` via split(":", 5), group by ag_id into a list of {from, to, reason, caller, criado_em}, and attach as `state_timeline` to the matching item (by agendamento_id). Items without GSC markers get an empty list. Preserve existing auth (_check_auth) + behavior when timeline is False (default) — zero change to current callers.
    Then run the full suite as the phase gate. The v2.1 regression tests (13 tests) + v2.2 tests must all stay green.
  </action>
  <verify>
    <automated>cd leadflow-backend; pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - grep finds `like("conteudo", "GSC:%")` in app/routers/admin.py
    - grep finds `state_timeline` in app/routers/admin.py
    - grep finds `timeline: bool = Query(` in app/routers/admin.py
    - admin.py ast.parse clean
    - full pytest suite (tests/) exits 0
  </acceptance_criteria>
  <done>/admin/grupo/status?timeline=true returns a per-agendamento GSC audit timeline; default behavior unchanged; entire suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /admin/grupo/status?timeline=true | authenticated admin endpoint returning audit data scoped by empresa_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | Information Disclosure | timeline leaks cross-tenant GSC markers | mitigate | the GSC query is scoped `.eq("empresa_id", empresa_id)` exactly like the existing GRUPO_STATE query; _check_auth(authorization) runs first (same gate as the base endpoint). No new auth surface. |
| T-13-11 | Elevation of Privilege | unauthenticated access to audit timeline | mitigate | timeline is a query param on an already-authenticated endpoint; await _check_auth(authorization) is unchanged and runs before any query. |
| T-13-12 | DoS | timeline query returns unbounded markers | mitigate | GSC query bounded with .limit(5000) (same pattern as the base .limit(2000)); ~150 markers/day means realistic volumes are tiny. |
</threat_model>

<verification>
- pytest tests/ exits 0 (full suite: v2.1 13 tests + v2.2 + new phase 13 tests)
- admin.py ast.parse clean; timeline=False default path byte-identical behavior
- All FSM-AUDIT + LEAVE requirements have at least one passing test
</verification>

<success_criteria>
- FSM-AUDIT-01: monotonic guard tested (block + force)
- FSM-AUDIT-02: GSC marker writes tested (accept + BLOCKED)
- FSM-AUDIT-03: CI test proves mandatory caller + no anonymous callsite + force only in admin
- LEAVE-01/02/03: REMOVE + saiu_em + DM redirect + LEFT_GROUP all tested
- SC5: /admin/grupo/status?timeline=true exposes the auditable timeline
- Full suite < 60s, green
</success_criteria>

<output>
After completion, create `.planning/phases/13-leave-handler-fsm-audit-monotonico/13-04-SUMMARY.md`
</output>
