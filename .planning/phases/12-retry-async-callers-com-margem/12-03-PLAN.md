---
phase: 12-retry-async-callers-com-margem
plan: 03
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - leadflow-backend/tests/test_probe_retry.py
autonomous: true
requirements: [PROBE-RETRY-01, PROBE-RETRY-02]
must_haves:
  truths:
    - "schedule_probe_retry enfileira job com id deterministico + replace_existing (PROBE-RETRY-01)"
    - "max_age guard descarta job com enqueued_at > 600s atras sem chamar probe (caso Valquiria)"
    - "table-first short-circuit encerra sem probe HTTP quando row existe (caso Rosania)"
    - "probe_in_group None nao enfileira; False enfileira; schedule_retry_on_negative=False nunca enfileira"
    - "attempts esgotados param sem re-enqueue; caller confirmacao ativa retry; aquec mantem sincrono"
  artifacts:
    - path: "leadflow-backend/tests/test_probe_retry.py"
      provides: "Suite pytest cobrindo Validation Architecture (11 testes)"
      contains: "def test_max_age_guard_expired"
  key_links:
    - from: "tests/test_probe_retry.py"
      to: "app.services.grupo_membership"
      via: "import schedule_probe_retry, _probe_retry_job, lead_in_group + mock scheduler"
      pattern: "from app.services.grupo_membership import"
---

<objective>
Implementar `tests/test_probe_retry.py` cobrindo a Validation Architecture do 12-RESEARCH.md: scheduling deterministico, max_age expiry (Valquiria), table-first short-circuit (Rosania), idempotencia, ativacao condicional (False vs None vs stub-off), e os 2 callers (confirmacao ativa retry, aquec sincrono). APScheduler e mockado/inspecionado — nenhuma execucao real de job.

Purpose: Bloquear regressao do motor de retry e da migracao de callers via pytest. Fecha PROBE-RETRY-01 + PROBE-RETRY-02 com cobertura automatizada.
Output: 1 arquivo de teste novo, suite roda em <60s.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-retry-async-callers-com-margem/12-RESEARCH.md

<interfaces>
<!-- Simbolos sob teste (apos Plans 12-01/12-02) + infra de teste existente. -->

# app/services/grupo_membership.py
def schedule_probe_retry(empresa_id, grupo_jid, telefone, lid, attempts_left=3, max_age_seconds=600, evo_url="", evo_key="", evo_inst="", agendamento_id="") -> None
async def _probe_retry_job(empresa_id, grupo_jid, telefone, lid, attempts_left, max_age_seconds, enqueued_at, evo_url, evo_key, evo_inst, agendamento_id="") -> None
async def lead_in_group(sb, empresa_id, telefone, grupo_jid, lid="", *, evo_url="", evo_key="", evo_inst="", allow_probe_fallback=True, schedule_retry_on_negative=False) -> dict
def _query_membership_row(sb, empresa_id, grupo_jid, telefone, lid)  # monkeypatchavel
def _reset_probe_locks_for_tests() -> None

# app/services/probe_cache.py
def get(empresa_id, grupo_jid, telefone, lid) -> bool | None
def clear_all() -> None

# tests/conftest.py (JA EXISTE): injeta stub de app.services.evolution em sys.modules
#   stub.verificar_lead_no_grupo e coroutine; testes monkeypatcham o atributo.
#   grupo_membership.py importa verificar_lead_no_grupo LOCALMENTE dentro das funcoes,
#   entao patch alvo = "app.services.grupo_membership.verificar_lead_no_grupo" quando
#   o import ja foi resolvido, OU patch no stub. Seguir padrao de test_lead_in_group_coalesce.py.

# Suite usa @pytest.mark.asyncio EXPLICITO (sem asyncio_mode=auto). Comando: pytest tests/test_probe_retry.py -x -q
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Confirmar infra de teste (conftest + asyncio marker + patch target)</name>
  <files>leadflow-backend/tests/test_probe_retry.py</files>
  <read_first>
    - leadflow-backend/tests/conftest.py (stub de evolution + sys.modules)
    - leadflow-backend/tests/test_lead_in_group_coalesce.py (padrao de mock scheduler + reset_state + patch de verificar_lead_no_grupo + @pytest.mark.asyncio)
    - leadflow-backend/tests/test_lead_in_group_decision.py (padrao de monkeypatch _query_membership_row + sb_mock)
  </read_first>
  <behavior>
    - Confirmar como os testes existentes patcham verificar_lead_no_grupo e _query_membership_row (alvo exato do patch string)
    - Confirmar marker asyncio: @pytest.mark.asyncio explicito em cada teste async
    - Criar o esqueleto do arquivo test_probe_retry.py com imports + fixtures mock_scheduler + reset_state (autouse)
  </behavior>
  <action>
    Antes de escrever, rodar `grep -n "patch(\|@pytest.mark.asyncio\|_query_membership_row\|verificar_lead_no_grupo\|mock_scheduler\|reset" leadflow-backend/tests/test_lead_in_group_coalesce.py leadflow-backend/tests/test_lead_in_group_decision.py` pra copiar o padrao EXATO de patch target usado naqueles testes (eles ja funcionam — replicar).

    Criar `tests/test_probe_retry.py` com cabecalho:
    ```python
    """Fase 12 (PROBE-RETRY-01/02): testes do motor de retry async + callers.

    APScheduler mockado — captura jobs enfileirados sem execucao real.
    """
    import pytest
    from unittest.mock import MagicMock, patch
    from datetime import datetime, timedelta


    @pytest.fixture
    def mock_scheduler():
        """Captura scheduler.add_job sem APScheduler real."""
        jobs = []
        mock = MagicMock()
        def _capture(fn, trigger, **kwargs):
            jobs.append({"fn": fn, "trigger": trigger, **kwargs})
        mock.add_job.side_effect = _capture
        with patch("app.services.grupo_membership.scheduler", mock):
            yield mock, jobs


    @pytest.fixture(autouse=True)
    def reset_state():
        from app.services import probe_cache
        from app.services import grupo_membership as gm
        probe_cache.clear_all()
        gm._reset_probe_locks_for_tests()
        yield
        probe_cache.clear_all()
        gm._reset_probe_locks_for_tests()
    ```
    IMPORTANTE: `schedule_probe_retry` faz `from app.scheduler import scheduler` LOCALMENTE dentro da funcao. O patch deve mirar onde o nome e RESOLVIDO. Se o import e local (`from app.scheduler import scheduler` dentro de schedule_probe_retry), o patch correto e `patch("app.scheduler.scheduler", mock)`. Verificar no codigo do Plan 12-01 como o import foi feito e ajustar o patch target da fixture mock_scheduler de acordo (`app.scheduler.scheduler` se import local; `app.services.grupo_membership.scheduler` se import no topo do modulo). Usar o que casa com o codigo real.
  </action>
  <verify>
    <automated>cd leadflow-backend; python -c "import ast; ast.parse(open('tests/test_probe_retry.py',encoding='utf-8').read()); print('AST OK')"</automated>
  </verify>
  <acceptance_criteria>
    - arquivo `leadflow-backend/tests/test_probe_retry.py` existe
    - `grep -n "def mock_scheduler" leadflow-backend/tests/test_probe_retry.py` retorna 1
    - `grep -n "autouse=True" leadflow-backend/tests/test_probe_retry.py` retorna 1
    - patch target do scheduler casa com o import real do Plan 12-01 (verificado manualmente)
    - AST parse exits 0
  </acceptance_criteria>
  <done>Esqueleto + fixtures presentes, patch targets confirmados contra codigo real, AST valido.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Testes do motor de retry (PROBE-RETRY-01) — 9 testes</name>
  <files>leadflow-backend/tests/test_probe_retry.py</files>
  <read_first>
    - leadflow-backend/tests/test_probe_retry.py (esqueleto da Task 1)
    - leadflow-backend/app/services/grupo_membership.py (schedule_probe_retry + _probe_retry_job + ativacao STEP 5 — codigo do Plan 12-01)
    - .planning/phases/12-retry-async-callers-com-margem/12-RESEARCH.md secao "Validation Architecture" (test map + exemplos test_rosania_sequence + test_max_age_guard_expired)
  </read_first>
  <behavior>
    Cobrir o "Phase Requirements → Test Map" do RESEARCH para PROBE-RETRY-01:
    - test_schedule_creates_job: schedule_probe_retry enfileira 1 job, id="probe_retry:{jid}:{tel}:0", replace_existing=True, run_date ~= now+30s
    - test_idempotent_job_id: 2 enqueues mesma chave/attempt → mesmo job_id (replace_existing); inspeciona jobs capturados
    - test_max_age_guard_expired: _probe_retry_job com enqueued_at=now-47h → NAO chama probe (probe_called False), retorna cedo
    - test_max_age_guard_valid: enqueued_at recente → probe E chamado
    - test_table_hit_skips_probe: _query_membership_row retorna row → probe NAO chamado, probe_cache.get(...) True
    - test_rosania_sequence: lead_in_group False→schedule attempt0; _probe_retry_job attempt0 False→schedule attempt1; attempt1 com row→TABLE_HIT, cache True, probe nao chamado no passo 3
    - test_max_attempts_no_reschedule: _probe_retry_job attempts_left=0 + probe False → nenhum novo job enfileirado
    - test_stub_false_no_retry: lead_in_group(schedule_retry_on_negative=False) + probe False → 0 jobs enfileirados
    - test_none_probe_no_retry: probe retorna in_group=None (erro) + schedule_retry_on_negative=True → 0 jobs enfileirados
  </behavior>
  <action>
    Implementar os 9 testes async (cada um `@pytest.mark.asyncio` exceto os puramente sincronos de schedule). Usar os exemplos test_rosania_sequence e test_max_age_guard_expired do RESEARCH (secao Validation Architecture) verbatim como base, ajustando os patch targets confirmados na Task 1.

    Pontos criticos:
    - Para lead_in_group: passar sb_mock (MagicMock), patch `_query_membership_row` (return None pra forcar probe) e patch da coroutine de probe. Seguir test_lead_in_group_coalesce.py.
    - Para distinguir False vs None: a fake_probe retorna `{"in_group": False}` ou `{"in_group": None}`.
    - test_schedule_creates_job: assert `jobs[0]["id"] == "probe_retry:123@g.us:5562984551622:0"`, `jobs[0]["replace_existing"] is True`, e que `kwargs["enqueued_at"]` esta presente.
    - test_max_age: usar timedelta(hours=47) pro expired; verificar via flag nonlocal que probe nao foi chamado.
    - Para `_probe_retry_job` precisar de get_supabase: patch `app.services.grupo_membership.get_supabase` ou `app.db.client.get_supabase` (confirmar import local no codigo) retornando MagicMock, e patch `_query_membership_row` pro controle.
    - Garantir que cada teste limpa estado via fixture autouse (probe_cache.clear_all + _reset_probe_locks).
  </action>
  <verify>
    <automated>cd leadflow-backend; python -m pytest tests/test_probe_retry.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" leadflow-backend/tests/test_probe_retry.py` retorna >=9
    - cada nome do test map presente: `grep -n "test_schedule_creates_job\|test_idempotent_job_id\|test_max_age_guard_expired\|test_max_age_guard_valid\|test_table_hit_skips_probe\|test_rosania_sequence\|test_max_attempts_no_reschedule\|test_stub_false_no_retry\|test_none_probe_no_retry" leadflow-backend/tests/test_probe_retry.py` retorna 9 matches
    - `python -m pytest tests/test_probe_retry.py -x -q` exits 0 (todos passing)
  </acceptance_criteria>
  <done>9 testes de PROBE-RETRY-01 passing, cobrindo scheduling/idempotencia/max_age/table-first/sequencia Rosania/attempts esgotados/condicao False-vs-None-vs-stub.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Testes dos callers (PROBE-RETRY-02) + suite green completa</name>
  <files>leadflow-backend/tests/test_probe_retry.py</files>
  <read_first>
    - leadflow-backend/tests/test_probe_retry.py (testes das Tasks 1-2)
    - leadflow-backend/app/routers/confirmacao_agendamento.py linhas 285-308 (caller migrado Plan 12-02)
    - leadflow-backend/app/routers/warmup_grupo.py notif job (caller migrado Plan 12-02)
    - .planning/phases/12-retry-async-callers-com-margem/12-RESEARCH.md test map linhas PROBE-RETRY-02
  </read_first>
  <behavior>
    - test_confirmacao_caller_activates_retry: verifica (smoke/grep ou import-introspection) que o callsite D-1 usa schedule_retry_on_negative=True. Como o caller esta dentro de uma funcao grande dependente de muito contexto, usar verificacao por inspecao de source (ler o arquivo e assert que a string "schedule_retry_on_negative=True" aparece no bloco D-1) OU teste comportamental se factivel com mocks minimos.
    - test_aquec_stays_sync: verifica que _executar_aquecimento_grupo_job NAO contem schedule_probe_retry (inspecao de source do warmup_grupo.py)
    - Suite completa green: pytest tests/ -x -q passa (nao regride os 19 testes da Fase 11 nem os da Fase 10)
  </behavior>
  <action>
    Implementar os 2 testes de PROBE-RETRY-02 usando inspecao de source (robusto e barato — o caller esta em funcao grande com muitas deps):
    ```python
    import inspect

    def test_confirmacao_caller_activates_retry():
        """Caller D-1 (confirmacao_agendamento) ativa retry via lead_in_group."""
        import app.routers.confirmacao_agendamento as conf
        src = inspect.getsource(conf)
        assert "schedule_retry_on_negative=True" in src
        assert "from app.services.grupo_membership import lead_in_group" in src

    def test_aquec_stays_sync():
        """Aquec NAO migra — _executar_aquecimento_grupo_job sem schedule_probe_retry."""
        import app.routers.warmup_grupo as wg
        aquec_src = inspect.getsource(wg._executar_aquecimento_grupo_job)
        assert "schedule_probe_retry" not in aquec_src, \
            "aquec deve permanecer sincrono (decisao AGORA)"
        # notif job DEVE ter o retry
        notif_src = inspect.getsource(wg._executar_notificacao_grupo_job)
        assert "schedule_probe_retry" in notif_src
    ```
    Ajustar os nomes das funcoes (`_executar_aquecimento_grupo_job` / `_executar_notificacao_grupo_job`) ao que existir de fato em warmup_grupo.py — confirmar via grep antes. Se os imports de modulo falharem por deps pesadas (httpx/idna como no conftest), usar leitura direta do arquivo via `open(...).read()` em vez de `inspect.getsource(modulo)` para evitar import: ler o source do arquivo e fatiar pela funcao alvo (ou simplesmente assert sobre o arquivo inteiro pro caso confirmacao, e sobre os blocos do warmup).

    Depois rodar a suite COMPLETA e garantir green.
  </action>
  <verify>
    <automated>cd leadflow-backend; python -m pytest tests/ -x -q 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "test_confirmacao_caller_activates_retry\|test_aquec_stays_sync" leadflow-backend/tests/test_probe_retry.py` retorna 2 matches
    - `python -m pytest tests/test_probe_retry.py -x -q` exits 0
    - `python -m pytest tests/ -x -q` exits 0 (suite completa green — Fases 10/11 nao regredidas; roda em <60s)
  </acceptance_criteria>
  <done>2 testes de PROBE-RETRY-02 passing; suite completa green; nenhuma regressao das fases anteriores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testes → modulos de producao | testes importam/inspecionam codigo; mock isola scheduler/probe/DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Tampering | teste com falso-positivo (mock mascara bug real) | mitigate | mock_scheduler captura args reais e asserta id/run_date/replace_existing; max_age/table-first testam comportamento, nao so chamadas; suite completa roda contra codigo real das Fases 10-11 |
| T-12-09 | (Test integrity) | conftest stub esconde import real de evolution | accept | conftest ja validado nas Fases 9/11; padrao de patch replicado dos testes existentes que ja passam |
</threat_model>

<verification>
- `python -m pytest tests/test_probe_retry.py -x -q` exits 0 (>=11 testes)
- `python -m pytest tests/ -x -q` exits 0 (suite completa, <60s)
- 11 nomes do test map presentes
</verification>

<success_criteria>
- Validation Architecture coberta: scheduling, max_age (Valquiria), table-first (Rosania), idempotencia, condicao False/None/stub, 2 callers
- PROBE-RETRY-01 + PROBE-RETRY-02 com cobertura automatizada
- Suite completa green sem regressao
</success_criteria>

<output>
After completion, create `.planning/phases/12-retry-async-callers-com-margem/12-03-SUMMARY.md`
</output>
