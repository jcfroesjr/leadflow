---
phase: 11-lead-in-group-consumer-migrar-fallback-callers-coalescing-async
plan: 04
type: execute
wave: 4
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - leadflow-backend/app/main.py
  - leadflow-backend/app/routers/admin.py
autonomous: false
requirements:
  - MEMB-05
  - PROBE-COALESCE-01

must_haves:
  truths:
    - "BUILD_VERSION em app/main.py bumpado de '2026-06-09-grupo-membership-v2-fundacao' para '2026-06-XX-lead-in-group-consumer'"
    - "Endpoint /admin/grupo-membership/health estendido para incluir lookup_stats (lock_dict_size + phase atualizada para 11-consumer)"
    - "Smoke manual em prod confirma: cache hit funciona, membership hit funciona, probe fallback funciona, source='orphan' aparece quando lead em instância antiga consulta"
    - "User pode validar deploy via curl /version (BUILD_VERSION novo) + curl /admin/grupo-membership/health (phase=11-consumer + lookup_stats presente)"
  artifacts:
    - path: "leadflow-backend/app/main.py"
      provides: "BUILD_VERSION bump para 2026-06-XX-lead-in-group-consumer"
      contains: "lead-in-group-consumer"
    - path: "leadflow-backend/app/routers/admin.py"
      provides: "healthcheck endpoint estendido com lookup_stats e phase=11-consumer"
      contains: "lock_dict_size"
  key_links:
    - from: "BUILD_VERSION em main.py"
      to: "/version endpoint"
      via: "FastAPI route serving __version__"
      pattern: "BUILD_VERSION"
    - from: "/admin/grupo-membership/health"
      to: "_probe_locks dict do grupo_membership.py"
      via: "len() para size metric defensive try/except"
      pattern: "len\\(_probe_locks\\)"
---

<objective>
Fechar a Fase 11 com deploy. Bumpar BUILD_VERSION em app/main.py para refletir nova capability (lead-in-group-consumer). Estender endpoint healthcheck existente /admin/grupo-membership/health (criado na Fase 10 Plan 05) para incluir lookup_stats com lock_dict_size (cresce em uso pra detectar leak — Pitfall 5) e atualizar phase para 11-consumer. Checkpoint humano para validação em prod via smoke + redeploy.

Purpose: Tornar o deploy verificável + permitir observabilidade do consumer. User precisa saber qual build está em prod e quantos locks estão vivos. Healthcheck cresce gradualmente: Fase 10 entregou skinny, Fase 11 adiciona lookup stats, Fase 14 expande para full com alerting hooks.

Output: 2 arquivos modificados + redeploy Easypanel + smoke validation via checkpoint human-verify. NÃO mexer em outras partes do healthcheck (cache_stats, total_rows, last_write_at, writes_by_source_24h já entregues na Fase 10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-lead-in-group-consumer-migrar-fallback-callers-coalescing-async/11-RESEARCH.md
@.planning/phases/11-lead-in-group-consumer-migrar-fallback-callers-coalescing-async/11-01-SUMMARY.md
@.planning/phases/11-lead-in-group-consumer-migrar-fallback-callers-coalescing-async/11-02-SUMMARY.md
@.planning/phases/11-lead-in-group-consumer-migrar-fallback-callers-coalescing-async/11-03-SUMMARY.md
@leadflow-backend/app/main.py
@leadflow-backend/app/routers/admin.py

<interfaces>
<!-- Contratos do endpoint healthcheck existente — Fase 10 entregou -->

From app/main.py:239 (em prod):
BUILD_VERSION = "2026-06-09-grupo-membership-v2-fundacao"

From app/routers/admin.py:1133-1210 (healthcheck Fase 10 — em prod):
Endpoint GET /admin/grupo-membership/health retorna JSON com fields:
  total_rows: int
  last_write_at: str | None
  writes_by_source_24h: dict
  cache_stats: dict (size, maxsize, ttl_seconds, hits, misses, writes, invalidations)
  phase: "10-fundacao"

From app.services.grupo_membership (Plan 11-01 entrega):
_probe_locks: dict (vazio no init; cresce com chamadas a lead_in_group)
</interfaces>

<non_regression>
- NÃO MEXER em outros campos do healthcheck (total_rows, last_write_at, writes_by_source_24h, cache_stats) — Fase 10 entregou.
- NÃO MEXER em outras partes de admin.py — apenas a função grupo_membership_health (linha ~1133).
- NÃO MEXER em main.py além da linha 239 (BUILD_VERSION).
- Plans 11-01/02/03 NÃO TOCADOS por esta fase. Esta plan ASSUME que 11-01/02/03 já foram committados; este plan adiciona o deploy step.
</non_regression>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump BUILD_VERSION em app/main.py</name>
  <read_first>
    - leadflow-backend/app/main.py linhas 230-250 (área do BUILD_VERSION)
  </read_first>
  <behavior>
    - Linha 239 atual: BUILD_VERSION = "2026-06-09-grupo-membership-v2-fundacao"
    - Substituir por nova versão refletindo Fase 11
    - Formato: YYYY-MM-DD-feature-slug (mesma convenção do existente)
    - Data: usar data REAL do dia do deploy
  </behavior>
  <files>leadflow-backend/app/main.py</files>
  <action>
Editar app/main.py substituindo a linha 239.

ATUAL:
```
BUILD_VERSION = "2026-06-09-grupo-membership-v2-fundacao"
```

NOVO (substituir data pela data REAL do deploy se diferente):
```
BUILD_VERSION = "2026-06-10-lead-in-group-consumer"
```

Justificativa do slug:
- 2026-06-10: data do deploy (executor ajusta se data for outra)
- lead-in-group-consumer: identifica a feature principal da Fase 11 (Plan 11-01)
- Sintaxe consistente com BUILD anterior

NÃO mexer em mais nada no arquivo. Outras strings em main.py (ex: __version__, app description) NÃO tocar.
  </action>
  <verify>
    <automated>cd c:/Projetos/Leadflow && python -c "import sys; sys.path.insert(0, 'leadflow-backend'); from app.main import BUILD_VERSION; assert 'lead-in-group' in BUILD_VERSION; assert '2026-06' in BUILD_VERSION; print(f'OK: {BUILD_VERSION}')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^BUILD_VERSION = .2026-06.*lead-in-group" leadflow-backend/app/main.py` retorna match
    - `grep -c "BUILD_VERSION = " leadflow-backend/app/main.py` == 1 (não duplicado)
    - String não contém mais "v2-fundacao" (substituído)
    - Module importa sem erro
  </acceptance_criteria>
  <done>
    BUILD_VERSION reflete capability da Fase 11. Endpoint /version (FastAPI default route) servirá a nova string após redeploy.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Estender endpoint /admin/grupo-membership/health com lookup_stats e phase=11-consumer</name>
  <read_first>
    - leadflow-backend/app/routers/admin.py linhas 1125-1210 (endpoint healthcheck atual)
    - leadflow-backend/app/services/grupo_membership.py (_probe_locks dict — Plan 11-01)
  </read_first>
  <behavior>
    - Endpoint atual retorna: {total_rows, last_write_at, writes_by_source_24h, cache_stats, phase}
    - Adicionar campo lookup_stats com sub-campo lock_dict_size (len de _probe_locks)
    - Atualizar phase de "10-fundacao" para "11-consumer"
    - Try/except defensivo no acesso a _probe_locks (módulo pode dar erro de import em paths edge)
    - Manter resto do payload intacto (Fase 10 fields)
  </behavior>
  <files>leadflow-backend/app/routers/admin.py</files>
  <action>
Editar app/routers/admin.py na função grupo_membership_health (linha ~1133).

Substituir o bloco de inicialização de `out` (linhas ~1155-1161):

ATUAL:
```
    sb = get_supabase()
    out: dict = {
        "total_rows": 0,
        "last_write_at": None,
        "writes_by_source_24h": {},
        "cache_stats": probe_cache.stats(),
        "phase": "10-fundacao",
    }
```

SUBSTITUIR POR:
```
    sb = get_supabase()
    out: dict = {
        "total_rows": 0,
        "last_write_at": None,
        "writes_by_source_24h": {},
        "cache_stats": probe_cache.stats(),
        "lookup_stats": {
            "lock_dict_size": 0,
        },
        "phase": "11-consumer",
    }

    # FASE 11: tamanho do dict de locks (PROBE-COALESCE-01).
    # Monitora crescimento — em prod esperamos ~150 entries esteady-state
    # (30 empresas x 5 grupos ativos). Pitfall 5 do RESEARCH: se >2000,
    # profile pra cleanup ativo (improvavel em single-worker com restart
    # diario do Easypanel).
    try:
        from app.services.grupo_membership import _probe_locks
        out["lookup_stats"]["lock_dict_size"] = len(_probe_locks)
    except Exception as e:
        out["lookup_stats"]["lock_dict_size_erro"] = str(e)
```

NÃO mexer em outras partes da função:
- Os 3 blocos try/except que populam total_rows, last_write_at, writes_by_source_24h permanecem INTACTOS
- O return final permanece INTACTO
- NÃO tocar em comentários/docstrings adjacentes

Estado esperado após edição (linhas 1153-1190):
- Dict `out` agora inclui `lookup_stats` e `phase="11-consumer"`
- Novo bloco try/except popula `lock_dict_size`
- Resto da função (3 try/except + return) preservados literalmente
  </action>
  <verify>
    <automated>cd c:/Projetos/Leadflow/leadflow-backend && python -c "import inspect; from app.routers.admin import grupo_membership_health; src = inspect.getsource(grupo_membership_health); assert 'lookup_stats' in src; assert '11-consumer' in src; assert '_probe_locks' in src; print('OK: healthcheck estendido')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "lookup_stats" leadflow-backend/app/routers/admin.py` retorna match
    - `grep -n "11-consumer" leadflow-backend/app/routers/admin.py` retorna match
    - `grep -n "lock_dict_size" leadflow-backend/app/routers/admin.py` retorna match
    - `grep -n "_probe_locks" leadflow-backend/app/routers/admin.py` retorna match
    - `grep -c "10-fundacao" leadflow-backend/app/routers/admin.py` == 0 (substituído por 11-consumer)
    - Module importa sem erro
    - Campos antigos preservados: `grep -c "cache_stats" leadflow-backend/app/routers/admin.py` >= 1
    - Campos antigos preservados: `grep -c "writes_by_source_24h" leadflow-backend/app/routers/admin.py` >= 1
    - Campos antigos preservados: `grep -c "total_rows" leadflow-backend/app/routers/admin.py` >= 2
  </acceptance_criteria>
  <done>
    Endpoint /admin/grupo-membership/health agora retorna lookup_stats com lock_dict_size + phase=11-consumer. Fase 10 fields preservados. Module importa OK.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Local smoke test — full pytest + import test antes do commit</name>
  <read_first>
    - leadflow-backend/tests/ (lista de arquivos)
  </read_first>
  <files>leadflow-backend/tests/ (read-only — não modifica arquivos, só executa)</files>
  <action>
Rodar a suite pytest completa antes do commit + deploy. Garante que Plans 11-01/02/03 + mudanças do 11-04 não regrediram nada.

```bash
cd leadflow-backend && python -m pytest tests/ -x --tb=short 2>&1 | tail -30
```

Esperado: 0 falhas, 0 erros. Suite completa em < 30s.

Adicional — smoke import test:

```bash
cd leadflow-backend && python -c "
# 1. BUILD_VERSION bumped
from app.main import BUILD_VERSION
assert 'lead-in-group' in BUILD_VERSION, f'BUILD_VERSION not bumped: {BUILD_VERSION}'
print(f'  BUILD_VERSION = {BUILD_VERSION}')

# 2. lead_in_group exportado
from app.services.grupo_membership import (
    lead_in_group, _probe_locks, _probe_locks_dict_lock,
    _get_probe_lock, _make_probe_lock_key, _query_membership_row,
)
import asyncio
assert asyncio.iscoroutinefunction(lead_in_group)
assert isinstance(_probe_locks, dict)
print(f'  lead_in_group OK, _probe_locks size={len(_probe_locks)}')

# 3. grupo_fallback migrado
from app.services.grupo_fallback import (
    ativar_fallback_se_necessario,
    polling_grupos_aguardando,
)
print('  grupo_fallback funcoes OK')

# 4. healthcheck endpoint atualizado
import inspect
from app.routers.admin import grupo_membership_health
src = inspect.getsource(grupo_membership_health)
assert 'lookup_stats' in src
assert '11-consumer' in src
print('  healthcheck endpoint estendido OK')

print('TODOS OS SMOKES PASSARAM')
"
```

Se algum smoke falhar, parar e revisar tasks anteriores antes de prosseguir para checkpoint.
  </action>
  <verify>
    <automated>cd c:/Projetos/Leadflow/leadflow-backend && python -m pytest tests/ -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ -x` → 0 falhas, 0 erros
    - Smoke import test (acima) imprime "TODOS OS SMOKES PASSARAM"
    - BUILD_VERSION smoke confirma string nova
    - lead_in_group importa como coroutinefunction
    - grupo_fallback funções importam
    - healthcheck endpoint contém lookup_stats e 11-consumer
  </acceptance_criteria>
  <done>
    Suite pytest verde + 4 smokes import-level passam. Código pronto para commit + deploy.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Commit + push para GitHub</name>
  <read_first>
    - .git status para verificar arquivos modificados
  </read_first>
  <files>leadflow-backend/.git (commit + push — não modifica source)</files>
  <action>
Commitar todas as mudanças Fase 11 + push.

Comandos:

```bash
cd c:/Projetos/Leadflow/leadflow-backend
git status

# Stage apenas arquivos da Fase 11 (não usar git add -A — pode incluir arquivos sensíveis)
git add app/services/grupo_membership.py
git add app/services/grupo_fallback.py
git add app/main.py
git add app/routers/admin.py
git add tests/test_lead_in_group_decision.py
git add tests/test_lead_in_group_coalesce.py
git add tests/test_grupo_fallback_migration.py

git status  # confirma só os 7 arquivos acima staged

git commit -m "feat(phase-11): lead_in_group consumer + migrate 7 grupo_fallback callsites + asyncio.Lock coalescing

- app/services/grupo_membership.py: adiciona lead_in_group() async consumer com decision tree (cache fast-path -> grupo_membership query -> orphan check (caso Fernanda) -> probe Evolution com coalescing double-checked locking). PROBE-COALESCE-01 via dict per-chave _probe_locks + meta-lock.
- app/services/grupo_fallback.py: migra 7 callsites de verificar_lead_no_grupo (linhas 251, 311, 337, 477, 624, 1173, 1256) para lead_in_group. Trata source='orphan'/instance_key_match=False como ativacao de Phase 7 (caso Fernanda). PROBE-BYPASS-01 (marker GRUPO_LEAD_ENTROU_CRIACAO < 5min) e ALERTA-GRUPO-01 (skip envio config 08/06) PRESERVADOS literalmente.
- tests/test_lead_in_group_decision.py: 7 testes unit decision tree.
- tests/test_lead_in_group_coalesce.py: 3 testes integration coalescing async.
- tests/test_grupo_fallback_migration.py: 8 testes CI grep validando migracao mecanica e preservacao.
- app/main.py: bump BUILD_VERSION para 2026-06-XX-lead-in-group-consumer.
- app/routers/admin.py: endpoint /admin/grupo-membership/health estendido com lookup_stats.lock_dict_size + phase=11-consumer.

Requirements: MEMB-05, PROBE-COALESCE-01
Phase: 11 (v2.2 milestone)"

git push origin HEAD
```

Após push:
1. Verificar na UI do GitHub se commit aparece
2. Aguardar pipeline GitHub->Easypanel (memória `feedback_easypanel_rebuild_real.md`)
3. Em caso de pipeline não-disparar, investigar webhook GitHub->Easypanel (NÃO pedir user pra rebuildar manual — quebra padrão da memória)
  </action>
  <verify>
    <automated>cd c:/Projetos/Leadflow/leadflow-backend && git log -1 --oneline</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --oneline` mostra commit com "feat(phase-11)"
    - `git status` mostra working tree clean
    - `git log origin/main..HEAD` vazio (push completou)
    - Branch atual está pushado para o remote
  </acceptance_criteria>
  <done>
    Commit Fase 11 pushed para GitHub. Webhook Easypanel deve disparar build automatico.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: CHECKPOINT — User valida deploy + smoke em prod</name>
  <files>leadflow-backend/app/main.py, leadflow-backend/app/routers/admin.py (deploy verification — read-only)</files>
  <action>
**O que foi entregue** (Plans 11-01/02/03/04 implementados e committed):
- lead_in_group() consumer com decision tree completa em app/services/grupo_membership.py
- PROBE-COALESCE-01 via asyncio.Lock per-chave + double-checked locking
- 7 substituições mecânicas em app/services/grupo_fallback.py (PROBE-BYPASS-01 e ALERTA-GRUPO-01 preservados)
- 18 testes pytest novos (7 decision + 3 coalesce + 8 migration grep)
- BUILD_VERSION bumpado para 2026-06-XX-lead-in-group-consumer
- /admin/grupo-membership/health estendido com lookup_stats.lock_dict_size + phase=11-consumer

Commit pushed para GitHub. Pipeline Easypanel deve estar em build automático.

**Como verificar** (aguardar Easypanel terminar o build ~2-3 min, depois rodar smoke):

**1. Confirma BUILD_VERSION em prod:**
```bash
curl -s https://leadflow-backend.bqvcbz.easypanel.host/version
```
Esperado: JSON com `{"version": "2026-06-XX-lead-in-group-consumer", ...}` (ou similar — endpoint exato pode variar)

**2. Confirma healthcheck novo:**
```bash
curl -s https://leadflow-backend.bqvcbz.easypanel.host/admin/grupo-membership/health | python -m json.tool
```
Esperado JSON com:
- `"phase": "11-consumer"` (atualizado)
- `"lookup_stats": {"lock_dict_size": <int>}` (novo campo)
- Fase 10 fields preservados: `cache_stats`, `total_rows`, `last_write_at`, `writes_by_source_24h`

**3. Smoke comportamental — observar logs prod via SSH** (memoria reference_easypanel_logs.md):
```bash
# Em outro terminal — SSH no Hostinger VPS
docker logs 3e74f7da5a68 --tail 200 -f | grep -E "\[MEMB-LOOKUP\]|\[FALLBACK-1_1\]"
```

Esperado em prod (aguardar 5-10 min de tráfego natural):
- Logs `[MEMB-LOOKUP] empresa=... grupo=... source={cache|membership|probe} in_group=...` aparecem
- Logs `[FALLBACK-1_1] ... lead_in_group ...` (substituiu "Evolution check") aparecem
- `source=membership` em pelo menos 1 caso (lead que webhook ja escreveu — confirma fonte primaria invertida)
- `source=cache` em pelo menos 1 caso (cache hit pos primeira consulta)
- Sem stack traces relacionados a lead_in_group, _probe_locks, ou grupo_membership

**4. Smoke caso negativo — confirmar PROBE-BYPASS-01 preservado:**
Aguardar um caso novo de criação de grupo (lead novo qualificado). Confirmar nos logs:
- `[FALLBACK-1_1] ... FSM=ATIVO + marker GRUPO_LEAD_ENTROU_CRIACAO < 5min — bypass probe (NAO ativa)`
Esse log deve continuar aparecendo (bypass ainda ativo).

**Cenários de bloqueio (acionar redeploy revert via Easypanel):**
- `/version` ainda mostra `grupo-membership-v2-fundacao` após 10 min → build não pegou (investigar pipeline GitHub->Easypanel, NÃO pedir user pra rebuildar manual)
- Stack traces relacionados a lead_in_group / _probe_locks / grupo_membership aparecem
- Caso real de regressão (lead que entrou no grupo mas bot mandou DM convite por erro)

**Resume signal:** Type "approved" quando smoke validado, ou descrever o que falhou.
  </action>
  <verify>
    <human>
      User executa os 4 checks acima (curl /version, curl /health, observar logs MEMB-LOOKUP, observar bypass preservado) e responde "approved" ou descreve falha.
    </human>
  </verify>
  <acceptance_criteria>
    - `/version` em prod retorna string contendo "lead-in-group-consumer"
    - `/admin/grupo-membership/health` retorna `phase: "11-consumer"` + `lookup_stats.lock_dict_size` presente
    - Logs `[MEMB-LOOKUP]` aparecem em prod com pelo menos 1 caso de `source=membership` (confirma fonte primaria invertida)
    - Log `bypass probe (NAO ativa)` continua aparecendo (PROBE-BYPASS-01 preservado)
    - Sem stack traces relacionados a lead_in_group / _probe_locks / grupo_membership
    - User responde "approved" no resume signal
  </acceptance_criteria>
  <done>
    User confirmou smoke validation completo em prod. Fase 11 oficialmente shippada. Pronto para `/gsd-plan-phase 12` (Retry async + callers com margem).
  </done>
</task>

</tasks>

<verification>
1. `grep -n "lead-in-group-consumer" leadflow-backend/app/main.py` retorna match (BUILD_VERSION bumped)
2. `grep -n "11-consumer" leadflow-backend/app/routers/admin.py` retorna match (phase atualizada)
3. `grep -n "lookup_stats" leadflow-backend/app/routers/admin.py` retorna match
4. `grep -n "lock_dict_size" leadflow-backend/app/routers/admin.py` retorna match
5. `pytest leadflow-backend/tests/ -x` → 0 falhas
6. Smoke import test passa (todos os 4 checks)
7. Commit pushado para GitHub (`git log origin/main..HEAD` vazio)
8. Após checkpoint: curl /version retorna build novo + curl /admin/grupo-membership/health retorna lookup_stats + phase=11-consumer
9. Logs prod mostram [MEMB-LOOKUP] aparecendo em consultas reais
10. PROBE-BYPASS-01 ainda ativo (log "bypass probe (NAO ativa)" continua aparecendo em casos reais)
</verification>

<success_criteria>
- [ ] BUILD_VERSION em app/main.py reflete Fase 11 (string contém "lead-in-group")
- [ ] Endpoint /admin/grupo-membership/health tem campo `lookup_stats.lock_dict_size`
- [ ] Endpoint /admin/grupo-membership/health tem `phase: "11-consumer"`
- [ ] Fase 10 fields preservados no healthcheck (cache_stats, total_rows, last_write_at, writes_by_source_24h)
- [ ] Suite pytest completa green (0 falhas)
- [ ] Commit Fase 11 pushed para GitHub com mensagem detalhada
- [ ] CHECKPOINT user-verify: build em prod confirmado (curl /version retorna nova string)
- [ ] CHECKPOINT user-verify: healthcheck em prod retorna phase=11-consumer + lookup_stats
- [ ] CHECKPOINT user-verify: logs [MEMB-LOOKUP] aparecem em tráfego real
- [ ] CHECKPOINT user-verify: PROBE-BYPASS-01 ainda ativo (log bypass continua)
</success_criteria>

<output>
After completion, create `.planning/phases/11-lead-in-group-consumer-migrar-fallback-callers-coalescing-async/11-04-SUMMARY.md`
</output>
