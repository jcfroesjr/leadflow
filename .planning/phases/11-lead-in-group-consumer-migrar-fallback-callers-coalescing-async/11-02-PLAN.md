---
phase: 11-lead-in-group-consumer-migrar-fallback-callers-coalescing-async
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - leadflow-backend/app/services/grupo_fallback.py
autonomous: true
requirements:
  - MEMB-05

must_haves:
  truths:
    - "6 blocos lógicos em grupo_fallback.py chamam lead_in_group() em vez de verificar_lead_no_grupo direto (7 substituições mecânicas nas linhas atuais 251, 311, 337, 477, 624, 1173, 1256)"
    - "PROBE-BYPASS-01 (marker GRUPO_LEAD_ENTROU_CRIACAO < 5min em linhas atuais 296-308) PRESERVADO LITERALMENTE — lead_in_group SÓ é chamado se bypass não disparou"
    - "Caso Fernanda (source='orphan' ou instance_key_match=False) tratado em blocos 1b/1c → grupo órfão = não-confirmado_grupo_atual → ativa Phase 7"
    - "Probe direto via verificar_lead_no_grupo REMOVIDO dos 6 blocos lógicos migrados, mantido em admin endpoints + leads.py:123 + confirmacao_agendamento.py:291 + warmup_grupo.py:781"
    - "Substituição usa kwargs explícitos (sb=, empresa_id=, telefone=, grupo_jid=, lid=, evo_url=, evo_key=, evo_inst=) para evitar troca acidental telefone↔grupo_jid (Pitfall 2 do RESEARCH §13)"
  artifacts:
    - path: "leadflow-backend/app/services/grupo_fallback.py"
      provides: "Import lead_in_group + 7 substituições mecânicas"
      contains: "from app.services.grupo_membership import lead_in_group"
    - path: "leadflow-backend/app/services/grupo_fallback.py"
      provides: "Marker bypass linhas 296-308 PRESERVADO (PROBE-BYPASS-01)"
      contains: "GRUPO_LEAD_ENTROU_CRIACAO"
  key_links:
    - from: "grupo_fallback.py:ativar_fallback_se_necessario"
      to: "lead_in_group()"
      via: "blocos 1b (legacy markers) + 1c (FSM=ATIVO bypass) + 2 (informativo)"
      pattern: "await lead_in_group"
    - from: "grupo_fallback.py:_disparar_convite_lead_se_aguardando"
      to: "lead_in_group()"
      via: "probe live antes de mandar DM convite (60s delayed)"
      pattern: "await lead_in_group"
    - from: "grupo_fallback.py:_disparar_alerta_grupo_se_aguardando"
      to: "lead_in_group()"
      via: "probe live antes de sync FSM (180s delayed)"
      pattern: "await lead_in_group"
    - from: "grupo_fallback.py:polling_grupos_aguardando"
      to: "lead_in_group()"
      via: "2 branches (GRUPO_AGUARDANDO_ENTRADA + FALLBACK_1_1)"
      pattern: "await lead_in_group"
---

<objective>
Migrar os 6 blocos lógicos (7 chamadas mecânicas) de `verificar_lead_no_grupo` em `app/services/grupo_fallback.py` para usar `lead_in_group()` do Plan 11-01. Adicionar import. Preservar LITERALMENTE PROBE-BYPASS-01 (marker GRUPO_LEAD_ENTROU_CRIACAO < 5min) — ele continua ANTES do consumer ser chamado. Tratar `source='orphan'` como equivalente a "não está no grupo atual" (caso Fernanda).

Purpose: Fechar o loop webhook-first — callers internos de `grupo_fallback.py` deixam de bater HTTP Evolution toda vez e passam a consultar a tabela materializada PRIMEIRO. Resultado: cache hit O(1) em vez de 500ms HTTP; 3 jobs paralelos viram 1 chamada Evolution; tabela vira fonte primária da verdade.

Output: `grupo_fallback.py` com 1 import adicionado + 7 substituições mecânicas + tratamento `source='orphan'` em 2 blocos críticos (1b/1c). Os 4 callers FORA DO ESCOPO (admin.py, leads.py:123, confirmacao_agendamento.py:291, warmup_grupo.py:781) NÃO são tocados — `verificar_lead_no_grupo` continua importado e exportado para eles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-lead-in-group-consumer-migrar-fallback-callers-coalescing-async/11-RESEARCH.md
@.planning/phases/11-lead-in-group-consumer-migrar-fallback-callers-coalescing-async/11-01-PLAN.md
@.planning/phases/11-lead-in-group-consumer-migrar-fallback-callers-coalescing-async/11-01-SUMMARY.md
@leadflow-backend/CLAUDE.md
@leadflow-backend/app/services/grupo_fallback.py

<interfaces>
<!-- Contratos extraídos do Plan 11-01. Executor NÃO precisa explorar. -->

From app/services/grupo_membership.py (após Plan 11-01):
```python
async def lead_in_group(
    sb,
    empresa_id: str,
    telefone,                       # str ou None
    grupo_jid: str,
    lid: str = "",
    *,
    evo_url: str = "",
    evo_key: str = "",
    evo_inst: str = "",
    allow_probe_fallback: bool = True,
    schedule_retry_on_negative: bool = False,  # Stub Fase 12
) -> dict:
    """Retorna {in_group, source, instance_key_match, last_event_at}.

    source ∈ {'cache', 'membership', 'orphan', 'probe', 'unknown'}
    in_group ∈ {True, False, None}
    """
```

From app/services/evolution.py:198 (NÃO mexer — outros callers usam):
```python
async def verificar_lead_no_grupo(
    base_url: str, api_key: str, instancia: str,
    grupo_jid: str, telefone: str, lead_lid: str = "",
) -> dict:
    """Retorna {ok, in_group, erro}."""
```

From grupo_fallback.py (estado atual em prod — linhas chave):
- Linha 33: `from app.db.client import get_supabase` (já importado)
- Linha 34-39: `from app.services.evolution import (verificar_lead_no_grupo, enviar_mensagem, enviar_convite_grupo, obter_link_convite_grupo,)`
- `sb = get_supabase()` está disponível em todos os 7 callsites (caller context)
- `empresa_id` está disponível em todos os 7 callsites (argumento das funções pai)
</interfaces>

<non_regression>
PRESERVAR LITERALMENTE (NÃO MEXER):
- **PROBE-BYPASS-01**: linhas atuais 285-308 — bloco do marker `GRUPO_LEAD_ENTROU_CRIACAO < 5min`. Este check vem ANTES do `verificar_lead_no_grupo` call no bloco 1c. Após migração: bypass continua ANTES de `lead_in_group()`.
- **ALERTA-GRUPO-01**: linha atual 647 — `print(f"[ALERTA-DELAYED] skip envio alerta grupo (config 08/06 — convite 1-1 cobre)")` — fluxo continua skip alerta. Só substitui a chamada de probe da linha 624.
- **AQUEC-FLOOR-01**: arquivo `warmup_grupo.py` NÃO tocado nesta fase.
- **GRUPO-REUSO-01**: arquivo `leads.py` NÃO tocado nesta fase.
- **RECOVERY-STARTUP-01 + RECOVERY-AQUEC-01**: não impactados.
- Markers LEAD_LID, GRUPO_LEAD_ENTROU_CRIACAO, GRUPO_AGUARDANDO_ENTRADA, CONVITE_1_1_ENVIADO, DM_CONVITE_TEXTO_ENVIADO — TODOS PRESERVADOS.

Imports:
- Linha 34-39 `from app.services.evolution import (verificar_lead_no_grupo, ...)` PRESERVADA (outros funções como `enviar_mensagem`, `enviar_convite_grupo`, `obter_link_convite_grupo` continuam usadas).
- Imports locais `from app.services.evolution import verificar_lead_no_grupo` em linhas 475 e 622: REMOVER (substituídos por lead_in_group).

Callsites NÃO migrar:
- `admin.py:769` + `admin.py:981` (endpoints diagnóstico — quer fresh probe)
- `leads.py:123` (GRUPO-REUSO-01)
- `confirmacao_agendamento.py:291` (Fase 12)
- `warmup_grupo.py:781` (Fase 12)
- `tests/test_verificar_lead_no_grupo_phase3.py` (testes v2.1)
</non_regression>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adicionar import lead_in_group + migrar blocos 1b/1c/2 (linhas atuais 251, 311, 337) em ativar_fallback_se_necessario</name>
  <read_first>
    - leadflow-backend/app/services/grupo_fallback.py linhas 1-40 (imports + topo)
    - leadflow-backend/app/services/grupo_fallback.py linhas 230-345 (blocos 1b, 1c, 2)
    - .planning/phases/11-lead-in-group-consumer-migrar-fallback-callers-coalescing-async/11-RESEARCH.md §7 (linhas 573-642 — padrão de substituição)
    - .planning/phases/11-lead-in-group-consumer-migrar-fallback-callers-coalescing-async/11-RESEARCH.md §1 (linhas 220-245 — tabela callsites)
  </read_first>
  <behavior>
    - Import `lead_in_group` adicionado depois da linha de import multi-line de `evolution` (~linha 40)
    - Bloco 1b (linha atual 251): substitui `verificar_lead_no_grupo` por `lead_in_group`, mantém try/except externo, trata `source='orphan'` como `_confirmado_grupo_atual = False`
    - Bloco 1c (linha atual 311): substitui `verificar_lead_no_grupo` por `lead_in_group`. PRESERVA marker bypass GRUPO_LEAD_ENTROU_CRIACAO (linhas 285-308) ANTES do call. Trata orphan como conservador (ativa fallback).
    - Bloco 2 (linha atual 337): substitui `verificar_lead_no_grupo` por `lead_in_group`. É só log informativo — não muda fluxo de decisão.
    - PROBE-BYPASS-01 (linhas 285-308) PRESERVADO LITERALMENTE — `lead_in_group` SÓ é chamado se bypass não dispara `return False`.
  </behavior>
  <files>leadflow-backend/app/services/grupo_fallback.py</files>
  <action>
**Mudança 1 — Import** (após linha 39, antes da linha 41 vazia):

ADICIONAR exatamente esta linha após a linha 39 (fim do import multi-line de `app.services.evolution`):

```python
from app.services.grupo_membership import lead_in_group
```

Estado esperado linhas 33-41:
```python
from app.db.client import get_supabase
from app.services.evolution import (
    verificar_lead_no_grupo,
    enviar_mensagem,
    enviar_convite_grupo,
    obter_link_convite_grupo,
)
from app.services.grupo_membership import lead_in_group
```

NÃO REMOVER `verificar_lead_no_grupo` do import — outros funções no mesmo arquivo podem precisar (e o linter vai reclamar).

**Mudança 2 — Bloco 1b** (linha atual 249-257):

ATUAL:
```python
            try:
                _lid_leg = _get_lead_lid_for_group(sb, empresa_id, telefone, grupo_jid)
                _check_leg = await verificar_lead_no_grupo(
                    evo_url, evo_key, evo_inst, grupo_jid, telefone, lead_lid=_lid_leg,
                )
                _confirmado_grupo_atual = _check_leg.get("in_group") is True
                print(f"[FALLBACK-1_1] {telefone} tem markers legados — Evolution in_group={_check_leg.get('in_group')!r} lid={_lid_leg!r}")
            except Exception as _e_chk_leg:
                print(f"[FALLBACK-1_1] erro check Evolution apos legacy: {_e_chk_leg} — ativa Phase 7 (conservador)")
```

SUBSTITUIR POR (Fase 11 — usa lead_in_group + trata orphan):
```python
            try:
                _lid_leg = _get_lead_lid_for_group(sb, empresa_id, telefone, grupo_jid)
                # FASE 11 (2026-06-09): lead_in_group consulta grupo_membership
                # PRIMEIRO; probe Evolution so se row ausente. Sentinel 'orphan'
                # (instance_key mismatch — caso Fernanda) tratado como
                # nao-confirmado_grupo_atual.
                _check_leg = await lead_in_group(
                    sb=sb, empresa_id=empresa_id,
                    telefone=telefone, grupo_jid=grupo_jid, lid=_lid_leg,
                    evo_url=evo_url, evo_key=evo_key, evo_inst=evo_inst,
                )
                _src_leg = _check_leg.get("source")
                if _src_leg == 'orphan' or _check_leg.get('instance_key_match') is False:
                    _confirmado_grupo_atual = False
                    print(f"[FALLBACK-1_1] {telefone} ORPHAN grupo={grupo_jid[:24]} (instance_key mismatch — caso Fernanda) — markers legados sao de instancia antiga")
                else:
                    _confirmado_grupo_atual = _check_leg.get("in_group") is True
                    print(f"[FALLBACK-1_1] {telefone} tem markers legados — lead_in_group in_group={_check_leg.get('in_group')!r} source={_src_leg} lid={_lid_leg!r}")
            except Exception as _e_chk_leg:
                print(f"[FALLBACK-1_1] erro check lead_in_group apos legacy: {_e_chk_leg} — ativa Phase 7 (conservador)")
```

**Mudança 3 — Bloco 1c (PRESERVA bypass — linhas 285-322)**:

A região 285-322 atualmente:
```python
    try:
        from app.services.grupo_state import get_grupo_state, STATE_ATIVO
        _ag_top = _buscar_proximo_agendamento(sb, empresa_id, telefone)
        if _ag_top.get("id"):
            _st_top = get_grupo_state(sb, empresa_id, _ag_top["id"])
            if _st_top == STATE_ATIVO:
                # FIX 01/06 (caso Ana Carla): probe Evolution dá falso negativo
                # nos primeiros 60-120s após criar grupo (propagação interna do
                # WhatsApp). Se temos marker GRUPO_LEAD_ENTROU_CRIACAO recente
                # (< 5min), confia em promote_admin sucesso e bypassa probe.
                try:
                    from datetime import datetime as _dt_re, timedelta as _td_re
                    _cutoff_recente = (_dt_re.utcnow() - _td_re(minutes=5)).isoformat()
                    _mk_recente = sb.table("conversas").select("criado_em") \
                        .eq("empresa_id", empresa_id) \
                        .eq("telefone", telefone) \
                        .eq("role", "sistema") \
                        .eq("conteudo", f"GRUPO_LEAD_ENTROU_CRIACAO:{grupo_jid}") \
                        .gt("criado_em", _cutoff_recente).limit(1).execute()
                    if _mk_recente.data:
                        print(f"[FALLBACK-1_1] {telefone} FSM=ATIVO + marker GRUPO_LEAD_ENTROU_CRIACAO < 5min — bypass probe (NAO ativa)")
                        return False
                except Exception as _e_mk:
                    print(f"[FALLBACK-1_1] erro check marker recente: {_e_mk}")
                try:
                    _lid_top = _get_lead_lid_for_group(sb, empresa_id, telefone, grupo_jid)
                    _probe_top = await verificar_lead_no_grupo(
                        evo_url, evo_key, evo_inst, grupo_jid, telefone, lead_lid=_lid_top,
                    )
                    if _probe_top.get("in_group") is True:
                        print(f"[FALLBACK-1_1] {telefone} FSM=ATIVO + probe confirma in_group=True — NAO ativa")
                        return False
                    print(f"[FALLBACK-1_1] {telefone} FSM=ATIVO MAS probe={_probe_top.get('in_group')!r} lid={_lid_top!r} — FSM herdado errado, ativa fallback (conservador)")
                except Exception as _e_pt:
                    print(f"[FALLBACK-1_1] erro probe live com FSM=ATIVO: {_e_pt} — segue conservador (ativa fallback)")
    except Exception as _e_st_top:
        print(f"[FALLBACK-1_1] erro check FSM top: {_e_st_top}")
```

PRESERVAR LITERALMENTE o bloco do bypass (linhas 285-308 — todo o `try` interno com `_mk_recente`). NÃO alterar essas linhas.

SUBSTITUIR APENAS o bloco try interno que faz o probe (linhas 309-319):

ATUAL:
```python
                try:
                    _lid_top = _get_lead_lid_for_group(sb, empresa_id, telefone, grupo_jid)
                    _probe_top = await verificar_lead_no_grupo(
                        evo_url, evo_key, evo_inst, grupo_jid, telefone, lead_lid=_lid_top,
                    )
                    if _probe_top.get("in_group") is True:
                        print(f"[FALLBACK-1_1] {telefone} FSM=ATIVO + probe confirma in_group=True — NAO ativa")
                        return False
                    print(f"[FALLBACK-1_1] {telefone} FSM=ATIVO MAS probe={_probe_top.get('in_group')!r} lid={_lid_top!r} — FSM herdado errado, ativa fallback (conservador)")
                except Exception as _e_pt:
                    print(f"[FALLBACK-1_1] erro probe live com FSM=ATIVO: {_e_pt} — segue conservador (ativa fallback)")
```

SUBSTITUIR POR:
```python
                # FASE 11 (2026-06-09): probe via lead_in_group (consulta
                # grupo_membership PRIMEIRO). Bypass PROBE-BYPASS-01 acima
                # NAO foi alterado — continua ANTES deste call.
                try:
                    _lid_top = _get_lead_lid_for_group(sb, empresa_id, telefone, grupo_jid)
                    _probe_top = await lead_in_group(
                        sb=sb, empresa_id=empresa_id,
                        telefone=telefone, grupo_jid=grupo_jid, lid=_lid_top,
                        evo_url=evo_url, evo_key=evo_key, evo_inst=evo_inst,
                    )
                    _src_top = _probe_top.get("source")
                    # Caso Fernanda: orphan grupo (instance_key mismatch).
                    # FSM=ATIVO mas grupo e de instancia antiga — ativa fallback
                    # (conservador, vai criar novo grupo via Phase 7).
                    if _src_top == 'orphan' or _probe_top.get('instance_key_match') is False:
                        print(f"[FALLBACK-1_1] {telefone} FSM=ATIVO + ORPHAN grupo={grupo_jid[:24]} (caso Fernanda) — ativa fallback (cria novo grupo)")
                    elif _probe_top.get("in_group") is True:
                        print(f"[FALLBACK-1_1] {telefone} FSM=ATIVO + lead_in_group confirma in_group=True source={_src_top} — NAO ativa")
                        return False
                    else:
                        print(f"[FALLBACK-1_1] {telefone} FSM=ATIVO MAS lead_in_group in_group={_probe_top.get('in_group')!r} source={_src_top} lid={_lid_top!r} — FSM herdado errado, ativa fallback (conservador)")
                except Exception as _e_pt:
                    print(f"[FALLBACK-1_1] erro lead_in_group com FSM=ATIVO: {_e_pt} — segue conservador (ativa fallback)")
```

**Mudança 4 — Bloco 2 (linhas atuais 335-342, informativo)**:

ATUAL:
```python
    try:
        _lid_inf = _get_lead_lid_for_group(sb, empresa_id, telefone, grupo_jid)
        check = await verificar_lead_no_grupo(
            evo_url, evo_key, evo_inst, grupo_jid, telefone, lead_lid=_lid_inf,
        )
        print(f"[FALLBACK-1_1] {telefone} evolution check (informativo): in_group={check.get('in_group')!r} lid={_lid_inf!r}")
    except Exception as _e_chk:
        print(f"[FALLBACK-1_1] {telefone} erro evolution check (ignorado): {_e_chk}")
```

SUBSTITUIR POR:
```python
    try:
        _lid_inf = _get_lead_lid_for_group(sb, empresa_id, telefone, grupo_jid)
        # FASE 11 (2026-06-09): substituido verificar_lead_no_grupo por
        # lead_in_group (consulta grupo_membership PRIMEIRO). Decisao continua
        # SEMPRE ativa Phase 7 (estrategia conservadora) — esta chamada e
        # so informativa.
        check = await lead_in_group(
            sb=sb, empresa_id=empresa_id,
            telefone=telefone, grupo_jid=grupo_jid, lid=_lid_inf,
            evo_url=evo_url, evo_key=evo_key, evo_inst=evo_inst,
        )
        print(f"[FALLBACK-1_1] {telefone} lead_in_group check (informativo): in_group={check.get('in_group')!r} source={check.get('source')} lid={_lid_inf!r}")
    except Exception as _e_chk:
        print(f"[FALLBACK-1_1] {telefone} erro lead_in_group check (ignorado): {_e_chk}")
```
  </action>
  <verify>
    <automated>cd c:/Projetos/Leadflow/leadflow-backend && python -c "from app.services.grupo_fallback import ativar_fallback_se_necessario; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^from app\.services\.grupo_membership import lead_in_group$" leadflow-backend/app/services/grupo_fallback.py` retorna match
    - `grep -c "await lead_in_group" leadflow-backend/app/services/grupo_fallback.py` >= 3 (3 substituições nesta task: blocos 1b, 1c, 2)
    - `grep -c "await verificar_lead_no_grupo" leadflow-backend/app/services/grupo_fallback.py` <= 4 (4 callsites restantes: 477, 624, 1173, 1256 — migrados em Tasks 2 e 3 deste plan)
    - `grep -n "GRUPO_LEAD_ENTROU_CRIACAO" leadflow-backend/app/services/grupo_fallback.py` retorna match (bypass PRESERVADO — linha atual ~302)
    - `grep -n "bypass probe (NAO ativa)" leadflow-backend/app/services/grupo_fallback.py` retorna match (log do bypass preservado)
    - `grep -n "caso Fernanda" leadflow-backend/app/services/grupo_fallback.py` retorna match (tratamento orphan adicionado)
    - `grep -n "instance_key_match" leadflow-backend/app/services/grupo_fallback.py` retorna >= 2 matches (blocos 1b e 1c tratam)
    - Import original preservado: `grep -nE "^    verificar_lead_no_grupo," leadflow-backend/app/services/grupo_fallback.py` retorna match
    - Module importa sem erro
  </acceptance_criteria>
  <done>
    Blocos 1b/1c/2 da função `ativar_fallback_se_necessario` migrados para usar `lead_in_group`. Marker bypass PROBE-BYPASS-01 (linhas 285-308) PRESERVADO. Sentinel `source='orphan'` tratado em blocos 1b/1c → ativa Phase 7 (caso Fernanda). Bloco 2 mantém só-informativo. Import adicionado. 3/7 substituições feitas.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrar callsites em delayed jobs (linhas atuais 477 em _disparar_convite_lead_se_aguardando + 624 em _disparar_alerta_grupo_se_aguardando)</name>
  <read_first>
    - leadflow-backend/app/services/grupo_fallback.py linhas 460-490 (callsite 477)
    - leadflow-backend/app/services/grupo_fallback.py linhas 615-650 (callsite 624)
    - .planning/phases/11-lead-in-group-consumer-migrar-fallback-callers-coalescing-async/11-RESEARCH.md §7 (Por callsite — observações específicas)
  </read_first>
  <behavior>
    - Callsite 477 (_disparar_convite_lead_se_aguardando — Bug Vanusa fix): tem import local linha 475 `from app.services.evolution import verificar_lead_no_grupo`. REMOVER esse import local + substituir probe por lead_in_group.
    - Callsite 624 (_disparar_alerta_grupo_se_aguardando — FIX Luciane): tem import local linha 622. REMOVER + substituir.
    - Ambos preservam o fluxo `processar_entrada_lead_no_grupo` quando `in_group=True` (sync FSM).
    - ALERTA-GRUPO-01: fluxo continua skip envio do alerta (linha 647) — só substitui o probe.
    - Tratamento orphan: nesses 2 callsites, orphan grupo (instance_key mismatch) → log + continua envio (conservador, mesmo comportamento do in_group=False atual)
  </behavior>
  <files>leadflow-backend/app/services/grupo_fallback.py</files>
  <action>
**Mudança 1 — Callsite linha atual 474-488** (`_disparar_convite_lead_se_aguardando`):

ATUAL:
```python
    try:
        from app.services.evolution import verificar_lead_no_grupo
        _lid_cv = _get_lead_lid_for_group(sb, empresa_id, telefone, grupo_jid)
        _check_live = await verificar_lead_no_grupo(
            evo_url, evo_key, evo_inst, grupo_jid, telefone, lead_lid=_lid_cv,
        )
        if _check_live.get("in_group") is True:
            print(f"[CONVITE-DELAYED] {telefone} ja esta no grupo (Evolution live, lid={_lid_cv!r}) — dispara entrada tardia e pula DM")
            try:
                await processar_entrada_lead_no_grupo(empresa_id, telefone, grupo_jid)
            except Exception as _e_pe:
                print(f"[CONVITE-DELAYED] erro processar entrada tardia: {_e_pe}")
            return
    except Exception as _e_lv:
        print(f"[CONVITE-DELAYED] erro check live Evolution: {_e_lv} — continua envio (defensivo)")
```

SUBSTITUIR POR:
```python
    try:
        # FASE 11 (2026-06-09): substituido verificar_lead_no_grupo por
        # lead_in_group (consulta grupo_membership PRIMEIRO). Import local
        # `from app.services.evolution import verificar_lead_no_grupo`
        # REMOVIDO — lead_in_group ja chama o probe internamente como fallback.
        _lid_cv = _get_lead_lid_for_group(sb, empresa_id, telefone, grupo_jid)
        _check_live = await lead_in_group(
            sb=sb, empresa_id=empresa_id,
            telefone=telefone, grupo_jid=grupo_jid, lid=_lid_cv,
            evo_url=evo_url, evo_key=evo_key, evo_inst=evo_inst,
        )
        _src_cv = _check_live.get("source")
        if _src_cv == 'orphan' or _check_live.get('instance_key_match') is False:
            # Grupo orfao — lead nao esta no grupo ATUAL. Continua envio
            # convite/DM (conservador).
            print(f"[CONVITE-DELAYED] {telefone} ORPHAN grupo={grupo_jid[:24]} (caso Fernanda) — continua envio")
        elif _check_live.get("in_group") is True:
            print(f"[CONVITE-DELAYED] {telefone} ja esta no grupo (lead_in_group source={_src_cv}, lid={_lid_cv!r}) — dispara entrada tardia e pula DM")
            try:
                await processar_entrada_lead_no_grupo(empresa_id, telefone, grupo_jid)
            except Exception as _e_pe:
                print(f"[CONVITE-DELAYED] erro processar entrada tardia: {_e_pe}")
            return
    except Exception as _e_lv:
        print(f"[CONVITE-DELAYED] erro lead_in_group live check: {_e_lv} — continua envio (defensivo)")
```

**Mudança 2 — Callsite linha atual 621-640** (`_disparar_alerta_grupo_se_aguardando`):

ATUAL:
```python
        try:
            from app.services.evolution import verificar_lead_no_grupo
            _lid_al = _get_lead_lid_for_group(sb, empresa_id, telefone, grupo_jid)
            _check_live = await verificar_lead_no_grupo(
                evo_url, evo_key, evo_inst, grupo_jid, telefone, lead_lid=_lid_al,
            )
            if _check_live.get("in_group") is True:
                # Lead confirmado dentro (numero limpo OU @lid conhecido).
                # Aproveita pra sincronizar FSM via processar_entrada_lead_no_grupo
                # (idempotente).
                print(f"[ALERTA-DELAYED] {telefone} live confirma in_group=True (lid={_lid_al!r}) — pula alerta + sincroniza FSM")
                try:
                    await processar_entrada_lead_no_grupo(empresa_id, telefone, grupo_jid)
                except Exception as _e_pe:
                    print(f"[ALERTA-DELAYED] erro sync FSM apos live True: {_e_pe}")
                return
            # in_group=False ou None -> envia alerta (conservador)
            print(f"[ALERTA-DELAYED] {telefone} live in_group={_check_live.get('in_group')!r} — continua envio")
        except Exception as _e_lv:
            print(f"[ALERTA-DELAYED] erro check live: {_e_lv} — continua envio defensivo")
```

SUBSTITUIR POR:
```python
        try:
            # FASE 11 (2026-06-09): substituido verificar_lead_no_grupo por
            # lead_in_group (consulta grupo_membership PRIMEIRO). Import local
            # `from app.services.evolution import verificar_lead_no_grupo`
            # REMOVIDO — lead_in_group ja chama probe internamente como fallback.
            _lid_al = _get_lead_lid_for_group(sb, empresa_id, telefone, grupo_jid)
            _check_live = await lead_in_group(
                sb=sb, empresa_id=empresa_id,
                telefone=telefone, grupo_jid=grupo_jid, lid=_lid_al,
                evo_url=evo_url, evo_key=evo_key, evo_inst=evo_inst,
            )
            _src_al = _check_live.get("source")
            if _src_al == 'orphan' or _check_live.get('instance_key_match') is False:
                # Grupo orfao — segue fluxo padrao (skip envio alerta via
                # ALERTA-GRUPO-01 fix 08/06 abaixo).
                print(f"[ALERTA-DELAYED] {telefone} ORPHAN grupo={grupo_jid[:24]} (caso Fernanda) — continua fluxo (skip envio cobre)")
            elif _check_live.get("in_group") is True:
                # Lead confirmado dentro (numero limpo OU @lid conhecido).
                # Aproveita pra sincronizar FSM via processar_entrada_lead_no_grupo
                # (idempotente).
                print(f"[ALERTA-DELAYED] {telefone} lead_in_group confirma in_group=True source={_src_al} (lid={_lid_al!r}) — pula alerta + sincroniza FSM")
                try:
                    await processar_entrada_lead_no_grupo(empresa_id, telefone, grupo_jid)
                except Exception as _e_pe:
                    print(f"[ALERTA-DELAYED] erro sync FSM apos live True: {_e_pe}")
                return
            # in_group=False ou None -> segue fluxo (skip envio via ALERTA-GRUPO-01)
            print(f"[ALERTA-DELAYED] {telefone} lead_in_group in_group={_check_live.get('in_group')!r} source={_src_al} — continua fluxo")
        except Exception as _e_lv:
            print(f"[ALERTA-DELAYED] erro lead_in_group live check: {_e_lv} — continua fluxo defensivo")
```

Importante:
- Imports locais (linhas 475 e 622) REMOVIDOS — `lead_in_group` já é importado no topo do arquivo (Task 1).
- ALERTA-GRUPO-01 fix preservado: linha 647 `print(f"[ALERTA-DELAYED] {telefone} skip envio alerta grupo ...")` continua intacta logo abaixo.
- Tratamento orphan apenas LOGA — não muda decisão (continua fluxo defensivo).
  </action>
  <verify>
    <automated>cd c:/Projetos/Leadflow/leadflow-backend && python -c "from app.services.grupo_fallback import _disparar_convite_lead_se_aguardando, _disparar_alerta_grupo_se_aguardando; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "await lead_in_group" leadflow-backend/app/services/grupo_fallback.py` >= 5 (3 da Task 1 + 2 desta task)
    - `grep -c "await verificar_lead_no_grupo" leadflow-backend/app/services/grupo_fallback.py` <= 2 (apenas 1173, 1256 restantes para Task 3)
    - Imports locais REMOVIDOS: `grep -c "from app.services.evolution import verificar_lead_no_grupo" leadflow-backend/app/services/grupo_fallback.py` == 0
    - `grep -n "FASE 11" leadflow-backend/app/services/grupo_fallback.py` retorna >= 5 matches (mudanças comentadas)
    - ALERTA-GRUPO-01 preservado: `grep -n "skip envio alerta grupo (config 08/06" leadflow-backend/app/services/grupo_fallback.py` retorna match
    - Função `processar_entrada_lead_no_grupo` ainda chamada em ambos os callsites: `grep -c "await processar_entrada_lead_no_grupo" leadflow-backend/app/services/grupo_fallback.py` >= 2
  </acceptance_criteria>
  <done>
    Callsites 477 + 624 migrados para `lead_in_group`. Imports locais redundantes removidos. Tratamento orphan adicionado em ambos (continua fluxo defensivo). ALERTA-GRUPO-01 fix preservado. 5/7 substituições feitas.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrar callsites do polling job (linhas atuais 1173 + 1256 em polling_grupos_aguardando)</name>
  <read_first>
    - leadflow-backend/app/services/grupo_fallback.py linhas 1140-1185 (branch GRUPO_AGUARDANDO_ENTRADA — linha 1173)
    - leadflow-backend/app/services/grupo_fallback.py linhas 1240-1268 (branch FALLBACK_1_1 — linha 1256)
    - .planning/phases/11-lead-in-group-consumer-migrar-fallback-callers-coalescing-async/11-RESEARCH.md §1 nota sobre cfg vs evo_url
  </read_first>
  <behavior>
    - Callsite 1173 (branch GRUPO_AGUARDANDO_ENTRADA): usa `cfg["url"]/cfg["key"]/cfg["inst"]` (dict de empresa) em vez de `evo_url/evo_key/evo_inst` locais. Adaptar nome.
    - Callsite 1256 (branch FALLBACK_1_1 FIX 26/05): mesmo padrão `cfg` mas com variável `tel` em vez de `telefone`. Adaptar.
    - Polling é loop — coalescing ajuda menos (1 job só), mas mantém consistência arquitetural.
    - Tratamento orphan: polling pula promoção a ATIVO (deixa em FALLBACK_1_1; Phase 13 LEAVE handler limpa).
  </behavior>
  <files>leadflow-backend/app/services/grupo_fallback.py</files>
  <action>
**Mudança 1 — Callsite linha atual 1171-1181** (branch GRUPO_AGUARDANDO_ENTRADA):

ATUAL:
```python
            try:
                _lid_pl = _get_lead_lid_for_group(sb, empresa_id, telefone, grupo_jid)
                check = await verificar_lead_no_grupo(
                    cfg["url"], cfg["key"], cfg["inst"], grupo_jid, telefone, lead_lid=_lid_pl,
                )
                if check.get("in_group") is True:
                    stats["entraram"] += 1
                    await processar_entrada_lead_no_grupo(empresa_id, telefone, grupo_jid)
            except Exception as e:
                stats["erros"] += 1
                print(f"[POLLING-GRUPO] erro verificando {telefone} {grupo_jid}: {e}")
```

SUBSTITUIR POR:
```python
            try:
                _lid_pl = _get_lead_lid_for_group(sb, empresa_id, telefone, grupo_jid)
                # FASE 11 (2026-06-09): substituido verificar_lead_no_grupo por
                # lead_in_group (consulta grupo_membership PRIMEIRO).
                # cfg vem do dict empresas_cache (instance key especifica).
                check = await lead_in_group(
                    sb=sb, empresa_id=empresa_id,
                    telefone=telefone, grupo_jid=grupo_jid, lid=_lid_pl,
                    evo_url=cfg["url"], evo_key=cfg["key"], evo_inst=cfg["inst"],
                )
                _src_pl = check.get("source")
                if _src_pl == 'orphan' or check.get('instance_key_match') is False:
                    # Grupo orfao — NAO promove ATIVO. Deixa em
                    # GRUPO_AGUARDANDO_ENTRADA; Phase 13 LEAVE handler limpa.
                    print(f"[POLLING-GRUPO] {telefone} ORPHAN grupo={grupo_jid[:24]} (caso Fernanda) — NAO promove ATIVO")
                elif check.get("in_group") is True:
                    stats["entraram"] += 1
                    await processar_entrada_lead_no_grupo(empresa_id, telefone, grupo_jid)
            except Exception as e:
                stats["erros"] += 1
                print(f"[POLLING-GRUPO] erro verificando {telefone} {grupo_jid}: {e}")
```

**Mudança 2 — Callsite linha atual 1254-1262** (branch FALLBACK_1_1 FIX 26/05):

ATUAL:
```python
                verificados_fb += 1
                _lid_fb = _get_lead_lid_for_group(sb, emp_id, tel, grupo_jid)
                check = await verificar_lead_no_grupo(
                    cfg["url"], cfg["key"], cfg["inst"], grupo_jid, tel, lead_lid=_lid_fb,
                )
                if check.get("in_group") is True:
                    print(f"[POLLING-GRUPO] FALLBACK_1_1 → ATIVO: {tel} grupo={grupo_jid[:24]}... (entrou depois)")
                    await processar_entrada_lead_no_grupo(emp_id, tel, grupo_jid)
                    promovidos_fb += 1
```

SUBSTITUIR POR:
```python
                verificados_fb += 1
                _lid_fb = _get_lead_lid_for_group(sb, emp_id, tel, grupo_jid)
                # FASE 11 (2026-06-09): substituido verificar_lead_no_grupo por
                # lead_in_group (consulta grupo_membership PRIMEIRO).
                # Note variaveis: emp_id (vs empresa_id) e tel (vs telefone) por
                # convencao do loop FALLBACK desta funcao.
                check = await lead_in_group(
                    sb=sb, empresa_id=emp_id,
                    telefone=tel, grupo_jid=grupo_jid, lid=_lid_fb,
                    evo_url=cfg["url"], evo_key=cfg["key"], evo_inst=cfg["inst"],
                )
                _src_fb = check.get("source")
                if _src_fb == 'orphan' or check.get('instance_key_match') is False:
                    # Grupo orfao — NAO promove ATIVO. Deixa em FALLBACK_1_1;
                    # Phase 13 LEAVE handler limpa.
                    print(f"[POLLING-GRUPO] FALLBACK_1_1 {tel} ORPHAN grupo={grupo_jid[:24]} (caso Fernanda) — NAO promove ATIVO")
                elif check.get("in_group") is True:
                    print(f"[POLLING-GRUPO] FALLBACK_1_1 → ATIVO: {tel} grupo={grupo_jid[:24]}... source={_src_fb} (entrou depois)")
                    await processar_entrada_lead_no_grupo(emp_id, tel, grupo_jid)
                    promovidos_fb += 1
```

Importante:
- Variáveis do callsite 1256 SÃO diferentes (`emp_id` vs `empresa_id`, `tel` vs `telefone`). NÃO normalizar — manter convenção local do loop.
- Polling continua usando `cfg["url"]/cfg["key"]/cfg["inst"]` — esses passam como `evo_url=`/`evo_key=`/`evo_inst=` (kwargs explícitos).
  </action>
  <verify>
    <automated>cd c:/Projetos/Leadflow/leadflow-backend && python -c "from app.services.grupo_fallback import polling_grupos_aguardando; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "await lead_in_group" leadflow-backend/app/services/grupo_fallback.py` >= 7 (7 substituições totais nos 6 blocos lógicos)
    - `grep -c "await verificar_lead_no_grupo" leadflow-backend/app/services/grupo_fallback.py` == 0 (TODAS as 7 substituições feitas)
    - Variáveis preservadas: `grep -n "emp_id=emp_id" leadflow-backend/app/services/grupo_fallback.py` retorna match (callsite 1256)
    - Variáveis preservadas: `grep -n "telefone=tel," leadflow-backend/app/services/grupo_fallback.py` retorna match (callsite 1256)
    - `grep -c "FASE 11" leadflow-backend/app/services/grupo_fallback.py` >= 7 (todos os 7 callsites comentados)
    - `grep -c "ORPHAN" leadflow-backend/app/services/grupo_fallback.py` >= 5 (tratamento orphan em 5+ callsites: 1b, 1c, convite, alerta, polling AGUARDANDO, polling FALLBACK)
    - Module importa sem erro
  </acceptance_criteria>
  <done>
    Callsites 1173 + 1256 migrados para `lead_in_group`. Variáveis `emp_id`/`tel` preservadas no FALLBACK loop. Polling NÃO promove ATIVO em caso de orphan (deixa pra Phase 13). 7/7 substituições mecânicas feitas.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Orthogonality validation final — confirma que Plan 11-02 não regrediu NÃO REGREDIR list</name>
  <read_first>
    - leadflow-backend/app/services/grupo_fallback.py (estado APÓS Tasks 1+2+3)
  </read_first>
  <action>
Validação consolidada por grep de NÃO-REGRESSÃO + cobertura de migração. Esta task NÃO modifica arquivo — só executa checks.

```bash
# === Migração completa (7 substituições) ===
grep -c "await lead_in_group" leadflow-backend/app/services/grupo_fallback.py  # expect: 7
grep -c "await verificar_lead_no_grupo" leadflow-backend/app/services/grupo_fallback.py  # expect: 0

# === Import preservado para outras funções no arquivo ===
grep -nE "^    verificar_lead_no_grupo," leadflow-backend/app/services/grupo_fallback.py  # expect: match (linha ~35)
grep -nE "^from app\.services\.grupo_membership import lead_in_group$" leadflow-backend/app/services/grupo_fallback.py  # expect: match (linha ~40)

# === PROBE-BYPASS-01 PRESERVADO LITERALMENTE ===
grep -n "GRUPO_LEAD_ENTROU_CRIACAO" leadflow-backend/app/services/grupo_fallback.py  # expect: match
grep -n "bypass probe (NAO ativa)" leadflow-backend/app/services/grupo_fallback.py  # expect: match
grep -n "marker GRUPO_LEAD_ENTROU_CRIACAO < 5min" leadflow-backend/app/services/grupo_fallback.py  # expect: match

# === ALERTA-GRUPO-01 PRESERVADO ===
grep -n "skip envio alerta grupo (config 08/06" leadflow-backend/app/services/grupo_fallback.py  # expect: match

# === Markers v2.1 PRESERVADOS (não removidos por refactor) ===
grep -c "GRUPO_AGUARDANDO_ENTRADA" leadflow-backend/app/services/grupo_fallback.py  # expect: >= 3
grep -c "CONVITE_1_1_ENVIADO" leadflow-backend/app/services/grupo_fallback.py  # expect: >= 1
grep -c "LEAD_LID" leadflow-backend/app/services/grupo_fallback.py  # expect: >= 1
grep -c "_get_lead_lid_for_group" leadflow-backend/app/services/grupo_fallback.py  # expect: >= 7 (cada callsite tem)

# === Tratamento orphan adicionado (caso Fernanda) ===
grep -c "ORPHAN" leadflow-backend/app/services/grupo_fallback.py  # expect: >= 5
grep -c "instance_key_match" leadflow-backend/app/services/grupo_fallback.py  # expect: >= 5
grep -c "caso Fernanda" leadflow-backend/app/services/grupo_fallback.py  # expect: >= 5

# === Outros arquivos NÃO TOCADOS ===
git diff --stat leadflow-backend/app/services/evolution.py  # expect: vazio
git diff --stat leadflow-backend/app/services/probe_cache.py  # expect: vazio
git diff --stat leadflow-backend/app/services/warmup_grupo.py  # expect: vazio
git diff --stat leadflow-backend/app/services/grupo_state.py  # expect: vazio
git diff --stat leadflow-backend/app/routers/admin.py  # expect: vazio
git diff --stat leadflow-backend/app/routers/leads.py  # expect: vazio
git diff --stat leadflow-backend/app/routers/confirmacao_agendamento.py  # expect: vazio

# === Smoke import — modulo carrega sem erro ===
cd leadflow-backend && python -c "
from app.services.grupo_fallback import (
    ativar_fallback_se_necessario,
    _disparar_convite_lead_se_aguardando,
    _disparar_alerta_grupo_se_aguardando,
    polling_grupos_aguardando,
)
print('OK — todas funções importáveis')
"
```

Se qualquer check falhar, identificar exatamente qual e voltar à task correspondente.
  </action>
  <verify>
    <automated>cd c:/Projetos/Leadflow/leadflow-backend && python -c "from app.services.grupo_fallback import ativar_fallback_se_necessario, _disparar_convite_lead_se_aguardando, _disparar_alerta_grupo_se_aguardando, polling_grupos_aguardando; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Todos os greps da action retornam matches esperados
    - `git diff --stat` em evolution.py, probe_cache.py, warmup_grupo.py, grupo_state.py, admin.py, leads.py, confirmacao_agendamento.py: vazio em TODOS
    - Smoke import test passa para todas as 4 funções
    - 7 substituições confirmadas (count == 7 para `await lead_in_group`, count == 0 para `await verificar_lead_no_grupo`)
    - PROBE-BYPASS-01 + ALERTA-GRUPO-01 + markers v2.1 todos preservados via grep
  </acceptance_criteria>
  <done>
    Migração 7/7 completa. Zero callsites `verificar_lead_no_grupo` em `grupo_fallback.py`. PROBE-BYPASS-01 (marker bypass 5min), ALERTA-GRUPO-01 (skip envio), markers v2.1 todos PRESERVADOS via grep. Outros arquivos do escopo (admin, leads, confirmacao, warmup, evolution, probe_cache) NÃO tocados. Plano pronto para Plan 11-03 (testes).
  </done>
</task>

</tasks>

<verification>
1. `grep -c "await lead_in_group" leadflow-backend/app/services/grupo_fallback.py` == 7
2. `grep -c "await verificar_lead_no_grupo" leadflow-backend/app/services/grupo_fallback.py` == 0
3. `grep -nE "^from app\.services\.grupo_membership import lead_in_group$" leadflow-backend/app/services/grupo_fallback.py` retorna match
4. `grep -n "GRUPO_LEAD_ENTROU_CRIACAO" leadflow-backend/app/services/grupo_fallback.py` retorna match (PROBE-BYPASS-01 preservado)
5. `grep -n "bypass probe (NAO ativa)" leadflow-backend/app/services/grupo_fallback.py` retorna match
6. `grep -n "skip envio alerta grupo (config 08/06" leadflow-backend/app/services/grupo_fallback.py` retorna match (ALERTA-GRUPO-01 preservado)
7. `grep -c "ORPHAN" leadflow-backend/app/services/grupo_fallback.py` >= 5 (caso Fernanda tratado)
8. `git diff --stat leadflow-backend/app/services/evolution.py` vazio
9. `git diff --stat leadflow-backend/app/services/probe_cache.py` vazio
10. `git diff --stat leadflow-backend/app/routers/admin.py` vazio
11. `git diff --stat leadflow-backend/app/routers/leads.py` vazio
12. `git diff --stat leadflow-backend/app/routers/confirmacao_agendamento.py` vazio
13. `git diff --stat leadflow-backend/app/services/warmup_grupo.py` vazio
14. `git diff --stat leadflow-backend/app/services/grupo_state.py` vazio
15. `python -c "from app.services.grupo_fallback import ativar_fallback_se_necessario, polling_grupos_aguardando"` OK
</verification>

<success_criteria>
- [ ] 7 substituições mecânicas feitas em 6 blocos lógicos (linhas atuais 251, 311, 337, 477, 624, 1173, 1256)
- [ ] Import `from app.services.grupo_membership import lead_in_group` adicionado
- [ ] PROBE-BYPASS-01 (linhas 285-308) PRESERVADO LITERALMENTE
- [ ] ALERTA-GRUPO-01 (skip envio linha 647) PRESERVADO
- [ ] Markers v2.1 (LEAD_LID, GRUPO_AGUARDANDO_ENTRADA, CONVITE_1_1_ENVIADO) preservados
- [ ] Tratamento `source='orphan'` adicionado nos 6 blocos lógicos (caso Fernanda)
- [ ] Imports locais redundantes de `verificar_lead_no_grupo` (linhas 475, 622) REMOVIDOS
- [ ] Outros arquivos NÃO REGREDIDOS: evolution.py, probe_cache.py, warmup_grupo.py, grupo_state.py, admin.py, leads.py, confirmacao_agendamento.py SEM diff
- [ ] Module importa sem erro
- [ ] Kwargs explícitos usados em todas as chamadas (sb=, empresa_id=, telefone=, grupo_jid=, lid=, evo_url=, evo_key=, evo_inst=) — Pitfall 2 evitado
</success_criteria>

<output>
After completion, create `.planning/phases/11-lead-in-group-consumer-migrar-fallback-callers-coalescing-async/11-02-SUMMARY.md`
</output>
